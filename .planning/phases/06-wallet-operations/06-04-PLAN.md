---
phase: 06-wallet-operations
plan: "04"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/features/scanner-frame.tsx
  - src/app/(main)/scan/page.tsx
  - src/app/(main)/scan/receive-qr/page.tsx
  - src/app/(main)/withdraw/page.tsx
  - src/app/(main)/withdraw/amount/page.tsx
  - src/app/(main)/withdraw/receipt/page.tsx
autonomous: true
requirements: [SCAN-01, SCAN-02, WTHD-01, WTHD-02, WTHD-03]

must_haves:
  truths:
    - "QR scanner page displays camera view with scan frame overlay"
    - "QR scanner falls back to file input on iOS PWA"
    - "User can select a recipient for withdrawal"
    - "User can enter withdrawal amount with balance validation"
    - "User can confirm withdrawal with passcode"
  artifacts:
    - path: "src/app/(main)/scan/page.tsx"
      provides: "QR scanner page with camera and file fallback"
      min_lines: 50
    - path: "src/app/(main)/scan/receive-qr/page.tsx"
      provides: "Receive Money QR display with wallet ID"
      min_lines: 30
    - path: "src/components/features/scanner-frame.tsx"
      provides: "Scan overlay with dark bg and white corner markers"
    - path: "src/app/(main)/withdraw/page.tsx"
      provides: "Withdrawal recipient selection page"
      min_lines: 40
    - path: "src/app/(main)/withdraw/amount/page.tsx"
      provides: "Withdrawal amount entry with balance validation"
      min_lines: 60
    - path: "src/app/(main)/withdraw/receipt/page.tsx"
      provides: "Withdrawal success receipt"
      min_lines: 40
  key_links:
    - from: "src/app/(main)/scan/page.tsx"
      to: "navigator.mediaDevices.getUserMedia"
      via: "camera access API"
      pattern: "getUserMedia"
    - from: "src/app/(main)/withdraw/amount/page.tsx"
      to: "src/components/features/passcode-sheet.tsx"
      via: "PasscodeSheet for confirmation"
      pattern: "PasscodeSheet"
    - from: "src/app/(main)/withdraw/amount/page.tsx"
      to: "/api/mock-payment/withdraw"
      via: "fetch POST on passcode verified"
      pattern: "fetch.*mock-payment/withdraw"
---

<objective>
QR scanner page with camera overlay and file fallback, receive money QR display, and complete withdrawal flow (recipient selection, amount entry with balance validation, passcode confirmation, receipt).

Purpose: Users can scan QR codes (mock) and withdraw funds to existing recipients with passcode security.
Output: 6 pages, 1 feature component.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-wallet-operations/06-CONTEXT.md
@.planning/phases/06-wallet-operations/06-UI-SPEC.md
@.planning/phases/06-wallet-operations/06-01-SUMMARY.md
@.planning/phases/05-transfer-recipients/05-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/stores/wallet-ops-store.ts:
```typescript
// State: withdrawRecipientId, withdrawAmount
// Actions: setWithdrawRecipient, setWithdrawAmount, resetWithdraw
```

<!-- Reusable Phase 5 components -->

From src/components/features/passcode-sheet.tsx:
```typescript
export function PasscodeSheet({ open, onOpenChange, onVerified }: {
  open: boolean; onOpenChange: (open: boolean) => void; onVerified: () => void;
})
```

From src/components/features/recipient-list.tsx:
```typescript
export function RecipientList({ recipients, isLoading, onSelect, selectedId, onEdit, mutate }: {
  recipients: Recipient[]; isLoading: boolean; onSelect: (r: Recipient) => void;
  selectedId: string | null; onEdit: (id: string) => void; mutate: KeyedMutator<RecipientsData>;
})
```

From src/components/features/amount-input.tsx:
```typescript
export function AmountInput({ value, onChange, disabled }: { value: string; onChange: (val: string) => void; disabled?: boolean })
```

From src/components/features/transfer-receipt.tsx:
```typescript
export interface TransferReceiptProps {
  transactionId: string; amount: number; fee: number; /* ... */
}
export function TransferReceipt(props: TransferReceiptProps)
```

From src/hooks/use-wallet.ts:
```typescript
export function useWallet(): { wallet: Wallet | null; isLoading: boolean; mutate: KeyedMutator }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QR scanner page with camera overlay and receive money QR</name>
  <files>
    src/components/features/scanner-frame.tsx,
    src/app/(main)/scan/page.tsx,
    src/app/(main)/scan/receive-qr/page.tsx
  </files>
  <read_first>
    src/components/features/camera-overlay.tsx,
    src/app/(main)/scan/page.tsx,
    src/hooks/use-wallet.ts,
    src/lib/currency.ts
  </read_first>
  <action>
1. Create `src/components/features/scanner-frame.tsx` per D-09 and UI-SPEC Screen 3:
   - 'use client' component
   - Full-height overlay with dark mask rgba(0,0,0,0.6) covering area outside scan frame
   - Centered 240x240px transparent scan area
   - White L-shaped corner markers at 4 corners: 24px length, 3px width, using absolute positioned divs with white borders
   - "Point your camera at a QR code" instruction text below scan frame: 12px 400 #FFFFFF, text-center, mt-4
   - Implementation: Use CSS clip-path or 4 dark overlays (top, bottom, left, right) around the transparent center. Simplest: single overlay with a transparent "hole" via box-shadow: `box-shadow: 0 0 0 9999px rgba(0,0,0,0.6)` on the scan frame div.

2. Rewrite `src/app/(main)/scan/page.tsx` per D-09/D-10 and UI-SPEC Screen 3:
   - 'use client' page
   - Header: "Scan QR Code" title in #0091EA on #FFE600, h-14. X close icon (lucide X) right side, routes to /home. No back button (Scan is a bottom nav tab).
   - Camera view: use navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' } })
   - Display video feed in full remaining height, object-cover
   - ScannerFrame overlay on top of video
   - Permission handling: try/catch getUserMedia. On denial: hide camera view, show "Camera access denied. Use gallery to scan." in #757575 centered
   - Bottom controls bar: absolute bottom with safe-area padding
     - "Upload from Gallery" button: outline, border-white, text-white, rounded-full, h-12
     - Hidden `<input type="file" accept="image/*">` triggered by gallery button (per D-10, mock only — no actual QR decoding)
     - "Receive Money with QR" button: bg-#FFE600, text-#212121, rounded-full, h-12, mt-3, navigates to /scan/receive-qr
   - Cleanup: stop all video tracks on unmount via useEffect cleanup
   - Mock behavior: file selection shows Sonner toast "QR code scanned (mock)" — no actual decoding in Phase 6

3. Create `src/app/(main)/scan/receive-qr/page.tsx` per D-11 and UI-SPEC Screen 4:
   - 'use client' page (create directory)
   - BackHeader with "Receive Money" title
   - White content area with QR card: bg-white, rounded-2xl, shadow-md, p-6
   - 2C2P logo placeholder text, centered
   - User wallet ID label: 12px 400 #757575
   - QR code: `<QRCode value={walletId} size={200} />` from react-qr-code, centered
   - Wallet ID display: 16px 700 #212121, text-center, mt-3
   - Get wallet ID from useWallet hook
   - "Share QR" button: bg-#FFE600, rounded-full, h-14, full-width, mt-6
   - Share implementation: try navigator.share({ text: walletId }), catch fallback to navigator.clipboard.writeText(walletId) + Sonner toast "Wallet ID copied"
   - Loading: skeleton while wallet loads
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/scan/page.tsx uses getUserMedia with facingMode environment
    - src/components/features/scanner-frame.tsx renders 240x240 frame with white corner markers
    - Camera denied state shows "Upload from Gallery" only with denial message
    - File input accepts image/* for iOS PWA fallback
    - src/app/(main)/scan/receive-qr/page.tsx displays wallet QR code with share functionality
    - Video tracks cleaned up on unmount
  </acceptance_criteria>
  <done>QR scanner with camera overlay, file fallback, permission handling, and receive money QR display all functional</done>
</task>

<task type="auto">
  <name>Task 2: Withdrawal flow — recipient selection, amount entry, passcode, receipt</name>
  <files>
    src/app/(main)/withdraw/page.tsx,
    src/app/(main)/withdraw/amount/page.tsx,
    src/app/(main)/withdraw/receipt/page.tsx
  </files>
  <read_first>
    src/components/features/recipient-list.tsx,
    src/components/features/amount-input.tsx,
    src/components/features/passcode-sheet.tsx,
    src/components/features/transfer-receipt.tsx,
    src/app/(main)/transfer/confirm/page.tsx,
    src/app/(main)/transfer/receipt/page.tsx,
    src/stores/wallet-ops-store.ts,
    src/hooks/use-wallet.ts,
    src/app/(main)/withdraw/page.tsx
  </read_first>
  <action>
1. Rewrite `src/app/(main)/withdraw/page.tsx` per D-06/D-07 and UI-SPEC Screen 7:
   - 'use client' page
   - BackHeader with "Withdraw" title
   - Fetch recipients via SWR: `useSWR('/api/recipients', fetcher)` — same pattern as Phase 5 transfer page
   - Reuse RecipientList component with onSelect handler
   - No "Create new recipient" link — withdrawal uses existing recipients only (per UI-SPEC)
   - On recipient select: store recipient ID in wallet-ops-store via setWithdrawRecipient, navigate to /withdraw/amount?recipientId={id}
   - Loading: RecipientList handles its own loading state

2. Create `src/app/(main)/withdraw/amount/page.tsx` per D-07 and UI-SPEC Screen 8:
   - 'use client' page (create directory)
   - BackHeader with "Withdraw" title
   - Get recipientId from searchParams
   - Fetch recipient details from SWR cache or API
   - Recipient summary card: flex items-center gap-3, bg-#F5F5F5, rounded-xl, px-4 py-3, mb-6
     - Avatar: 40px circle bg-#FFE600, initial letter 16px 700 #212121
     - Name: 16px 700 #212121
     - Transfer type: 12px 400 #757575
   - Reuse AmountInput component for amount entry
   - "Available: {balance} THB" line: 12px 400 #757575, text-center, mt-1
   - Balance validation: if amount > wallet balance, show "Insufficient balance. Enter an amount up to {available} THB." in 12px #F44336
   - Sticky bottom "Withdraw" CTA: bg-#FFE600, rounded-full, h-14, disabled when amount = 0 or amount > balance
   - On "Withdraw" tap: open PasscodeSheet
   - PasscodeSheet onVerified: POST to `/api/mock-payment/withdraw` with { amount: amountInSatang, recipient_id: recipientId }
   - On success: store transaction_id, navigate to /withdraw/receipt
   - On failure: show Sonner toast with error message per UI-SPEC

3. Create `src/app/(main)/withdraw/receipt/page.tsx` per UI-SPEC Screen 9 post-confirmation:
   - 'use client' page (create directory)
   - Custom header with X close button (not BackHeader — terminal screen, same pattern as Phase 5 transfer receipt)
   - Display receipt using a simplified TransactionDetail-like layout (or reuse TransactionDetail from Plan 03 if available, but since Plan 03 is parallel, build inline):
     - Status badge: "Completed" with green semantic colors
     - Receipt card: bg-white, rounded-2xl, shadow-sm, p-4
     - Rows: Date/Time, Type "Withdrawal", Reference No., Recipient name, Amount, Fee (0 for withdrawals), Total
   - "Done" CTA: bg-#FFE600, rounded-full, h-14, navigates to /home
   - Trigger wallet SWR mutate on mount for balance refresh
   - Get transaction data from wallet-ops-store or from API via transaction_id stored in component state / searchParams
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/withdraw/page.tsx renders RecipientList and navigates on selection
    - src/app/(main)/withdraw/amount/page.tsx shows recipient summary, AmountInput, balance validation
    - Amount > balance shows insufficient balance error and disables CTA
    - PasscodeSheet opens on "Withdraw" tap
    - POST to /api/mock-payment/withdraw fires on passcode verified
    - src/app/(main)/withdraw/receipt/page.tsx shows withdrawal receipt with "Done" navigation to /home
    - Wallet balance revalidates after withdrawal
  </acceptance_criteria>
  <done>Complete withdrawal flow: select recipient, enter amount with balance check, confirm with passcode, see receipt</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- QR scanner shows camera with overlay and handles permission denial
- File picker fallback works
- Receive Money QR displays wallet ID
- Withdrawal flow completes: recipient -> amount -> passcode -> receipt
- Balance updates after withdrawal
</verification>

<success_criteria>
QR scanner with camera overlay and file fallback operational. Withdrawal flow from recipient selection through passcode confirmation to receipt complete. Balance validated and updated throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/06-wallet-operations/06-04-SUMMARY.md`
</output>
