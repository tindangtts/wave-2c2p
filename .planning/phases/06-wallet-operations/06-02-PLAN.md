---
phase: 06-wallet-operations
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/features/add-money-header.tsx
  - src/components/features/bank-channel-grid.tsx
  - src/components/features/convenience-channel-list.tsx
  - src/components/features/qr-display.tsx
  - src/components/features/qr-expiry-timer.tsx
  - src/app/(main)/add-money/page.tsx
  - src/app/(main)/add-money/qr/page.tsx
autonomous: true
requirements: [ADDM-01, ADDM-02, ADDM-03, ADDM-04, ADDM-05]

must_haves:
  truths:
    - "User can enter a top-up amount with balance and max limit displayed"
    - "User can select a bank or convenience store channel from the grid"
    - "QR code displays with payment details and expiration countdown"
    - "QR timer color shifts at thresholds (>5m normal, 1-5m orange, <60s red)"
    - "Expired QR dims and shows generate new button"
  artifacts:
    - path: "src/app/(main)/add-money/page.tsx"
      provides: "Add Money amount entry + channel selection page"
      min_lines: 80
    - path: "src/app/(main)/add-money/qr/page.tsx"
      provides: "QR code display with expiry timer"
      min_lines: 60
    - path: "src/components/features/bank-channel-grid.tsx"
      provides: "3x2 bank icon button grid"
    - path: "src/components/features/qr-display.tsx"
      provides: "QR code card with payment details"
    - path: "src/components/features/qr-expiry-timer.tsx"
      provides: "MM:SS countdown with color thresholds"
  key_links:
    - from: "src/app/(main)/add-money/page.tsx"
      to: "/add-money/qr"
      via: "router.push with channel and amount query params"
      pattern: "router.push.*add-money/qr"
    - from: "src/app/(main)/add-money/qr/page.tsx"
      to: "/api/mock-payment/topup"
      via: "fetch POST on mount"
      pattern: "fetch.*mock-payment/topup"
    - from: "src/components/features/qr-display.tsx"
      to: "react-qr-code"
      via: "QRCode component"
      pattern: "import.*react-qr-code"
---

<objective>
Complete Add Money flow: amount entry page with balance/max display and bank/convenience channel grid, plus QR code display page with expiry countdown timer.

Purpose: Users can top up their wallet by selecting an amount and payment channel, then scanning a QR code at their bank or convenience store.
Output: 2 pages, 5 feature components, react-qr-code installed.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-wallet-operations/06-CONTEXT.md
@.planning/phases/06-wallet-operations/06-UI-SPEC.md
@.planning/phases/06-wallet-operations/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/stores/wallet-ops-store.ts:
```typescript
export const useWalletOpsStore = create<WalletOpsState>()(
  persist(/* ... */)
)
// State: topupAmount, topupChannel, withdrawAmount, withdrawRecipientId
// Actions: setTopupAmount, setTopupChannel, resetTopup, ...
```

From src/lib/wallet/schemas.ts:
```typescript
export const topupAmountSchema = z./* min 15000, max 2500000 satang */
export const topupChannelSchema = z.enum(['scb','ktb','bay','bbl','kbank','gsb','service_123','cenpay'])
```

From src/hooks/use-wallet.ts:
```typescript
export function useWallet(): { wallet: Wallet | null; isLoading: boolean; mutate: KeyedMutator }
```

From src/components/features/amount-input.tsx:
```typescript
export function AmountInput({ value, onChange, disabled }: { value: string; onChange: (val: string) => void; disabled?: boolean })
```

From src/components/layout/back-header.tsx:
```typescript
// BackHeader with title, blue text on yellow bg — standard pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Money page with amount entry and channel selection</name>
  <files>
    src/components/features/bank-channel-grid.tsx,
    src/components/features/convenience-channel-list.tsx,
    src/app/(main)/add-money/page.tsx
  </files>
  <read_first>
    src/components/features/amount-input.tsx,
    src/components/layout/back-header.tsx,
    src/hooks/use-wallet.ts,
    src/stores/wallet-ops-store.ts,
    src/lib/wallet/schemas.ts,
    src/app/(main)/add-money/page.tsx
  </read_first>
  <action>
1. Install react-qr-code: `npm install react-qr-code`

2. Create `src/components/features/bank-channel-grid.tsx` per D-03 and UI-SPEC Screen 1:
   - 'use client' component
   - 6 bank buttons in 3x2 grid: SCB, KTB, Bay (Krungsri), BBL, KBANK, GSB
   - Each button: 88px wide x 72px tall, bg-white, border 1px #E0E0E0, rounded-xl, active:bg-#F5F5F5
   - Bank logo placeholder: 40x40px rounded-lg div with bank abbreviation text (no actual logo images — use colored bg + text abbreviation: SCB purple, KTB blue, Bay yellow, BBL dark blue, KBANK green, GSB pink)
   - 12px 400 #212121 label below icon
   - Props: `onSelect: (channel: string) => void`
   - Grid layout: `grid grid-cols-3 gap-3`

3. Create `src/components/features/convenience-channel-list.tsx` per D-03:
   - 'use client' component
   - Two rows: "123 Service" and "CenPay"
   - Each row: h-14 (56px), flex items-center, px-4, border-b #E0E0E0 (last has no border)
   - Left: colored placeholder icon (40x40 rounded-full), right: ChevronRight lucide icon
   - Props: `onSelect: (channel: string) => void`

4. Rewrite `src/app/(main)/add-money/page.tsx` per UI-SPEC Screen 1:
   - 'use client' page
   - BackHeader with "Add Money" title (use useTranslations('wallet'))
   - Balance info block: "Wallet Balance: {formatted}" and "Max Top-up: 25,000.00 THB" in 12px #757575
   - Reuse AmountInput component from Phase 5 for amount entry
   - "Minimum top-up: 150 THB" caption below amount when amount < 150 and > 0, in 12px #F44336
   - "Maximum top-up is 25,000 THB" caption when amount > 25000 in 12px #F44336
   - Section headings: "Top-up Channels" (16px 700), "Top-up via Banking Services" (12px 400 #757575), "Top-up via 123 Service" (12px 400 #757575)
   - BankChannelGrid and ConvenienceChannelList components
   - On channel select: store amount + channel in wallet-ops-store, then router.push(`/add-money/qr?channel=${channel}&amount=${amountSatang}`)
   - Sticky bottom "Continue" CTA: rounded-full, bg-#FFE600, text-#212121, full-width, h-14, disabled when amount < 150 or > 25000 or 0
   - NOTE: The "Continue" button is disabled until amount is valid AND a channel is selected. Channel selection happens via tapping a bank/convenience option which both sets channel AND navigates to QR page.
   - Actually per UI-SPEC state transitions: bank/convenience tap navigates directly — no separate "Continue" needed for channel. "Continue" validates amount, then user picks channel below. Tapping a channel icon navigates. So: "Continue" scrolls to channel section or is hidden. Simpler: remove "Continue" CTA. User enters amount, then taps a channel to proceed. BUT UI-SPEC explicitly shows "Continue" CTA. Resolution: "Continue" is disabled until amount valid. Once valid, channel section is interactive. Tapping a channel navigates to QR page. "Continue" can alternatively scroll-to-channels. Keep "Continue" as the navigation trigger: user enters amount, taps Continue, which enables channel selection (or just let channels be always tappable when amount valid). Simplest: channels are always visible but tapping one when amount invalid shows error toast. When amount valid, tapping channel navigates.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/add-money/page.tsx renders BackHeader, balance info, AmountInput, BankChannelGrid, ConvenienceChannelList
    - src/components/features/bank-channel-grid.tsx renders 6 bank buttons in 3x2 grid
    - src/components/features/convenience-channel-list.tsx renders 2 convenience store rows
    - Channel tap with valid amount navigates to /add-money/qr with query params
    - Amount validation shows min/max error captions
    - react-qr-code installed in package.json
  </acceptance_criteria>
  <done>Add Money page renders with amount input, bank grid, convenience list, and navigates to QR page on channel selection</done>
</task>

<task type="auto">
  <name>Task 2: QR code display page with expiry timer</name>
  <files>
    src/components/features/qr-display.tsx,
    src/components/features/qr-expiry-timer.tsx,
    src/app/(main)/add-money/qr/page.tsx
  </files>
  <read_first>
    src/components/features/rate-timer.tsx,
    src/components/features/transfer-receipt.tsx,
    src/stores/wallet-ops-store.ts,
    src/hooks/use-wallet.ts,
    src/lib/currency.ts
  </read_first>
  <action>
1. Create `src/components/features/qr-expiry-timer.tsx` per D-04 and UI-SPEC interaction contract:
   - 'use client' component
   - Props: `expiresAt: string (ISO date)`, `onExpired: () => void`
   - Uses setInterval (1s) to count down
   - Displays "Expires in MM:SS"
   - Color thresholds: > 5min = #212121, 1-5min = #FF9800, < 60s = #F44336
   - 12px weight 700, text-center
   - Calls onExpired when timer reaches 0
   - Cleans up interval on unmount

2. Create `src/components/features/qr-display.tsx` per D-04 and UI-SPEC Screen 2:
   - 'use client' component
   - Props: `paymentCode: string, amount: number (satang), merchantName: string, expiresAt: string, expired: boolean`
   - White card: rounded-2xl, shadow-md, p-6
   - 2C2P logo placeholder: h-8 centered text "2C2P WAVE" in blue, mb-2
   - Merchant name: 12px 400 #757575, text-center, mb-4
   - QR code: `<QRCode value={paymentCode} size={200} bgColor="#FFFFFF" fgColor="#212121" />` from react-qr-code, centered, mb-4
   - When expired: QR gets opacity-40 overlay
   - Payment details: "Payment Code:" label + value, "Amount (THB):" label + formatted amount
   - Divider 1px #E0E0E0
   - "Please pay before {formatted time}" in 12px 400 #757575

3. Create/rewrite `src/app/(main)/add-money/qr/page.tsx` per UI-SPEC Screen 2:
   - 'use client' page (create directory if needed: `src/app/(main)/add-money/qr/`)
   - Reads channel and amount from searchParams (useSearchParams)
   - On mount: POST to `/api/mock-payment/topup` with { amount, channel }
   - Stores response: transaction_id, qr_data (paymentCode, amount, merchantName, expiresAt)
   - BackHeader with "Top-up with QR" title
   - QRDisplay component with payment data
   - QRExpiryTimer below QR card
   - On expired: set expired state, replace "View Instructions" with "Generate New QR" button
   - CTA group: "View Instructions" (outline, border-#0091EA, text-#0091EA, rounded-full) and "Done" (bg-#FFE600, rounded-full, h-14)
   - "Done" navigates to /add-money and calls wallet SWR mutate for balance refresh
   - Loading state: spinner in QR area while API responds
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/add-money/qr/page.tsx calls POST /api/mock-payment/topup on mount
    - src/components/features/qr-display.tsx renders QRCode from react-qr-code with payment details
    - src/components/features/qr-expiry-timer.tsx counts down with color thresholds
    - Expired state dims QR and shows "Generate New QR" button
    - "Done" button navigates to /add-money and triggers wallet revalidation
  </acceptance_criteria>
  <done>QR code page displays with payment details, countdown timer with color thresholds, expired state handling, and Done navigation</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Add Money page shows balance, amount input, bank grid, convenience list
- QR page shows QR code, payment details, countdown timer
- Timer color shifts work at thresholds
- Expired QR dims and shows regenerate option
</verification>

<success_criteria>
Complete Add Money flow: user enters amount, selects bank/convenience channel, sees QR code with expiry countdown, can complete or regenerate on expiry.
</success_criteria>

<output>
After completion, create `.planning/phases/06-wallet-operations/06-02-SUMMARY.md`
</output>
