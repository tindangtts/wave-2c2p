---
phase: 06-wallet-operations
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/features/history-filter-bar.tsx
  - src/components/features/date-range-picker.tsx
  - src/components/features/transaction-row.tsx
  - src/components/features/transaction-detail.tsx
  - src/app/(main)/history/page.tsx
  - src/app/(main)/history/[id]/page.tsx
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]

must_haves:
  truths:
    - "User can view chronological transaction list with infinite scroll"
    - "User can filter by date range using calendar picker"
    - "User can filter by transaction type and status chips"
    - "User can view transaction detail with full receipt breakdown"
    - "Date picker displays Thai Buddhist calendar years correctly"
  artifacts:
    - path: "src/app/(main)/history/page.tsx"
      provides: "Transaction history page with infinite scroll and filters"
      min_lines: 80
    - path: "src/app/(main)/history/[id]/page.tsx"
      provides: "Transaction detail page with receipt layout"
      min_lines: 50
    - path: "src/components/features/history-filter-bar.tsx"
      provides: "Type + status filter chips"
    - path: "src/components/features/date-range-picker.tsx"
      provides: "Calendar picker with Buddhist year display"
    - path: "src/components/features/transaction-row.tsx"
      provides: "Transaction row with icon, name, amount, status"
    - path: "src/components/features/transaction-detail.tsx"
      provides: "Full receipt card matching TransferReceipt pattern"
  key_links:
    - from: "src/app/(main)/history/page.tsx"
      to: "src/hooks/use-transactions.ts"
      via: "useTransactions hook with filters"
      pattern: "useTransactions"
    - from: "src/app/(main)/history/page.tsx"
      to: "/history/[id]"
      via: "router.push on row tap"
      pattern: "router.push.*history/"
    - from: "src/components/features/date-range-picker.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "shadcn Calendar in Popover"
      pattern: "Calendar.*mode=\"range\""
---

<objective>
Transaction history with infinite scroll, filter chips (type + status), date range picker with Buddhist calendar years, and transaction detail receipt view.

Purpose: Users can review their complete transaction history with powerful filtering and see full details of any transaction.
Output: 2 pages, 4 feature components.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-wallet-operations/06-CONTEXT.md
@.planning/phases/06-wallet-operations/06-UI-SPEC.md
@.planning/phases/06-wallet-operations/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/hooks/use-transactions.ts:
```typescript
export function useTransactions(filters: { type?: string; status?: string; dateFrom?: string; dateTo?: string }):
  { transactions: Transaction[]; isLoading: boolean; isLoadingMore: boolean; isEmpty: boolean; isReachingEnd: boolean; size: number; setSize: (s: number | ((s: number) => number)) => void; mutate: KeyedMutator }
```

From src/types/index.ts:
```typescript
export type TransactionStatus = "success" | "pending" | "rejected" | "processing" | "failed";
export type TransactionType = "add_money" | "send_money" | "withdraw" | "receive" | "bill_payment";
export interface Transaction { id, type, amount, currency, fee, status, recipient_id, channel, reference_number, description, created_at, updated_at }
```

From src/components/features/transfer-receipt.tsx:
```typescript
export interface TransferReceiptProps {
  transactionId: string; amount: number; fee: number; convertedPya: number;
  rate: number; channel: TransferChannel; senderName: string; senderPhone: string;
  // ... receipt display component
}
```

From src/components/features/recent-history.tsx:
```typescript
// Transaction row pattern: type icon circle (40px), name + date (2 lines), amount + status badge
```

From src/components/ui/calendar.tsx + popover.tsx (shadcn):
```typescript
// Calendar component with mode="range" for date range selection
// Popover + PopoverTrigger + PopoverContent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter bar, date range picker, transaction row components</name>
  <files>
    src/components/features/history-filter-bar.tsx,
    src/components/features/date-range-picker.tsx,
    src/components/features/transaction-row.tsx
  </files>
  <read_first>
    src/components/features/recent-history.tsx,
    src/components/ui/calendar.tsx,
    src/components/ui/popover.tsx,
    src/lib/currency.ts
  </read_first>
  <action>
1. Create `src/components/features/history-filter-bar.tsx` per D-14 and UI-SPEC Screen 5:
   - 'use client' component
   - Props: `typeFilter: string, statusFilter: string, dateRange: { from?: Date, to?: Date }, onTypeChange: (type: string) => void, onStatusChange: (status: string) => void, onDateRangeChange: (range: { from?: Date, to?: Date }) => void`
   - Row 1: DateRangePicker trigger (full-width)
   - Row 2: Type filter chips in horizontal scroll (overflow-x-auto, no scrollbar via `scrollbar-hide` class or `[&::-webkit-scrollbar]:hidden`): "All", "Transfer", "Top-up", "Withdrawal" per D-14
   - Row 3: Status filter chips: "All", "Success", "Pending", "Failed" per D-14
   - Chip styling: rounded-full, h-8, px-3, 12px 400. Active: bg-#FFE600 text-#212121. Inactive: bg-#F5F5F5 text-#757575
   - "All" chip resets the filter for that row
   - Map chip labels to TransactionType values: "Transfer" -> "send_money", "Top-up" -> "add_money", "Withdrawal" -> "withdraw"
   - Map status labels: "Success" -> "success", "Pending" -> "pending", "Failed" -> "failed"
   - Use useTranslations('wallet') for chip labels

2. Create `src/components/features/date-range-picker.tsx` per D-13 and UI-SPEC:
   - 'use client' component
   - Props: `dateRange: { from?: Date, to?: Date }, onChange: (range: { from?: Date, to?: Date }) => void`
   - Uses shadcn Popover + Calendar with mode="range"
   - Trigger button: outline, rounded-xl, h-11, px-3, Calendar icon (lucide) left, date range text right
   - Placeholder: "Select date range" when no dates selected
   - Selected: "Apr 1 - Apr 14" format using date-fns `format`
   - Buddhist calendar year display per D-13/HIST-05: For Thai locale, display year as CE + 543 (e.g., 2026 -> 2569). Detect locale via `useLocale()` from next-intl. If locale === 'th', format year with +543 offset. Implementation: custom `formatCaption` prop on Calendar that adds 543 to year for 'th' locale.
   - "Clear" button in popover footer to reset date range
   - Close popover on second date selection (range complete)

3. Create `src/components/features/transaction-row.tsx` per D-15 and UI-SPEC Screen 5:
   - 'use client' component
   - Props: `transaction: Transaction, onClick: (id: string) => void`
   - Layout: flex items-center gap-3 px-4 py-3 min-h-[60px], border-b border-#F5F5F5
   - Left: type icon circle 40px rounded-full
     - Transfer (send_money): bg-#E3F2FD, ArrowUpRight icon #0091EA
     - Top-up (add_money): bg-#E8F5E9, Plus icon #00C853
     - Withdrawal (withdraw): bg-#FFF3E0, ArrowDownLeft icon #FF9800
     - Receive: bg-#E8F5E9, ArrowDownLeft icon #00C853
   - Center: two lines — recipient/description 16px 400 #212121, date+time 12px 400 #9E9E9E ("Apr 14, 10:23 AM" via date-fns format)
   - Right: amount 16px 700 (credit +amount #00C853, debit -amount #F44336) + status badge (10px, semantic colors from UI-SPEC)
   - Credit types: add_money, receive. Debit types: send_money, withdraw.
   - Amount formatted with `formatCurrency` from src/lib/currency.ts (satang to baht display)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/history-filter-bar.tsx renders date picker trigger + type chips + status chips
    - src/components/features/date-range-picker.tsx uses shadcn Calendar in Popover with mode="range"
    - src/components/features/date-range-picker.tsx adds 543 to year display for Thai locale
    - src/components/features/transaction-row.tsx renders icon circle, name+date, amount+badge
    - Filter chips use yellow active / gray inactive styling
  </acceptance_criteria>
  <done>Three filter/display components compile clean and follow UI-SPEC spacing, colors, and typography</done>
</task>

<task type="auto">
  <name>Task 2: History page with infinite scroll and transaction detail page</name>
  <files>
    src/components/features/transaction-detail.tsx,
    src/app/(main)/history/page.tsx,
    src/app/(main)/history/[id]/page.tsx
  </files>
  <read_first>
    src/components/features/transfer-receipt.tsx,
    src/hooks/use-transactions.ts,
    src/components/features/history-filter-bar.tsx,
    src/components/features/transaction-row.tsx,
    src/app/(main)/history/page.tsx
  </read_first>
  <action>
1. Create `src/components/features/transaction-detail.tsx` per D-16 and UI-SPEC Screen 6:
   - 'use client' component reusing TransferReceipt card layout pattern
   - Props: `transaction: Transaction` (full transaction object)
   - Status badge row: centered, semantic colors (success green bg, pending orange bg, failed red bg)
   - Receipt card: bg-white, rounded-2xl, shadow-sm, p-4
   - 2C2P WAVE logo placeholder: h-8, centered, mb-4
   - Breakdown rows (label left 16px 400 #757575, value right 16px 700 #212121):
     - Date/Time (formatted with date-fns)
     - Type (human readable: "Transfer", "Top-up", "Withdrawal")
     - Reference No. (monospace 12px 400 #757575)
     - Sender / Recipient (if available)
     - Separator line 1px #E0E0E0
     - Amount, Fee (in #F44336), Total (20px 700)
   - Use useTranslations('wallet') for labels

2. Rewrite `src/app/(main)/history/page.tsx` per UI-SPEC Screen 5:
   - 'use client' page
   - BackHeader with "Transaction History" title
   - State: typeFilter (default "all"), statusFilter (default "all"), dateRange ({ from?: Date, to?: Date })
   - HistoryFilterBar component with all filter state and handlers
   - useTransactions hook with filters: pass type (unless "all"), status (unless "all"), dateFrom (ISO string), dateTo (ISO string)
   - Infinite scroll list:
     - Group transactions by date: "Today", "Yesterday", or formatted date ("April 12, 2026")
     - Date group headers: sticky, 12px 400 #9E9E9E, mx-4, py-2, bg-white
     - Use date-fns `isToday`, `isYesterday`, `format` for grouping
     - TransactionRow for each transaction, onClick navigates to `/history/${tx.id}`
   - Loading: 5 skeleton rows on initial load (animate-pulse, bg-#F5F5F5 blocks matching row shape)
   - Next page loading: 3 skeleton rows at bottom
   - Infinite scroll trigger: IntersectionObserver on sentinel div (20px) at bottom of list
     - When sentinel visible and !isReachingEnd and !isLoadingMore: call setSize(size + 1)
   - Empty state: centered, "No transactions found" + "Try adjusting your filters" (or "No transactions yet" + "Your transaction history will appear here" if no filters active)

3. Create directory and rewrite `src/app/(main)/history/[id]/page.tsx` per UI-SPEC Screen 6:
   - 'use client' page
   - Gets transaction ID from params (async params in Next.js 16 — use `use(params)` or `await params`)
   - Fetches single transaction via SWR: `useSWR('/api/transactions/${id}', fetcher)` — OR filter from cache if available. Simplest: fetch `/api/transactions?id=${id}` or add a dedicated GET endpoint. Since transactions API returns array, add id filter support: when `id` searchParam is provided, return single transaction. Alternative: use the existing transaction data from SWR cache in the history page's mutate. Simplest approach: GET `/api/transactions?id={id}` returns the single transaction.
   - BackHeader with "Transaction Detail" title
   - TransactionDetail component with transaction data
   - Sticky bottom "Close" CTA: bg-#FFE600, rounded-full, h-14, navigates to /history
   - Loading state: skeleton receipt card (6 rows animated)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/history/page.tsx uses useTransactions with filters and renders infinite scroll list
    - History page groups transactions by date with sticky headers
    - IntersectionObserver triggers next page load
    - Empty state displays when no transactions match filters
    - src/app/(main)/history/[id]/page.tsx renders TransactionDetail with full receipt breakdown
    - src/components/features/transaction-detail.tsx shows status badge, receipt rows, amount/fee/total
    - "Close" button navigates back to /history
  </acceptance_criteria>
  <done>Transaction history with infinite scroll, date grouping, filters, and detail page with receipt breakdown all functional</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- History page renders filter bar, date picker, infinite scroll list
- Transaction rows show correct icons, amounts (credit green / debit red), status badges
- Date range picker shows Buddhist year for Thai locale
- Detail page shows full receipt with all fields
</verification>

<success_criteria>
Complete transaction history: infinite scroll with 20-per-page loading, type + status filter chips, date range picker with Buddhist calendar, date-grouped list, and detail receipt view.
</success_criteria>

<output>
After completion, create `.planning/phases/06-wallet-operations/06-03-SUMMARY.md`
</output>
