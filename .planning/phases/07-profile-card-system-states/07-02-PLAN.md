---
phase: 07-profile-card-system-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/app-error-boundary.tsx
  - src/components/features/maintenance-modal.tsx
  - src/components/features/update-required-modal.tsx
  - src/components/features/system-state-checker.tsx
  - src/components/features/page-skeleton.tsx
  - src/app/layout.tsx
  - src/app/(main)/profile/loading.tsx
  - src/app/error.tsx
autonomous: true
requirements:
  - SYST-01
  - SYST-02
  - SYST-03
  - SYST-04
  - SYST-05

must_haves:
  truths:
    - "Maintenance modal shows when SYSTEM_MAINTENANCE env var is true"
    - "Update required modal shows when SYSTEM_UPDATE_REQUIRED env var is true"
    - "Error boundary catches render errors and shows friendly retry UI"
    - "Loading skeletons display during data fetching on profile pages"
    - "System modals are non-dismissible (no overlay click dismiss)"
  artifacts:
    - path: "src/components/features/maintenance-modal.tsx"
      provides: "Maintenance AlertDialog per D-13"
    - path: "src/components/features/update-required-modal.tsx"
      provides: "Update Required AlertDialog per D-14"
    - path: "src/components/layout/app-error-boundary.tsx"
      provides: "React error boundary with Try Again per D-15"
    - path: "src/components/features/page-skeleton.tsx"
      provides: "Shimmer skeleton components per D-16"
    - path: "src/app/error.tsx"
      provides: "Next.js error boundary page"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/features/system-state-checker.tsx"
      via: "rendered inside NextIntlClientProvider"
      pattern: "SystemStateChecker"
    - from: "src/components/features/system-state-checker.tsx"
      to: "src/components/features/maintenance-modal.tsx"
      via: "conditional render based on API check"
      pattern: "MaintenanceModal"
---

<objective>
Build system state screens: maintenance modal (D-13), update required modal (D-14), error boundary (D-15), and loading skeletons (D-16). These are app-level concerns that wrap the entire application.

Purpose: Ensures the app handles failure states gracefully — maintenance downtime, forced updates, unhandled errors, and loading states all have proper UI.
Output: System modals triggered by env vars, error boundary in root layout, skeleton components.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profile-card-system-states/07-CONTEXT.md
@.planning/phases/07-profile-card-system-states/07-UI-SPEC.md

<interfaces>
From src/app/layout.tsx:
- Root layout is async Server Component
- Wraps children in NextIntlClientProvider
- Container: mx-auto w-full max-w-[430px] min-h-dvh flex flex-col

From src/components/ui/alert-dialog.tsx (already installed):
- AlertDialog, AlertDialogContent, AlertDialogAction, AlertDialogCancel, AlertDialogTitle, AlertDialogDescription

From src/components/ui/skeleton.tsx (already installed):
- Skeleton component with className prop, uses animate-pulse
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: System modals (maintenance + update) and env var checker</name>
  <files>
    src/components/features/maintenance-modal.tsx
    src/components/features/update-required-modal.tsx
    src/components/features/system-state-checker.tsx
    src/app/api/system-status/route.ts
  </files>
  <read_first>
    - src/components/ui/alert-dialog.tsx (AlertDialog API)
    - src/components/features/kyc-expired-modal.tsx (existing AlertDialog modal pattern)
    - src/app/layout.tsx (where to mount system checker)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 586-608 system modal layouts)
  </read_first>
  <action>
    1. Create `src/app/api/system-status/route.ts` — GET endpoint that reads env vars `SYSTEM_MAINTENANCE` and `SYSTEM_UPDATE_REQUIRED` from `process.env` and returns JSON: `{ maintenance: boolean, updateRequired: boolean }`. This keeps env var reading server-side (env vars with SYSTEM_ prefix are NOT prefixed with NEXT_PUBLIC_ so they are server-only).
    2. Create `MaintenanceModal` ('use client') per D-13 and UI-SPEC Screen 7:
       - AlertDialog with `open` prop (controlled, no close on overlay click — omit AlertDialogCancel and use `onOpenAutoFocus` to prevent escape).
       - AlertDialogContent: bg-white rounded-2xl max-w-sm mx-4 p-6 text-center.
       - Wrench icon (lucide `Wrench`) 48x48 color #FF9800 mb-4 mx-auto.
       - Title: "System Under Maintenance" — 20px bold #212121.
       - Body: "Currently system is under maintenance. We will be back soon." — 12px 400 #757575.
       - AlertDialogAction: "Ok" — rounded-full bg-[#FFE600] text-[#212121] h-12 full-width. On click: dismiss (but modal re-checks on next navigation).
       - Props: `open: boolean`, `onClose: () => void`.
    3. Create `UpdateRequiredModal` ('use client') per D-14:
       - Same container style as maintenance.
       - Download icon (lucide `Download`) 48x48 color #0091EA.
       - Title: "Software Update" — 20px bold.
       - Body: "A software update is required to continue using the app." — 12px 400 #757575.
       - Two buttons in flex row gap-3: "Quit" (outline, rounded-full h-12 flex-1 border-[#E0E0E0] text-#212121) and "Now" (bg-[#FFE600] text-#212121 rounded-full h-12 flex-1). Both can dismiss for mock purposes.
       - Props: `open: boolean`.
    4. Create `SystemStateChecker` ('use client') — mounted once in root layout. On mount, fetches `/api/system-status`. If `maintenance` is true, renders `MaintenanceModal open={true}`. If `updateRequired` is true, renders `UpdateRequiredModal open={true}`. Uses `useState` to track status. Fetches once on mount (no polling needed for mock).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MaintenanceModal renders AlertDialog with Wrench icon, title, body, Ok button
    - UpdateRequiredModal renders AlertDialog with Download icon, title, body, Quit/Now buttons
    - SystemStateChecker fetches /api/system-status and conditionally renders modals
    - /api/system-status route returns { maintenance: boolean, updateRequired: boolean }
  </acceptance_criteria>
  <done>System modals render correctly when triggered by env vars via API endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Error boundary, loading skeletons, and root layout wiring</name>
  <files>
    src/components/layout/app-error-boundary.tsx
    src/components/features/page-skeleton.tsx
    src/app/error.tsx
    src/app/global-error.tsx
    src/app/(main)/profile/loading.tsx
    src/app/(main)/profile/card/loading.tsx
    src/app/layout.tsx
  </files>
  <read_first>
    - src/app/layout.tsx (current root layout — will add SystemStateChecker)
    - src/components/ui/skeleton.tsx (existing Skeleton component)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 614-644 error boundary + skeleton layouts)
  </read_first>
  <action>
    1. Create `src/app/error.tsx` ('use client') — Next.js error boundary page per UI-SPEC Screen 8:
       - Full-screen: bg-[#FAFAFA] flex flex-col items-center justify-center px-4 min-h-dvh.
       - AlertCircle icon (lucide) 48x48 #F44336 mb-4.
       - "Something went wrong" — 20px bold #212121 text-center mb-2.
       - "An unexpected error occurred. Please try again." — 16px 400 #757575 text-center mb-8.
       - "Try Again" button: rounded-full bg-[#FFE600] text-[#212121] h-14 px-8. Calls `reset()` from error boundary props.
       - Receives `{ error, reset }` props from Next.js.
    2. Create `src/app/global-error.tsx` ('use client') — same UI as error.tsx but wraps its own html/body tags (Next.js requirement for global error boundary). Import fonts and globals.css.
    3. Create `src/components/layout/app-error-boundary.tsx` — export as a re-export alias; the actual error boundary is handled by Next.js `error.tsx` convention. This file can be a simple class-based React error boundary for use inside specific components if needed (optional utility). Props: `children`, `fallback?`. Catches errors in render, shows fallback or default error UI. Provides `reset` via button click that calls `this.setState({ hasError: false })`.
    4. Create `src/components/features/page-skeleton.tsx` — exports `ProfileMenuSkeleton` and `CardPageSkeleton`:
       - `ProfileMenuSkeleton`: yellow header area (bg-[#FFE600] h-32) with 72px circle skeleton centered + name bar skeleton. Below: 3 sections each with heading skeleton (h-3 w-24) and 3-4 row skeletons (h-14 rounded-xl bg-[#F5F5F5]).
       - `CardPageSkeleton`: card rectangle (343px x 200px bg-[#E0E0E0] rounded-2xl animate-pulse), action row (2 pill skeletons h-10), info block (3 rows h-5 bg-[#F5F5F5]).
       - Use Tailwind `animate-pulse` for shimmer.
    5. Create `src/app/(main)/profile/loading.tsx` — renders `ProfileMenuSkeleton`.
    6. Create `src/app/(main)/profile/card/loading.tsx` — renders `CardPageSkeleton`. Create the `card/` directory.
    7. Update `src/app/layout.tsx` — add `SystemStateChecker` inside the NextIntlClientProvider, after children: `<SystemStateChecker />`. Import from `@/components/features/system-state-checker`. Keep all existing code unchanged.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/error.tsx exists with AlertCircle icon, "Something went wrong" heading, Try Again button calling reset()
    - src/app/global-error.tsx exists with own html/body tags
    - PageSkeleton exports ProfileMenuSkeleton and CardPageSkeleton with animate-pulse
    - src/app/(main)/profile/loading.tsx renders ProfileMenuSkeleton
    - src/app/layout.tsx includes SystemStateChecker inside NextIntlClientProvider
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Error boundary catches unhandled errors with friendly UI. Loading skeletons display during profile page transitions. System state checker is mounted at app root.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Setting `SYSTEM_MAINTENANCE=true` in `.env.local` and visiting any page shows maintenance modal
- Setting `SYSTEM_UPDATE_REQUIRED=true` shows update modal
- Throwing an error in a component triggers the error boundary with "Try Again" button
- Navigating to `/profile` shows skeleton while loading
</verification>

<success_criteria>
System modals (D-13, D-14) trigger from env vars. Error boundary (D-15) wraps entire app. Loading skeletons (D-16) display on profile pages. All system states are user-friendly and match UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/07-profile-card-system-states/07-02-SUMMARY.md`
</output>
