---
phase: 07-profile-card-system-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en/profile.json
  - messages/th/profile.json
  - messages/mm/profile.json
  - src/components/features/profile-header.tsx
  - src/components/features/profile-menu-section.tsx
  - src/components/features/profile-menu-item.tsx
  - src/app/(main)/profile/page.tsx
  - src/app/(main)/profile/information/page.tsx
  - src/app/(main)/profile/contact-us/page.tsx
  - src/app/(main)/profile/limits-fees/page.tsx
  - src/app/(main)/profile/terms/page.tsx
  - src/app/(main)/profile/privacy/page.tsx
  - src/app/(main)/profile/faq/page.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-04
  - PROF-06
  - PROF-07
  - PROF-08

must_haves:
  truths:
    - "Profile menu matches Pencil layout with yellow header, avatar initials, sectioned menu list"
    - "Each menu item navigates to the correct sub-page route"
    - "Logout clears Supabase session and Zustand stores, redirects to /login"
    - "Information page shows user profile fields read-only"
    - "Contact Us, Limits & Fees, Terms, Privacy, FAQ pages render with placeholder content"
    - "Language badge next to Language menu item shows current locale (EN/TH/MM)"
  artifacts:
    - path: "messages/en/profile.json"
      provides: "i18n strings for all profile screens"
    - path: "src/components/features/profile-header.tsx"
      provides: "Yellow header with avatar initials and name"
    - path: "src/components/features/profile-menu-section.tsx"
      provides: "Titled section container"
    - path: "src/components/features/profile-menu-item.tsx"
      provides: "Menu row with icon, label, trailing badge/chevron"
    - path: "src/app/(main)/profile/page.tsx"
      provides: "Rewritten profile menu page"
  key_links:
    - from: "src/app/(main)/profile/page.tsx"
      to: "src/components/features/profile-header.tsx"
      via: "import and render"
      pattern: "ProfileHeader"
    - from: "src/app/(main)/profile/page.tsx"
      to: "/api/auth/logout or supabase.auth.signOut"
      via: "logout handler"
      pattern: "signOut|logout"
---

<objective>
Build the profile menu page (per D-01), i18n messages for the entire profile namespace, reusable ProfileHeader / ProfileMenuSection / ProfileMenuItem components, logout functionality (per D-03), and all static content sub-pages (Information, Contact Us, Limits & Fees, Terms, Privacy, FAQ).

Purpose: Establishes the profile page hub and all navigation targets that do not require multi-step flows. Downstream plans (03, 04) depend on these routes existing.
Output: Fully navigable profile menu with 6 static sub-pages, i18n for all 3 locales.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profile-card-system-states/07-CONTEXT.md
@.planning/phases/07-profile-card-system-states/07-UI-SPEC.md

<interfaces>
<!-- Existing layout and auth patterns the executor needs -->

From src/components/layout/back-header.tsx:
- BackHeader component with `title` prop for sub-page navigation

From src/app/(main)/layout.tsx:
- Main layout wraps children with BottomNav — profile page gets bottom nav automatically

From src/lib/supabase/server.ts:
- `createClient()` — async, returns Supabase server client

From src/lib/supabase/client.ts:
- `createClient()` — browser Supabase client for signOut

From src/stores/:
- registration-store.ts, kyc-store.ts, transfer-store.ts, wallet-ops-store.ts — all need clearing on logout

From messages/en/auth.json, home.json, etc.:
- Pattern: JSON object with flat or nested keys, loaded via next-intl namespace
- i18n request.ts merges namespaces via spread under namespace key
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n messages and reusable profile components</name>
  <files>
    messages/en/profile.json
    messages/th/profile.json
    messages/mm/profile.json
    src/i18n/request.ts
    src/components/features/profile-header.tsx
    src/components/features/profile-menu-section.tsx
    src/components/features/profile-menu-item.tsx
  </files>
  <read_first>
    - messages/en/home.json (pattern for i18n structure)
    - messages/en/auth.json (pattern for nested keys)
    - src/i18n/request.ts (to add profile namespace merge)
    - src/components/features/wallet-card.tsx (component pattern reference)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 219-358 copywriting contract)
  </read_first>
  <action>
    1. Create `messages/en/profile.json` with ALL copywriting from UI-SPEC Copywriting Contract. Structure:
       ```json
       {
         "menu": { "title": "Profile Setting", "biometrics": "Biometrics", "enableBiometrics": "Enable Touch ID / Face ID", "settings": "Settings", ... all menu items per UI-SPEC },
         "information": { "title": "Information", "fullName": "Full Name", "phone": "Phone Number", "dob": "Date of Birth" },
         "changePhone": { "title": "Change Phone Number", "step1Instruction": "Enter your new phone number", ... },
         "changePasscode": { "title": "Enter Current Passcode", ... },
         "language": { "title": "Language", "en": "English", "th": "ภาษาไทย", "mm": "မြန်မာဘာသာ" },
         "referFriends": { "title": "Refer Friends", ... },
         "notifications": { "title": "Notification Settings", ... },
         "contactUs": { "title": "Contact Us" },
         "limitsFees": { "title": "Limits and Fees" },
         "terms": { "title": "Terms and Conditions" },
         "privacy": { "title": "Privacy and Policy" },
         "faq": { "title": "Q&A Session" },
         "card": { "title": "Visa Card", ... all card copywriting },
         "system": { "maintenance": { ... }, "update": { ... }, "error": { ... } },
         "logout": "Logout",
         "version": "V 1.0"
       }
       ```
       Include ALL strings from the UI-SPEC Copywriting Contract — every screen, every label, every error message.
    2. Create `messages/th/profile.json` and `messages/mm/profile.json` with same keys, Thai and Myanmar translations. Use reasonable translations for placeholder content pages.
    3. Update `src/i18n/request.ts` to merge profile namespace: `profile: (await import(\`../../messages/\${locale}/profile.json\`)).default` alongside existing namespaces.
    4. Create `ProfileHeader` ('use client') — yellow header bg-[#FFE600], centered title "Profile Setting" in 20px bold #0091EA, avatar circle 72px diameter with border-2 #212121 on yellow bg showing 2-letter initials (24px bold #212121), full name below (16px bold #212121). Props: `name: string` (derives initials from first+last name first letters). Renders user's wallet balance below name if desired (optional, Claude's discretion — keep simple).
    5. Create `ProfileMenuSection` — renders section heading (12px 400 #9E9E9E, uppercase, tracking-[0.08em], px-4 pt-4 pb-2) and children.
    6. Create `ProfileMenuItem` — row h-14 px-4 flex items-center. Props: `icon: LucideIcon`, `label: string`, `onClick: () => void`, `trailing?: ReactNode` (for language badge), `trailingBadge?: string` (shortcut for pill badge). Renders: icon 24px #757575 left, label 16px #212121 flex-1, optional trailing content, ChevronRight 20px #9E9E9E. Divider 1px #E0E0E0 ml-14 below each item (via CSS border-b or explicit Separator).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - messages/en/profile.json exists with keys: menu.title, menu.biometrics, menu.settings, menu.helpSupport, changePhone.title, changePasscode.title, language.title, card.title, system.maintenance.title, logout, version
    - messages/th/profile.json and messages/mm/profile.json exist with same key structure
    - src/i18n/request.ts imports and merges profile namespace
    - ProfileHeader renders yellow bg header with initials avatar
    - ProfileMenuSection renders uppercase section heading
    - ProfileMenuItem renders icon + label + chevron row
  </acceptance_criteria>
  <done>All 3 locale files contain complete profile namespace. Three reusable profile components compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Profile menu page rewrite, logout, and static sub-pages</name>
  <files>
    src/app/(main)/profile/page.tsx
    src/app/(main)/profile/information/page.tsx
    src/app/(main)/profile/contact-us/page.tsx
    src/app/(main)/profile/limits-fees/page.tsx
    src/app/(main)/profile/terms/page.tsx
    src/app/(main)/profile/privacy/page.tsx
    src/app/(main)/profile/faq/page.tsx
  </files>
  <read_first>
    - src/app/(main)/profile/page.tsx (current scaffold to rewrite)
    - src/components/layout/back-header.tsx (sub-page header)
    - src/lib/supabase/client.ts (browser client for signOut)
    - src/stores/registration-store.ts (store reset pattern)
    - src/stores/transfer-store.ts (store reset pattern)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 366-419 profile menu layout, lines 649-668 static pages)
  </read_first>
  <action>
    1. Rewrite `src/app/(main)/profile/page.tsx` ('use client') per D-01 and UI-SPEC Screen 1:
       - Import and render `ProfileHeader` with user name from Supabase session (fetch via `supabase.auth.getUser()` + `user_profiles` table query). Use SWR or useEffect for data fetch.
       - Three `ProfileMenuSection` blocks: "BIOMETRICS" (one Switch toggle row for Touch ID/Face ID — mock only, no persistence per deferred), "SETTINGS" (Information, Refer Friends, Change Passcode, Change Phone Number, Manage Personal Limitation, Notification Settings), "HELP & SUPPORT" (Contact Us, Language with locale badge, Limits and Fees, Q&A Session, Terms and Conditions, Privacy and Policy).
       - Each menu item uses `ProfileMenuItem` with correct lucide icon per UI-SPEC: Info, Gift, Lock, Phone, Shield, Bell, MessageCircle, Globe, BarChart3, HelpCircle, FileText, Eye.
       - Language menu item shows trailing badge pill: 12px #0091EA on #E3F2FD bg, rounded-full, px-2 py-0.5 — displays current locale code (EN/TH/MM) per D-09.
       - Routes: `/profile/information`, `/profile/refer-friends`, `/profile/change-passcode`, `/profile/change-phone`, `/profile/limits-fees`, `/profile/notifications`, `/profile/language`, `/profile/contact-us`, `/profile/faq`, `/profile/terms`, `/profile/privacy`, `/profile/card` (linked from elsewhere or profile if desired).
       - Logout button: full-width outline, border-[#F44336] text-[#F44336] rounded-full h-12 font-bold. On tap per D-03: call `supabase.auth.signOut()`, clear all Zustand stores (registration, kyc, transfer, wallet-ops — call `.getState().reset?.()` or equivalent), then `router.push('/login')`.
       - Version text "V 1.0" centered at bottom, 12px #9E9E9E.
       - All text via `useTranslations('profile')` from profile namespace.
       - Install shadcn `switch` component: `npx shadcn@latest add switch --yes` (needed for biometrics toggle).
    2. Create `src/app/(main)/profile/information/page.tsx` — BackHeader "Information", read-only field list showing Full Name, Phone Number, Date of Birth from user profile data. Each field: label 12px #9E9E9E, value 16px #212121, h-14, border-b #E0E0E0. Fetch user profile via SWR or useEffect from Supabase.
    3. Create `src/app/(main)/profile/contact-us/page.tsx` — BackHeader "Contact Us". Placeholder content: call center number "1234", LINE @2c2pwave, email support@2c2pwave.com. Each row: icon + text, tappable.
    4. Create `src/app/(main)/profile/limits-fees/page.tsx` — BackHeader "Limits and Fees". Section headings (16px bold #212121): "Transfer Limits" (daily 50,000 THB, monthly 200,000 THB), "Fees" (transfer fee 99 THB, top-up free, withdrawal 25 THB). Placeholder values.
    5. Create `src/app/(main)/profile/terms/page.tsx` — BackHeader "Terms and Conditions". Lorem ipsum placeholder with section headings.
    6. Create `src/app/(main)/profile/privacy/page.tsx` — BackHeader "Privacy and Policy". Lorem ipsum placeholder.
    7. Create `src/app/(main)/profile/faq/page.tsx` — BackHeader "Q&A Session". 5 placeholder Q&A items with question heading + answer body.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Profile page renders yellow header with avatar, 3 menu sections, logout button, version text
    - Biometrics section has a Switch toggle (mock, no persistence)
    - Language menu item shows locale badge (EN/TH/MM)
    - Logout calls signOut, clears stores, redirects to /login
    - All 6 static sub-pages render with BackHeader and content
    - All pages use i18n translations from profile namespace
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Profile menu matches UI-SPEC layout. All 6 static sub-pages are navigable. Logout clears session and redirects.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Profile page at `/profile` renders yellow header, avatar, 3 sections, logout, version
- Each menu item navigates to correct sub-page
- Logout clears session and redirects to `/login`
- All static sub-pages render with BackHeader
</verification>

<success_criteria>
Profile menu matches Pencil layout (D-01). All static content pages are navigable. Logout works end-to-end. i18n messages cover the entire profile namespace for all 3 locales.
</success_criteria>

<output>
After completion, create `.planning/phases/07-profile-card-system-states/07-01-SUMMARY.md`
</output>
