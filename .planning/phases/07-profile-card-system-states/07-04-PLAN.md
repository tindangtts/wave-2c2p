---
phase: 07-profile-card-system-states
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/features/visa-card-display.tsx
  - src/components/features/card-reveal-button.tsx
  - src/components/features/freeze-card-toggle.tsx
  - src/app/(main)/profile/card/page.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03

must_haves:
  truths:
    - "Virtual Visa card displays with blue gradient, masked number, holder name, expiry"
    - "User can tap reveal button to see full card number for 10 seconds then it auto-hides"
    - "User can freeze/unfreeze card via toggle with AlertDialog confirmation"
    - "Frozen card shows dark overlay with Snowflake icon and 'Card Frozen' text"
    - "Card info section shows card number, expiry, and status badge (Active/Frozen)"
  artifacts:
    - path: "src/components/features/visa-card-display.tsx"
      provides: "Blue gradient card face with masked/revealed number, freeze overlay"
    - path: "src/components/features/card-reveal-button.tsx"
      provides: "Eye toggle with 10s auto-hide timer"
    - path: "src/components/features/freeze-card-toggle.tsx"
      provides: "Switch with AlertDialog confirmation"
    - path: "src/app/(main)/profile/card/page.tsx"
      provides: "Card page composing all card components"
  key_links:
    - from: "src/app/(main)/profile/card/page.tsx"
      to: "src/components/features/visa-card-display.tsx"
      via: "import and render with reveal/freeze state"
      pattern: "VisaCardDisplay"
    - from: "src/components/features/card-reveal-button.tsx"
      to: "10s setTimeout"
      via: "auto-hide timer after reveal"
      pattern: "setTimeout.*10000|10_000"
---

<objective>
Build the virtual Visa card page with card display, reveal/hide toggle (10s auto-hide), and freeze/unfreeze with confirmation dialogs. All mock data — no real card API.

Purpose: Delivers the virtual card experience per D-10, D-11, D-12. Users see a premium card UI with interactive reveal and freeze controls.
Output: Fully interactive card page at /profile/card.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profile-card-system-states/07-CONTEXT.md
@.planning/phases/07-profile-card-system-states/07-UI-SPEC.md
@.planning/phases/07-profile-card-system-states/07-01-SUMMARY.md

<interfaces>
From src/components/layout/back-header.tsx:
- BackHeader with `title` prop

From src/components/ui/alert-dialog.tsx:
- AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogAction, AlertDialogCancel, AlertDialogTitle, AlertDialogDescription

From src/components/ui/switch.tsx (installed in plan 01):
- Switch component with checked/onCheckedChange props

From src/components/ui/badge.tsx:
- Badge component with variant prop

From messages/en/profile.json (created in plan 01):
- profile.card.* keys for all card copywriting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card display component and reveal button</name>
  <files>
    src/components/features/visa-card-display.tsx
    src/components/features/card-reveal-button.tsx
  </files>
  <read_first>
    - src/components/features/wallet-card.tsx (existing card-like component pattern)
    - src/components/ui/badge.tsx (Badge for status)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 429-476 card layout + state transitions)
  </read_first>
  <action>
    1. Create `VisaCardDisplay` ('use client') per D-10 and UI-SPEC Screen 2:
       - Container: w-full max-w-[343px] h-[200px] rounded-2xl relative overflow-hidden mx-auto.
       - Background: `background: linear-gradient(135deg, #1565C0 0%, #0D47A1 50%, #1976D2 100%)`.
       - Shadow: `shadow-[0_8px_32px_rgba(0,0,0,0.18)]`.
       - Top row (mt-5 mx-5 flex justify-between items-start): chip icon placeholder (32x24 rounded-sm bg-amber-300/80) left; "VISA" text SVG or styled span (32px wide, white, font-bold italic tracking-wider) right.
       - Card number row (mt-8 mx-5): masked "•••• •••• •••• {last4}" or revealed "{full16}" — 20px bold white tracking-widest.
       - Bottom row (mx-5 mb-5 flex justify-between items-end absolute bottom-0 left-0 right-0): left column "VALID THRU" (10px 400 rgba(255,255,255,0.7)) + date (12px bold white); right column holder name (12px 400 white uppercase).
       - Freeze overlay (conditional): absolute inset-0 bg-black/55 rounded-2xl flex flex-col items-center justify-center. Snowflake icon (lucide `Snowflake`) 32px white. "Card Frozen" 16px bold white mt-2.
       - Props: `cardNumber: string` (full 16 digits), `holderName: string`, `expiry: string` (MM/YY), `revealed: boolean`, `frozen: boolean`.
       - Mock card data: number "4532 0198 7654 3210", expiry "12/28", holder from user profile name.
    2. Create `CardRevealButton` ('use client') per D-11:
       - Button variant="outline" rounded-full h-10 px-4, border-[#E0E0E0] bg-white.
       - When not revealed: Eye icon (lucide `Eye`) 16px + "Show Card Details" 12px 400.
       - When revealed: EyeOff icon (lucide `EyeOff`) 16px + "Hide Card Details" 12px 400.
       - Props: `revealed: boolean`, `onToggle: () => void`.
       - Auto-hide logic lives in the parent page — the button just toggles. Parent manages a `useEffect` with `setTimeout(10000)` that calls `setRevealed(false)` when revealed becomes true. Cleanup clears timeout on unmount or when revealed changes. After auto-hide, show toast: "Card details hidden for security." via sonner.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - VisaCardDisplay renders blue gradient card 343x200 with chip, VISA mark, masked/revealed number, expiry, holder name
    - Freeze overlay renders conditionally with Snowflake icon and "Card Frozen" text
    - CardRevealButton shows Eye/EyeOff icon with correct label text
    - Both components accept typed props and compile without errors
  </acceptance_criteria>
  <done>Card face renders with gradient, masked number, and freeze overlay. Reveal button toggles eye icon state.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze toggle, card page composition, and card info section</name>
  <files>
    src/components/features/freeze-card-toggle.tsx
    src/app/(main)/profile/card/page.tsx
  </files>
  <read_first>
    - src/components/ui/switch.tsx (Switch component API — checked, onCheckedChange)
    - src/components/ui/alert-dialog.tsx (AlertDialog API)
    - src/components/features/visa-card-display.tsx (just created in task 1)
    - src/components/features/card-reveal-button.tsx (just created in task 1)
    - src/components/layout/back-header.tsx (BackHeader props)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 429-476)
  </read_first>
  <action>
    1. Create `FreezeCardToggle` ('use client') per D-12:
       - Flex row: "Freeze Card" label 12px 400 #212121, Switch component.
       - Switch: active (frozen) bg-[#0091EA], size 44x24.
       - On toggle attempt: open AlertDialog for confirmation before changing state.
       - Freeze confirm: title "Freeze Card?", body "Transactions will be declined while your card is frozen.", Cancel + "Freeze" (bg-[#FFE600]).
       - Unfreeze confirm: title "Unfreeze Card?", body "Your card will be ready to use again.", Cancel + "Unfreeze" (bg-[#FFE600]).
       - Props: `frozen: boolean`, `onFreezeChange: (frozen: boolean) => void`.
       - Uses controlled AlertDialog (open state managed internally). On confirm action, calls `onFreezeChange(!frozen)`.
    2. Create `src/app/(main)/profile/card/page.tsx` ('use client') per UI-SPEC Screen 2:
       - BackHeader: title "Visa Card", rendered on yellow bg pattern (per UI-SPEC: #0091EA text on #FFE600 bg — use BackHeader with appropriate styling or custom yellow header matching profile page).
       - Content area: bg-[#FAFAFA] flex-1 overflow-y-auto px-4 pt-6.
       - State: `const [revealed, setRevealed] = useState(false)` and `const [frozen, setFrozen] = useState(false)`.
       - Render VisaCardDisplay with mock data: cardNumber="4532019876543210", holderName from user name (or hardcode "LALITA TUNGTRAKUL" for demo), expiry="12/28", revealed, frozen.
       - Card action row (mt-6 flex items-center justify-between): CardRevealButton + FreezeCardToggle.
       - Auto-hide effect: `useEffect` — when `revealed` becomes true, start 10s timer to `setRevealed(false)` + toast. Clear on cleanup.
       - When frozen, disable reveal button (cannot reveal frozen card).
       - Card info section (mt-6 bg-white rounded-xl p-4 shadow-sm):
         - Row: "Card Number" 12px #9E9E9E left, masked/revealed number 12px #212121 right.
         - Divider 1px #E0E0E0 my-3.
         - Row: "Expiry Date" left, "12/28" right.
         - Divider.
         - Row: "Card Status" left, Badge right. Active: text-[#00C853] bg-[#E8F5E9]. Frozen: text-[#0091EA] bg-[#E3F2FD].
       - All text from `useTranslations('profile')` under `card.*` keys.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Card page at /profile/card renders blue gradient card with masked number
    - Tapping "Show Card Details" reveals full number, auto-hides after 10 seconds with toast
    - Freeze toggle shows AlertDialog confirmation before changing state
    - Frozen card shows dark overlay with Snowflake icon
    - Card info section shows number, expiry, status badge (Active green / Frozen blue)
    - Reveal button disabled when card is frozen
    - Build succeeds
  </acceptance_criteria>
  <done>Virtual Visa card page is fully interactive with reveal (10s auto-hide) and freeze/unfreeze (with confirmation). All mock data, no real card API.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Card page renders at `/profile/card` with blue gradient card
- Reveal shows full number, auto-hides after 10s
- Freeze toggle requires confirmation dialog, shows overlay when frozen
- Card info section updates status badge based on freeze state
</verification>

<success_criteria>
Virtual Visa card displays masked number with reveal/hide toggle (D-11, 10s auto-hide) and freeze/unfreeze with confirmation (D-12). Card face matches UI-SPEC gradient and layout. All mock data per D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/07-profile-card-system-states/07-04-SUMMARY.md`
</output>
