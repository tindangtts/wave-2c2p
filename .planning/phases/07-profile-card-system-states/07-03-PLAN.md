---
phase: 07-profile-card-system-states
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(main)/profile/change-phone/page.tsx
  - src/app/(main)/profile/change-passcode/page.tsx
  - src/app/(main)/profile/language/page.tsx
  - src/app/(main)/profile/refer-friends/page.tsx
  - src/app/(main)/profile/notifications/page.tsx
  - src/components/features/language-selector.tsx
  - src/components/features/referral-card.tsx
  - src/components/features/notification-toggle-list.tsx
  - src/app/api/auth/change-phone/route.ts
  - src/app/api/auth/verify-change-phone/route.ts
  - src/app/api/auth/change-passcode/route.ts
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - PROF-05

must_haves:
  truths:
    - "User can enter a new phone number, receive mock OTP, verify, and see success"
    - "User can enter current passcode, set new passcode, confirm, and see success"
    - "Language selector shows EN/TH/MM options with current locale highlighted yellow"
    - "Tapping a language option updates the entire UI locale immediately without page reload"
    - "Referral page shows QR code with referral code and share button"
  artifacts:
    - path: "src/app/(main)/profile/change-phone/page.tsx"
      provides: "Multi-step phone change flow"
    - path: "src/app/(main)/profile/change-passcode/page.tsx"
      provides: "3-step passcode change flow"
    - path: "src/app/(main)/profile/language/page.tsx"
      provides: "Language selector page"
    - path: "src/components/features/referral-card.tsx"
      provides: "Referral code + QR + share"
    - path: "src/app/api/auth/change-phone/route.ts"
      provides: "Mock OTP send for phone change"
    - path: "src/app/api/auth/change-passcode/route.ts"
      provides: "Passcode change API"
  key_links:
    - from: "src/app/(main)/profile/change-passcode/page.tsx"
      to: "/api/auth/passcode/verify"
      via: "POST to verify current passcode"
      pattern: "fetch.*passcode/verify"
    - from: "src/app/(main)/profile/change-passcode/page.tsx"
      to: "/api/auth/change-passcode"
      via: "POST to save new passcode"
      pattern: "fetch.*change-passcode"
    - from: "src/app/(main)/profile/language/page.tsx"
      to: "next-intl locale cookie"
      via: "server action or cookie set"
      pattern: "locale|cookie"
---

<objective>
Build the interactive profile sub-pages: phone number change (D-04/D-05), passcode change (D-06/D-07), language selector (D-08/D-09), referral page, and notification settings.

Purpose: Completes all interactive profile features requiring multi-step flows, API calls, or state changes.
Output: 5 fully functional sub-pages with mock APIs.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-profile-card-system-states/07-CONTEXT.md
@.planning/phases/07-profile-card-system-states/07-UI-SPEC.md
@.planning/phases/07-profile-card-system-states/07-01-SUMMARY.md

<interfaces>
From src/components/features/passcode-keypad.tsx:
```typescript
interface PasscodeKeypadProps {
  value: string
  onChange: (value: string) => void
  onComplete: (code: string) => void
  error?: string
  isLoading?: boolean
}
export function PasscodeKeypad({ value, onChange, onComplete, error, isLoading }: PasscodeKeypadProps)
```

From src/components/ui/input-otp.tsx:
- InputOTP, InputOTPGroup, InputOTPSlot — 6-digit OTP input

From src/app/(auth)/otp/page.tsx:
- Pattern: useState for otp/error/isLoading, 60s countdown timer for resend, maskPhone utility

From src/lib/auth/passcode.ts:
```typescript
export function hashPasscode(passcode: string): string
export function verifyPasscode(passcode: string, stored: string): boolean
```

From src/app/api/auth/passcode/verify/route.ts:
- POST endpoint: accepts { passcode }, verifies against stored hash, returns success/error

From src/components/features/step-indicator.tsx:
- StepIndicator component with namespace prop for i18n step labels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phone change flow, passcode change flow, and their mock APIs</name>
  <files>
    src/app/api/auth/change-phone/route.ts
    src/app/api/auth/verify-change-phone/route.ts
    src/app/api/auth/change-passcode/route.ts
    src/app/(main)/profile/change-phone/page.tsx
    src/app/(main)/profile/change-passcode/page.tsx
  </files>
  <read_first>
    - src/app/(auth)/otp/page.tsx (OTP flow pattern — timer, resend, verification)
    - src/app/(auth)/passcode/page.tsx (passcode entry pattern — keypad, dots, error shake)
    - src/app/api/auth/passcode/verify/route.ts (passcode verify API pattern)
    - src/app/api/auth/otp/verify/route.ts (OTP verify API pattern — if exists, else src/app/api/auth/otp/)
    - src/lib/auth/passcode.ts (hashPasscode, verifyPasscode)
    - src/components/features/passcode-keypad.tsx (full component for reuse)
    - src/components/ui/input-otp.tsx (OTP input for reuse)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 479-529 change-phone + change-passcode layouts)
  </read_first>
  <action>
    1. Create `POST /api/auth/change-phone` per D-05:
       - Accepts `{ phone, countryCode }`. Validates phone format.
       - Mock: always returns `{ success: true, message: "OTP sent" }` with 500ms delay.
       - Requires authenticated user (check supabase.auth.getUser()).
    2. Create `POST /api/auth/verify-change-phone` per D-05:
       - Accepts `{ phone, countryCode, otp }`. Mock: OTP "123456" always succeeds.
       - On success: update user_profiles.phone in Supabase, return `{ success: true }`.
       - On failure (wrong OTP): return 400 `{ error: "Incorrect OTP" }`.
    3. Create `POST /api/auth/change-passcode`:
       - Accepts `{ currentPasscode, newPasscode }`. Per D-07: verifies current via PBKDF2 (`verifyPasscode`).
       - If current is correct: hash new passcode via `hashPasscode`, update user_profiles.passcode_hash, return `{ success: true }`.
       - If wrong: return 400 `{ error: "Incorrect passcode", attemptsRemaining: N }`.
       - Max 5 attempts with 30-min lockout (reuse pattern from passcode/verify route).
    4. Create `src/app/(main)/profile/change-phone/page.tsx` ('use client') per D-04 and UI-SPEC Screen 3:
       - 2-step flow managed by `useState<1 | 2 | 'success'>(1)`.
       - Step 1: BackHeader "Change Phone Number", instruction text, phone input with country code selector (reuse pattern from login page — Select for +66/+95, Input for digits). CTA "Send OTP" — posts to `/api/auth/change-phone`.
       - Step 2: Instruction "Enter the OTP sent to {maskedPhone}", InputOTP 6-digit, resend timer (60s countdown, "Resend code" link active after expiry per UI-SPEC). CTA "Verify" — posts to `/api/auth/verify-change-phone`.
       - Success screen: check icon, "Phone Number Updated", "Your phone number has been successfully changed.", "Back to Profile" button → router.push('/profile').
       - All text from `useTranslations('profile')` under `changePhone.*` keys.
    5. Create `src/app/(main)/profile/change-passcode/page.tsx` ('use client') per D-06 and UI-SPEC Screen 4:
       - 3-step flow managed by `useState<1 | 2 | 3 | 'success'>(1)`.
       - Each step: full-screen passcode entry (no BackHeader — matches Phase 2 passcode pattern). Title centered at top: Step 1 "Enter Current Passcode", Step 2 "Enter New Passcode", Step 3 "Confirm New Passcode". Instruction text below. 6-dot indicator + PasscodeKeypad.
       - Step 1 onComplete: POST to `/api/auth/passcode/verify` with entered passcode. If success → step 2. If fail → show error "Incorrect passcode. {N} attempts remaining." with red dots + shake.
       - Step 2 onComplete: store new passcode in local state, advance to step 3.
       - Step 3 onComplete: compare with step 2 value. If match → POST to `/api/auth/change-passcode` with current + new. If mismatch → error "Passcodes do not match. Please try again." + shake.
       - Success screen: check icon, "Passcode Changed", "Your passcode has been successfully updated.", "Done" button → router.push('/profile').
       - Back navigation: provide a back chevron or use router.back() on step 1.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - /api/auth/change-phone returns { success: true } for valid phone
    - /api/auth/verify-change-phone returns success for OTP "123456"
    - /api/auth/change-passcode verifies current passcode before accepting new one
    - Change phone page has 2-step flow: phone input → OTP → success
    - Change passcode page has 3-step flow: current → new → confirm → success
    - Both pages reuse existing PasscodeKeypad and InputOTP components
    - Error states (wrong OTP, wrong passcode, mismatch) display with correct messages
  </acceptance_criteria>
  <done>Phone change and passcode change flows work end-to-end with mock APIs. Error handling matches UI-SPEC copywriting.</done>
</task>

<task type="auto">
  <name>Task 2: Language selector, referral page, and notification settings</name>
  <files>
    src/components/features/language-selector.tsx
    src/components/features/referral-card.tsx
    src/components/features/notification-toggle-list.tsx
    src/app/(main)/profile/language/page.tsx
    src/app/(main)/profile/refer-friends/page.tsx
    src/app/(main)/profile/notifications/page.tsx
  </files>
  <read_first>
    - src/i18n/request.ts (locale detection pattern, cookie name)
    - src/app/(auth)/login/page.tsx (country selector UI pattern for reference)
    - src/components/features/qr-display.tsx (QR code rendering pattern if exists, else check react-qr-code usage)
    - src/components/features/transfer-receipt.tsx (Web Share API pattern if used)
    - .planning/phases/07-profile-card-system-states/07-UI-SPEC.md (lines 533-582 language + referral layouts)
  </read_first>
  <action>
    1. Create `LanguageSelector` ('use client') component:
       - 3 rows, each h-[52px] flex items-center px-4 rounded-xl.
       - Each row: flag emoji (GB/TH/MM) 24px, label ("English" / "ภาษาไทย" / "မြန်မာဘာသာ") 16px 400 #212121, Check icon 20px #0091EA if active.
       - Myanmar label gets `lang="my"` attribute for correct script shaping.
       - Active row: bg-[#FFE600]. Inactive: transparent.
       - On tap: sets locale cookie (same cookie name used by next-intl — check request.ts for cookie name, likely "NEXT_LOCALE"), then calls `router.refresh()` to trigger next-intl re-render per D-08. No full page reload needed.
       - Props: `currentLocale: string`.
    2. Create `src/app/(main)/profile/language/page.tsx` per UI-SPEC Screen 5:
       - BackHeader "Language". Render LanguageSelector with current locale from cookie or next-intl `useLocale()`.
       - All text from profile.language namespace.
    3. Create `ReferralCard` ('use client') component per UI-SPEC Screen 6:
       - White card: rounded-2xl shadow-md p-6.
       - "Your Referral Code" — 12px #9E9E9E.
       - Referral code — 24px bold #212121 tracking-[0.12em]. Generate mock code from user ID or use static "WAVE2C2P" for demo.
       - QR Code: use `react-qr-code` library (install via `npm install react-qr-code`). `<QRCode value={referralCode} size={160} fgColor="#212121" bgColor="#FFFFFF" />`.
       - Monthly count — "0 referrals this month" — 12px #757575.
    4. Create `src/app/(main)/profile/refer-friends/page.tsx`:
       - BackHeader "Refer Friends". Instruction text. ReferralCard. Share CTA button: rounded-full bg-[#FFE600] text-[#212121] h-14 full-width.
       - Share button: uses `navigator.share({ title, text, url })` if available (Web Share API). Falls back to `navigator.clipboard.writeText()` with toast "Referral code copied!" via sonner.
    5. Create `NotificationToggleList` ('use client') component:
       - 3 rows, each h-14 flex items-center justify-between px-4.
       - Each: label (16px 400 #212121), description below (12px #757575), Switch toggle right side.
       - Toggles: Push Notifications, Email Notifications, SMS Notifications.
       - State: local useState — mock only, no persistence per deferred. Switch active color #0091EA.
    6. Create `src/app/(main)/profile/notifications/page.tsx`:
       - BackHeader "Notification Settings". NotificationToggleList. "Save Changes" CTA at bottom (rounded-full bg-[#FFE600] text-[#212121] h-14 full-width) — on tap shows toast "Settings saved" (mock).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Language page shows 3 options with current locale highlighted yellow and check icon
    - Tapping a language option updates locale cookie and refreshes UI
    - Referral page shows QR code rendered by react-qr-code at 160x160
    - Share button uses Web Share API with clipboard fallback
    - Notification settings page shows 3 toggle rows with Switch components
    - All pages use i18n translations from profile namespace
    - Build succeeds
  </acceptance_criteria>
  <done>Language switching works without reload. Referral page shows QR and share. Notification toggles render as mock UI.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Phone change flow: enter phone → send OTP → enter "123456" → success → back to profile
- Passcode change: enter current → enter new → confirm → success
- Language selector highlights current, tapping another updates all UI text
- Referral page shows QR code with share functionality
</verification>

<success_criteria>
All interactive profile sub-pages work end-to-end. Phone change reuses Phase 2 OTP pattern. Passcode change reuses Phase 2 keypad. Language switch updates immediately via next-intl cookie.
</success_criteria>

<output>
After completion, create `.planning/phases/07-profile-card-system-states/07-03-SUMMARY.md`
</output>
