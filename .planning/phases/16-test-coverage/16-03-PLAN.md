---
phase: 16-test-coverage
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/app/(auth)/register/__tests__/personal-info.test.tsx
  - src/app/(main)/transfer/__tests__/new-recipient.test.tsx
  - src/app/(main)/transfer/__tests__/amount.test.tsx
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Registration personal-info form shows validation errors on empty required fields"
    - "Recipient form shows validation errors on invalid phone and missing required fields"
    - "Transfer amount form rejects amounts below minimum and above maximum"
  artifacts:
    - path: "src/app/(auth)/register/__tests__/personal-info.test.tsx"
      provides: "RTL tests for personal-info registration form"
      min_lines: 40
    - path: "src/app/(main)/transfer/__tests__/new-recipient.test.tsx"
      provides: "RTL tests for new-recipient form"
      min_lines: 40
    - path: "src/app/(main)/transfer/__tests__/amount.test.tsx"
      provides: "RTL tests for transfer amount entry"
      min_lines: 30
  key_links:
    - from: "src/app/(auth)/register/__tests__/personal-info.test.tsx"
      to: "src/app/(auth)/register/personal-info/page.tsx"
      via: "import default and render"
      pattern: "import.*personal-info/page"
    - from: "src/app/(main)/transfer/__tests__/new-recipient.test.tsx"
      to: "src/app/(main)/transfer/new-recipient/page.tsx"
      via: "import default and render"
      pattern: "import.*new-recipient/page"
---

<objective>
Write React Testing Library tests for three key form components: registration personal-info, new-recipient, and transfer amount entry.

Purpose: TEST-03 requires RTL tests that verify form submission with valid data succeeds and invalid data shows field errors.
Output: 3 new .test.tsx files, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-test-coverage/16-RESEARCH.md
@.planning/phases/16-test-coverage/16-01-SUMMARY.md

@src/app/(auth)/register/personal-info/page.tsx
@src/app/(main)/transfer/new-recipient/page.tsx
@src/app/(main)/transfer/amount/page.tsx
@src/lib/auth/schemas.ts
@src/lib/transfer/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Personal-info registration form RTL test</name>
  <files>src/app/(auth)/register/__tests__/personal-info.test.tsx</files>
  <read_first>
    - src/app/(auth)/register/personal-info/page.tsx (full file — identify all imports, hooks used, form fields rendered, button text, error display pattern)
    - src/lib/auth/schemas.ts (personalInfoSchema — requires title, firstName, lastName, gender, dateOfBirth, email, nationality)
    - src/stores/registration-store.ts (if used by the page — check for useRegistrationStore)
  </read_first>
  <action>
Create src/app/(auth)/register/__tests__/personal-info.test.tsx.

CRITICAL mock setup (must be BEFORE component import):
```typescript
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock next-intl — returns the translation key as-is
vi.mock('next-intl', () => ({
  useTranslations: () => (key: string) => key,
}))

// Mock next/navigation
const mockPush = vi.fn()
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: mockPush, replace: vi.fn(), back: vi.fn() }),
  usePathname: () => '/register/personal-info',
  useSearchParams: () => new URLSearchParams(),
}))
```

Also mock any Zustand stores and SWR hooks the page imports. Read the page.tsx FIRST to identify all imports that need mocking.

Test cases:
1. 'renders the form with required fields' — render page, check that firstName, lastName input fields exist (by role or label/placeholder)
2. 'shows validation errors when submitting empty form' — render page, click the submit/continue button, await validation error text to appear (check for 'required' or the zod error message key)
3. 'accepts valid input without showing errors' — fill in all required fields (title, firstName, lastName, gender, dateOfBirth, nationality), submit, verify no error text appears

Use `userEvent.setup()` for all interactions. Use `screen.getByRole`, `screen.getByPlaceholderText`, or `screen.getByLabelText` to find form elements. Since next-intl returns keys, look for the translation key strings in rendered output.

NOTE: The form likely uses shadcn Select components (for title, gender, nationality). These render as custom dropdowns, NOT native select elements. You may need to click the trigger button and then click an option. Read the page.tsx to understand the exact UI pattern before writing tests.

If the form is too complex to fully test submission (many custom components), focus on:
- Rendering without crash
- Showing validation errors on empty submit
- Individual field validation
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx vitest run src/app/\(auth\)/register/__tests__/personal-info.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/register/__tests__/personal-info.test.tsx exists with 3+ test cases
    - All mocks (next-intl, next/navigation, stores) are set up before component import
    - Tests pass without errors
  </acceptance_criteria>
  <done>Personal-info form renders, shows validation errors on empty submit, accepts valid input.</done>
</task>

<task type="auto">
  <name>Task 2: New-recipient and transfer amount form RTL tests</name>
  <files>src/app/(main)/transfer/__tests__/new-recipient.test.tsx, src/app/(main)/transfer/__tests__/amount.test.tsx</files>
  <read_first>
    - src/app/(main)/transfer/new-recipient/page.tsx (full file — identify all imports, form fields, mocking needs)
    - src/app/(main)/transfer/amount/page.tsx (full file — identify AmountInput component, useTransferStore, useWallet hook)
    - src/stores/transfer-store.ts (understand store shape for mocking)
    - src/hooks/use-wallet.ts (understand return shape for mocking)
    - src/lib/transfer/schemas.ts (recipientFormSchema fields, transferAmountSchema)
  </read_first>
  <action>
Create two test files following the same mock pattern as Task 1.

**new-recipient.test.tsx:**

Mock next-intl, next/navigation, and any stores/hooks the page imports. Read page.tsx first to identify all imports.

Test cases:
1. 'renders the new recipient form' — check key fields exist (first_name, last_name, phone inputs)
2. 'shows validation errors on empty submit' — click submit button, await error messages
3. 'shows bank fields when bank_transfer is selected' — if transfer_type selector exists, select bank_transfer and verify bank_name/account_no fields appear

**amount.test.tsx:**

Mock next-intl, next/navigation, useTransferStore (provide mock recipient and channel), useWallet (provide mock balance).

Test cases:
1. 'renders amount input' — check AmountInput or input field exists
2. 'shows error for amount below minimum' — enter 99 (below 100 THB min), submit, check error
3. 'shows error for amount above maximum' — enter 26000 (above 25000 THB max), submit, check error

IMPORTANT: Read each page.tsx fully before writing tests. The pages may use custom components (AmountInput, MyanmarAddressPicker) that need specific interaction patterns. Mock all external hooks/stores identified in the imports.

For the new-recipient page which imports MyanmarAddressPicker, mock it:
```typescript
vi.mock('@/components/features/myanmar-address-picker', () => ({
  MyanmarAddressPicker: ({ onChange }: { onChange: (addr: unknown) => void }) => (
    <div data-testid="myanmar-address-picker" />
  ),
}))
```

Similarly mock any other feature components that are complex to render in tests.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx vitest run src/app/\(main\)/transfer/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/transfer/__tests__/new-recipient.test.tsx exists with 3+ test cases
    - src/app/(main)/transfer/__tests__/amount.test.tsx exists with 3+ test cases
    - All tests pass
  </acceptance_criteria>
  <done>Recipient form and amount entry form have RTL tests covering rendering, validation errors on invalid input, and valid submission paths.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app` — all component tests pass
- `npm test` — full suite including new component tests passes
</verification>

<success_criteria>
TEST-03 satisfied: Registration, recipient, and amount form components have RTL tests that verify valid submit succeeds and invalid submit shows errors.
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-coverage/16-03-SUMMARY.md`
</output>
