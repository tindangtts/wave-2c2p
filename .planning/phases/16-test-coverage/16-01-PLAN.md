---
phase: 16-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/test-setup.ts
  - package.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "npx vitest run executes successfully with jsdom environment"
    - "Existing 44 tests (currency, auth schemas, passcode, bank-accounts) still pass after config upgrade"
    - "@testing-library/react, @testing-library/user-event, @testing-library/jest-dom, jsdom are installed"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with react plugin, jsdom environment, setupFiles, @ alias"
      contains: "plugins.*react"
    - path: "src/test-setup.ts"
      provides: "Test setup importing jest-dom matchers"
      contains: "@testing-library/jest-dom"
    - path: "package.json"
      provides: "test script and RTL devDependencies"
      contains: "@testing-library/react"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test-setup.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*test-setup"
---

<objective>
Upgrade the existing Vitest config to support React component testing with jsdom and RTL, install missing test dependencies, and verify all existing tests still pass.

Purpose: Foundation for all subsequent test plans — RTL component tests (Plan 03) and schema/currency tests (Plan 02) both depend on this config being correct.
Output: Updated vitest.config.ts, new src/test-setup.ts, installed RTL packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-test-coverage/16-RESEARCH.md

@vitest.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RTL dependencies and update vitest.config.ts</name>
  <files>package.json, vitest.config.ts, src/test-setup.ts</files>
  <read_first>
    - vitest.config.ts (current config — node environment, @ alias, no plugins)
    - package.json (confirm @vitejs/plugin-react 6.0.1 already in devDependencies)
  </read_first>
  <action>
1. Install missing test dependencies:
   ```
   npm install --save-dev @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom
   ```

2. Add `"test"` script to package.json scripts section:
   ```json
   "test": "vitest run"
   ```

3. Replace vitest.config.ts with upgraded config. CRITICAL: @vitejs/plugin-react is already installed (6.0.1 in devDependencies) — do NOT reinstall. The config must:
   - Import and use `react()` plugin from `@vitejs/plugin-react`
   - Set `environment: 'jsdom'` (safe for existing pure-TS tests too)
   - Add `setupFiles: ['./src/test-setup.ts']`
   - Keep `globals: true` for describe/it/expect without imports
   - Keep existing `@` path alias

   Target vitest.config.ts content:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import path from 'path'

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./src/test-setup.ts'],
       globals: true,
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

4. Create src/test-setup.ts:
   ```typescript
   import '@testing-library/jest-dom'
   ```
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains `plugins: [react()]` and `environment: 'jsdom'`
    - src/test-setup.ts exists and imports @testing-library/jest-dom
    - package.json has "test": "vitest run" in scripts
    - package.json devDependencies include @testing-library/react, @testing-library/user-event, @testing-library/jest-dom, jsdom
    - `npm test` passes all existing 44 tests (currency, auth schemas, passcode, bank-accounts)
  </acceptance_criteria>
  <done>All 4 existing test files pass under the upgraded jsdom + React plugin config. RTL is installed and ready for component tests.</done>
</task>

</tasks>

<verification>
- `npm test` exits with code 0
- `grep -c "plugins.*react" vitest.config.ts` returns 1
- `grep -c "jest-dom" src/test-setup.ts` returns 1
- `grep "@testing-library/react" package.json` returns a match
</verification>

<success_criteria>
Vitest runs with jsdom environment and React plugin. All 44 existing tests pass. RTL packages installed. Ready for Plans 02 and 03.
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-coverage/16-01-SUMMARY.md`
</output>
