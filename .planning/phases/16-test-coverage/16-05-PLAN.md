---
phase: 16-test-coverage
plan: 05
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - src/e2e/transfer.spec.ts
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "npx playwright test src/e2e/transfer.spec.ts passes"
    - "E2E test completes transfer confirmation and reaches receipt page"
  artifacts:
    - path: "src/e2e/transfer.spec.ts"
      provides: "Transfer confirmation -> receipt E2E test"
      min_lines: 30
  key_links:
    - from: "src/e2e/transfer.spec.ts"
      to: "http://localhost:3000/transfer"
      via: "page navigation through transfer flow"
      pattern: "page.goto.*transfer"
---

<objective>
Write a Playwright E2E test covering the transfer confirmation -> receipt happy path.

Purpose: TEST-05 requires an E2E test that navigates through the transfer flow (select recipient, enter amount, confirm, see receipt).
Output: src/e2e/transfer.spec.ts, passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-test-coverage/16-RESEARCH.md
@.planning/phases/16-test-coverage/16-04-SUMMARY.md

@src/app/(main)/transfer/recipient/page.tsx
@src/app/(main)/transfer/new-recipient/page.tsx
@src/app/(main)/transfer/amount/page.tsx
@src/app/(main)/transfer/channel/page.tsx
@src/app/(main)/transfer/confirm/page.tsx
@src/app/(main)/transfer/receipt/page.tsx
@playwright.config.ts
@src/e2e/registration.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transfer confirmation -> receipt E2E test</name>
  <files>src/e2e/transfer.spec.ts</files>
  <read_first>
    - src/e2e/registration.spec.ts (reference for established E2E patterns, auth flow for session setup)
    - src/proxy.ts (understand auth guards on /transfer/* routes — test must be authenticated)
    - src/app/(main)/transfer/recipient/page.tsx (full file — understand recipient selection/creation)
    - src/app/(main)/transfer/new-recipient/page.tsx (full file — understand form fields for creating a recipient)
    - src/app/(main)/transfer/amount/page.tsx (full file — understand amount input interaction)
    - src/app/(main)/transfer/channel/page.tsx (full file — understand channel selection, if exists as separate step)
    - src/app/(main)/transfer/confirm/page.tsx (full file — understand confirmation screen, confirm button)
    - src/app/(main)/transfer/receipt/page.tsx (full file — understand receipt display)
    - src/stores/transfer-store.ts (understand store shape — the flow may require store population)
    - src/app/api/mock-payment/route.ts (understand mock payment API behavior in DEMO_MODE)
  </read_first>
  <action>
Create src/e2e/transfer.spec.ts.

CRITICAL: Read ALL transfer flow pages first. The test must follow the actual navigation flow with real selectors.

The test needs an authenticated session. Two approaches:
1. **Preferred: Reuse auth from registration test** — Use Playwright's `storageState` to save session after login in a global setup, then load it in this test.
2. **Simpler: Log in at the start of this test** — Navigate through the demo login flow (phone -> OTP -> passcode) at the start of the test, then proceed to transfer.

Choose approach 2 (simpler) unless the registration spec already sets up storageState. Either way, read registration.spec.ts first to see what pattern it uses.

The transfer happy path:
1. Authenticate (demo login flow or storageState)
2. Navigate to /transfer/recipient (or /transfer)
3. Either select an existing recipient OR create a new one:
   - If creating new: fill first_name, last_name, phone (+959...), transfer_purpose, relationship, address fields, submit
   - If existing recipients are seeded in demo mode, select one
4. Enter transfer amount (e.g., 10000 = 100 THB) on the amount page
5. Select transfer channel (e.g., wave_agent) if it's a separate step
6. On confirmation page — verify sender/receiver details are shown, click confirm button
7. Wait for receipt page — verify URL contains /receipt, verify receipt content is displayed (amount, status)

Test structure:
```typescript
import { test, expect } from '@playwright/test'

test.describe('Transfer flow', () => {
  test('completes transfer and shows receipt', async ({ page }) => {
    // Auth: login with demo credentials
    // ... (same pattern as registration spec)

    // Navigate to transfer
    // ... select/create recipient
    // ... enter amount
    // ... confirm
    // ... verify receipt
    await expect(page).toHaveURL(/receipt/)
  })
})
```

Use `test.slow()` since this is a multi-step flow.
Use `page.waitForURL()` between navigation steps.
Use realistic selectors based on what the pages actually render.

If the transfer flow requires a wallet balance (checked at the API level), the mock payment API in DEMO_MODE should handle this. Verify by reading the mock-payment route.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx playwright test src/e2e/transfer.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/e2e/transfer.spec.ts exists with at least 1 test
    - Test authenticates, navigates transfer flow, reaches receipt page
    - `npx playwright test src/e2e/transfer.spec.ts` passes
  </acceptance_criteria>
  <done>Transfer confirmation -> receipt E2E happy path completes with DEMO_MODE=true. TEST-05 satisfied.</done>
</task>

</tasks>

<verification>
- `npx playwright test src/e2e/transfer.spec.ts --reporter=line` passes
- `npm run test:e2e` runs both registration and transfer E2E tests, both pass
</verification>

<success_criteria>
TEST-05 satisfied: `npm run test:e2e` covers the transfer confirmation -> receipt happy path end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-coverage/16-05-SUMMARY.md`
</output>
