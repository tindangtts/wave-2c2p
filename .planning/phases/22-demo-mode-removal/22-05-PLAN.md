---
phase: 22-demo-mode-removal
plan: "05"
type: execute
wave: 2
depends_on:
  - "22-01"
  - "22-02"
  - "22-03"
  - "22-04"
files_modified: []
autonomous: true
requirements:
  - DATA-08

must_haves:
  truths:
    - "grep -rn isDemoMode src/ returns zero matches"
    - "grep -rn DEMO_MODE src/ returns zero matches"
    - "src/lib/demo.ts does not exist"
    - "npm run build completes without errors with DEMO_MODE unset"
  artifacts:
    - path: "src/"
      provides: "Zero demo references across entire source tree"
  key_links:
    - from: "npm run build"
      to: "TypeScript compiler"
      via: "Next.js build pipeline"
      pattern: "Route \\(app\\)"
---

<objective>
Final verification after all four Wave 1 cleanup plans have completed. Confirm zero isDemoMode/DEMO_MODE references exist anywhere in src/, then run npm run build to prove the codebase compiles cleanly without the demo module.

Purpose: Prove the removal is complete and the app builds correctly without demo code.
Output: Passing build output confirming DATA-08 success criteria are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/22-demo-mode-removal/22-01-SUMMARY.md
@.planning/phases/22-demo-mode-removal/22-02-SUMMARY.md
@.planning/phases/22-demo-mode-removal/22-03-SUMMARY.md
@.planning/phases/22-demo-mode-removal/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify zero isDemoMode/DEMO_MODE references across entire src/ tree</name>
  <files></files>
  <action>
    Run the following grep commands. Each MUST return zero matches (empty output or count of 0):

    1. Check for isDemoMode symbol:
       ```bash
       grep -rn "isDemoMode" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/
       ```

    2. Check for DEMO_MODE env variable:
       ```bash
       grep -rn "DEMO_MODE" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/
       ```

    3. Check for any remaining demo.ts imports:
       ```bash
       grep -rn "from '.*lib/demo'\|from \".*lib/demo\"" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/
       ```

    4. Confirm demo.ts file is gone:
       ```bash
       ls /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/lib/demo.ts 2>&1
       ```
       Expected: "No such file or directory"

    If ANY grep returns matches, identify which plan failed to remove that reference and fix it before proceeding to Task 2. Each remaining reference must be removed following the same pattern: delete the import line and the `if (isDemoMode) { ... }` block.
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|DEMO_MODE" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>The grep count returns 0. Zero isDemoMode or DEMO_MODE references exist anywhere in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Run production build with DEMO_MODE unset and confirm it passes</name>
  <files></files>
  <action>
    Run the production build with DEMO_MODE explicitly unset to confirm it completes without TypeScript errors or missing variable warnings:

    ```bash
    cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && DEMO_MODE= npm run build 2>&1
    ```

    Expected outcome:
    - Build completes (exit code 0)
    - Output contains "Route (app)" route listing
    - No TypeScript errors referencing isDemoMode, demo.ts, or missing exports
    - No "Cannot find module '@/lib/demo'" errors

    If the build fails with TypeScript errors:
    - Read the error output to identify which file still has a reference
    - Fix that file using the same removal pattern (remove import + remove isDemoMode block)
    - Re-run the build

    If the build fails for unrelated reasons (e.g., missing env vars for Supabase/DB), that is acceptable — the goal is zero errors caused by the demo removal. Document any pre-existing build warnings in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && DEMO_MODE= npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build completes. The last 5 lines of build output show the route listing or "compiled successfully" — not a TypeScript error about isDemoMode or @/lib/demo.</done>
</task>

</tasks>

<verification>
Both tasks pass:
1. `grep -rn "isDemoMode\|DEMO_MODE" src/` → 0 matches
2. `DEMO_MODE= npm run build` → exits 0 with no demo-related errors
3. `src/lib/demo.ts` does not exist
</verification>

<success_criteria>
- Zero isDemoMode or DEMO_MODE references in all of src/
- src/lib/demo.ts deleted
- npm run build passes with DEMO_MODE unset
- All four DATA-08 success criteria from ROADMAP.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-mode-removal/22-05-SUMMARY.md`
</output>
