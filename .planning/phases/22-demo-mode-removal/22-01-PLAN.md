---
phase: 22-demo-mode-removal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/wallet/route.ts
  - src/app/api/transactions/route.ts
  - src/app/api/wallet/transactions/route.ts
  - src/app/api/cards/route.ts
  - src/app/api/notifications/route.ts
  - src/app/api/voucher/route.ts
  - src/app/api/mock-payment/process-transfer/route.ts
  - src/app/api/mock-payment/p2p-transfer/route.ts
  - src/app/api/mock-payment/topup/route.ts
  - src/app/api/mock-payment/withdraw/route.ts
autonomous: true
requirements:
  - DATA-08

must_haves:
  truths:
    - "No file in Group A imports from @/lib/demo"
    - "No isDemoMode conditional block exists in any Group A file"
    - "Every Group A route executes its Drizzle path unconditionally"
  artifacts:
    - path: "src/app/api/wallet/route.ts"
      provides: "Wallet API — real Drizzle path only"
    - path: "src/app/api/transactions/route.ts"
      provides: "Transactions API — real Drizzle path only"
    - path: "src/app/api/wallet/transactions/route.ts"
      provides: "Recent transactions API — real Drizzle path only"
    - path: "src/app/api/cards/route.ts"
      provides: "Cards API — real Drizzle path only"
    - path: "src/app/api/notifications/route.ts"
      provides: "Notifications API — real Drizzle path only"
    - path: "src/app/api/voucher/route.ts"
      provides: "Voucher API — real Drizzle path only"
    - path: "src/app/api/mock-payment/process-transfer/route.ts"
      provides: "Transfer API — real Drizzle path only"
    - path: "src/app/api/mock-payment/p2p-transfer/route.ts"
      provides: "P2P transfer API — real Drizzle path only"
    - path: "src/app/api/mock-payment/topup/route.ts"
      provides: "Topup API — real Drizzle path only"
    - path: "src/app/api/mock-payment/withdraw/route.ts"
      provides: "Withdraw API — real Drizzle path only"
  key_links:
    - from: "each Group A route"
      to: "Drizzle db"
      via: "direct import, no isDemoMode guard"
      pattern: "^(?!.*isDemoMode).*import.*from '@/db'"
---

<objective>
Remove all isDemoMode imports and conditional branches from the 10 Group A API routes (those already migrated to Drizzle in Phases 18-21). These files all follow the same pattern: an early `if (isDemoMode) { return mock }` block guarding the real Drizzle path. After this plan every route runs its Drizzle path unconditionally.

Purpose: Eliminate the dead demo branches from the data-layer routes so they exclusively use the real Supabase/Drizzle path.
Output: 10 modified route files with zero isDemoMode references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Pattern to apply in every file below. Mechanical removal. -->

BEFORE (example from wallet/route.ts):
```typescript
import { isDemoMode, DEMO_WALLET, DEMO_PROFILE } from "@/lib/demo";
// ...
if (isDemoMode) {
  return NextResponse.json({ ... });  // mock block
}
// real Drizzle code below
```

AFTER:
```typescript
// import line removed entirely
// if (isDemoMode) block removed entirely
// real Drizzle code moves up to fill the gap
```

Rules:
1. Remove the `import { isDemoMode, ... } from "@/lib/demo"` line entirely.
2. Remove the entire `if (isDemoMode) { ... return ... }` block.
3. Keep ALL code after the demo block unchanged.
4. Remove any local `DEMO_*` constant declarations defined in the file (e.g., `const DEMO_NOTIFICATIONS = [...]`).
5. Do NOT remove other imports, logic, or helper functions.
6. The file must still compile — check that no other identifier from the demo import is used outside the removed block.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove isDemoMode from wallet, transactions, wallet/transactions, cards, notifications, voucher</name>
  <read_first>
    - src/app/api/wallet/route.ts
    - src/app/api/transactions/route.ts
    - src/app/api/wallet/transactions/route.ts
    - src/app/api/cards/route.ts
    - src/app/api/notifications/route.ts
    - src/app/api/voucher/route.ts
  </read_first>
  <files>src/app/api/wallet/route.ts, src/app/api/transactions/route.ts, src/app/api/wallet/transactions/route.ts, src/app/api/cards/route.ts, src/app/api/notifications/route.ts, src/app/api/voucher/route.ts</files>
  <action>
    Apply the removal pattern (defined in interfaces block above) to each of these 6 files:

    **src/app/api/wallet/route.ts**
    - Remove: `import { isDemoMode, DEMO_WALLET, DEMO_PROFILE } from "@/lib/demo";`
    - Remove: `if (isDemoMode) { return NextResponse.json({ wallet: {...}, profile: {...} }); }` block (lines 10-23)

    **src/app/api/transactions/route.ts**
    - Remove: `import { isDemoMode, DEMO_TRANSACTIONS } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { ... return NextResponse.json(filtered) }` block (the entire demo branch inside GET)

    **src/app/api/wallet/transactions/route.ts**
    - Remove: `import { isDemoMode, DEMO_TRANSACTIONS } from "@/lib/demo";`
    - Remove: `if (isDemoMode) { return NextResponse.json({ transactions: DEMO_TRANSACTIONS.slice(0, 5) }); }` block

    **src/app/api/cards/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ cards: [...] }) }` block

    **src/app/api/notifications/route.ts**
    - Remove: `import { isDemoMode } from "@/lib/demo"`
    - Remove: the `DEMO_NOTIFICATIONS` array constant declared at the top of the file
    - Remove: `if (isDemoMode) { return NextResponse.json({ notifications: DEMO_NOTIFICATIONS }) }` in GET
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` in PATCH

    **src/app/api/voucher/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: the `VALID_VOUCHERS` in-memory map constant
    - Remove: `if (isDemoMode) { ... return NextResponse.json({ success: true, voucher: {...} }) }` block
    - NOTE: The VALID_VOUCHERS map is only used inside the demo block — safe to delete entirely

    After editing each file, confirm the file still has a valid import section and the remaining code is intact.
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/wallet/route.ts src/app/api/transactions/route.ts src/app/api/wallet/transactions/route.ts src/app/api/cards/route.ts src/app/api/notifications/route.ts src/app/api/voucher/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0. All 6 files have no isDemoMode or demo import references.</done>
</task>

<task type="auto">
  <name>Task 2: Remove isDemoMode from process-transfer, p2p-transfer, topup, withdraw</name>
  <read_first>
    - src/app/api/mock-payment/process-transfer/route.ts
    - src/app/api/mock-payment/p2p-transfer/route.ts
    - src/app/api/mock-payment/topup/route.ts
    - src/app/api/mock-payment/withdraw/route.ts
  </read_first>
  <files>src/app/api/mock-payment/process-transfer/route.ts, src/app/api/mock-payment/p2p-transfer/route.ts, src/app/api/mock-payment/topup/route.ts, src/app/api/mock-payment/withdraw/route.ts</files>
  <action>
    Apply the removal pattern (defined in interfaces block above) to each of these 4 payment routes:

    **src/app/api/mock-payment/process-transfer/route.ts**
    - Remove: `import { isDemoMode } from "@/lib/demo";`
    - Remove: the entire `if (isDemoMode) { const body = await request.json(); ... return NextResponse.json({...}); }` early-return block at the top of POST
    - Keep: everything from `const supabase = await createClient();` onward unchanged

    **src/app/api/mock-payment/p2p-transfer/route.ts**
    - Remove: the `isDemoMode` import line (also removes DEMO_USER, DEMO_WALLET if imported)
    - Remove: the `if (isDemoMode) { ... return NextResponse.json({...}); }` early-return block in POST
    - Keep: the Drizzle path (supabase auth + wallet lookup + db.batch()) unchanged

    **src/app/api/mock-payment/topup/route.ts**
    - Remove: the `isDemoMode` import line (also removes DEMO_USER, DEMO_WALLET if imported)
    - Remove: the `if (isDemoMode) { ... return NextResponse.json({...}); }` early-return block in POST
    - Keep: the real Drizzle path unchanged

    **src/app/api/mock-payment/withdraw/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: the `if (isDemoMode) { ... return NextResponse.json({...}); }` early-return block in POST
    - Keep: the real Drizzle path unchanged

    After editing each file, the POST handler in each route should start with Supabase auth (createClient / getUser) as the first action inside the try block, after any validation.
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/mock-payment/process-transfer/route.ts src/app/api/mock-payment/p2p-transfer/route.ts src/app/api/mock-payment/topup/route.ts src/app/api/mock-payment/withdraw/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0. All 4 payment routes have no isDemoMode or demo import references.</done>
</task>

</tasks>

<verification>
After both tasks: `grep -rn "isDemoMode\|from '.*lib/demo'\|from \".*lib/demo\"" src/app/api/wallet/route.ts src/app/api/transactions/route.ts src/app/api/wallet/transactions/route.ts src/app/api/cards/route.ts src/app/api/notifications/route.ts src/app/api/voucher/route.ts src/app/api/mock-payment/process-transfer/route.ts src/app/api/mock-payment/p2p-transfer/route.ts src/app/api/mock-payment/topup/route.ts src/app/api/mock-payment/withdraw/route.ts` must return zero matches.
</verification>

<success_criteria>
All 10 Group A route files contain zero references to isDemoMode, DEMO_MODE, or @/lib/demo. Each file retains its complete Drizzle real path. TypeScript compilation errors are not introduced (no removed identifiers still referenced).
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-mode-removal/22-01-SUMMARY.md`
</output>
