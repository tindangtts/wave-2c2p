---
phase: 22-demo-mode-removal
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/bank-accounts/route.ts
  - src/app/api/spending-limits/route.ts
  - src/app/api/statement/route.ts
  - src/app/api/referral/stats/route.ts
  - src/app/api/mock-kyc/submit/route.ts
  - src/app/api/mock-payment/refresh-secret-code/route.ts
  - src/app/api/recipients/route.ts
  - src/app/api/recipients/[id]/route.ts
autonomous: true
requirements:
  - DATA-08

must_haves:
  truths:
    - "No file in Group C imports from @/lib/demo"
    - "No isDemoMode conditional block exists in any Group C file"
    - "All Group C routes execute only the real Supabase/Drizzle path"
  artifacts:
    - path: "src/app/api/bank-accounts/route.ts"
      provides: "Bank accounts API — real path only"
    - path: "src/app/api/spending-limits/route.ts"
      provides: "Spending limits API — real path only"
    - path: "src/app/api/statement/route.ts"
      provides: "Statement API — real Supabase path only"
    - path: "src/app/api/referral/stats/route.ts"
      provides: "Referral stats API — real Supabase path only"
    - path: "src/app/api/mock-kyc/submit/route.ts"
      provides: "KYC submit — real path only"
    - path: "src/app/api/mock-payment/refresh-secret-code/route.ts"
      provides: "Refresh secret code — real path only"
    - path: "src/app/api/recipients/route.ts"
      provides: "Recipients GET/POST — real path only"
    - path: "src/app/api/recipients/[id]/route.ts"
      provides: "Recipients PUT — real path only"
  key_links:
    - from: "bank-accounts GET/POST/DELETE"
      to: "Supabase bank_accounts table"
      via: "createClient(), no isDemoMode guard"
      pattern: "supabase.from\\('bank_accounts'\\)"
---

<objective>
Remove all isDemoMode imports and conditional branches from the 8 Group C API routes: bank-accounts, spending-limits, statement, referral/stats, mock-kyc/submit, mock-payment/refresh-secret-code, recipients, and recipients/[id].

Purpose: Eliminate the remaining demo branches from the other API routes so they run exclusively on real Supabase data.
Output: 8 modified route files with zero isDemoMode references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Same mechanical pattern as Plans 01 and 02. -->

BEFORE:
```typescript
import { isDemoMode, DEMO_USER } from '@/lib/demo'
const DEMO_BANK_ACCOUNTS = [...]   // local constant in file

export async function GET() {
  try {
    if (isDemoMode) {
      return NextResponse.json({ bank_accounts: DEMO_BANK_ACCOUNTS })
    }
    // real Supabase path...
  }
}
```

AFTER:
```typescript
// import removed
// DEMO_BANK_ACCOUNTS constant removed
export async function GET() {
  try {
    // real Supabase path starts immediately
  }
}
```

Rules:
1. Remove the `import { isDemoMode, ... } from '@/lib/demo'` line.
2. Remove any local `DEMO_*` constant declared in the file.
3. Remove every `if (isDemoMode) { ... }` block (may appear in multiple handlers in the same file).
4. Keep ALL remaining code unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove isDemoMode from bank-accounts, spending-limits, statement, referral/stats</name>
  <read_first>
    - src/app/api/bank-accounts/route.ts
    - src/app/api/spending-limits/route.ts
    - src/app/api/statement/route.ts
    - src/app/api/referral/stats/route.ts
  </read_first>
  <files>src/app/api/bank-accounts/route.ts, src/app/api/spending-limits/route.ts, src/app/api/statement/route.ts, src/app/api/referral/stats/route.ts</files>
  <action>
    **src/app/api/bank-accounts/route.ts**
    - Remove: `import { isDemoMode, DEMO_USER } from '@/lib/demo'`
    - Remove: `const DEMO_BANK_ACCOUNTS = [...]` array (used only in demo block)
    - Remove: `if (isDemoMode) { return NextResponse.json({ bank_accounts: DEMO_BANK_ACCOUNTS }) }` in GET
    - Remove: `if (isDemoMode) { if (!parseResult.success) {...} return NextResponse.json({ bank_account: {...} }, { status: 201 }) }` in POST
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` in DELETE
    - NOTE: The POST handler has a subtlety — after removing the demo block, the `const supabase = createClient()` and `getUser()` calls come next, followed by a second `if (!parseResult.success)` check. This second validation check already exists in the real path and must be kept. The overall POST flow after removal is: parse body → safeParse → createClient() + getUser() → if (!parseResult.success) return 400 → insert.

    **src/app/api/spending-limits/route.ts**
    - Remove: `import { isDemoMode } from "@/lib/demo"`
    - Remove: `const DEMO_SPENDING_LIMITS = {...}` constant
    - Remove: `if (isDemoMode) { return NextResponse.json(DEMO_SPENDING_LIMITS) }` in GET
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` in PATCH (which comes after the tier parsing)

    **src/app/api/statement/route.ts**
    - Remove: `import { isDemoMode, DEMO_TRANSACTIONS } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { const from = ...; const to = ...; const filtered = ...; return NextResponse.json({ transactions: filtered }) }` block

    **src/app/api/referral/stats/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ referredCount: 3, totalBonusSatang: 30000, referralCode: 'WAVE2C2P' }) }` block
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/bank-accounts/route.ts src/app/api/spending-limits/route.ts src/app/api/statement/route.ts src/app/api/referral/stats/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove isDemoMode from mock-kyc/submit, refresh-secret-code, recipients, recipients/[id]</name>
  <read_first>
    - src/app/api/mock-kyc/submit/route.ts
    - src/app/api/mock-payment/refresh-secret-code/route.ts
    - src/app/api/recipients/route.ts
    - src/app/api/recipients/[id]/route.ts
  </read_first>
  <files>src/app/api/mock-kyc/submit/route.ts, src/app/api/mock-payment/refresh-secret-code/route.ts, src/app/api/recipients/route.ts, src/app/api/recipients/[id]/route.ts</files>
  <action>
    **src/app/api/mock-kyc/submit/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true, status: 'pending', verification_id: `KYC-${Date.now()}` }) }` early-return block
    - Keep: everything from `const supabase = await createClient()` onward unchanged

    **src/app/api/mock-payment/refresh-secret-code/route.ts**
    - Remove: `import { isDemoMode } from "@/lib/demo";`
    - Remove: `if (isDemoMode) { const { transaction_id } = body...; if (!transaction_id) {...} const secret_code = ...; return NextResponse.json({ success: true, secret_code }) }` demo block
    - Keep: the real path (supabase auth + transaction_id validation + secret_code generation) unchanged
    - NOTE: Both the demo block and real path parse `transaction_id` from body. After removing the demo block, the `const { transaction_id } = body` extraction on the real path already handles this.

    **src/app/api/recipients/route.ts**
    - Remove: `import { isDemoMode, DEMO_RECIPIENTS } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { const sorted = [...DEMO_RECIPIENTS].sort(...); return NextResponse.json({ recipients: sorted }) }` block in GET
    - NOTE: The POST handler in this file does NOT have an isDemoMode block — leave it unchanged.

    **src/app/api/recipients/[id]/route.ts**
    - Remove: `import { isDemoMode, DEMO_RECIPIENTS } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { const { id } = await params; const recipient = DEMO_RECIPIENTS.find(...); if (!recipient) {...} return NextResponse.json({ recipient }) }` block in PUT
    - Keep: `const { id } = await params` on the real path (it appears again after the demo block)
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/mock-kyc/submit/route.ts src/app/api/mock-payment/refresh-secret-code/route.ts src/app/api/recipients/route.ts "src/app/api/recipients/[id]/route.ts" 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0. All 4 files are clean.</done>
</task>

</tasks>

<verification>
After both tasks: `grep -rn "isDemoMode\|from '.*lib/demo'\|from \".*lib/demo\"" src/app/api/bank-accounts/ src/app/api/spending-limits/ src/app/api/statement/ src/app/api/referral/ src/app/api/mock-kyc/ src/app/api/mock-payment/refresh-secret-code/ src/app/api/recipients/` must return zero matches.
</verification>

<success_criteria>
All 8 Group C route files contain zero references to isDemoMode or @/lib/demo. Files retain their real Supabase paths. No TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-mode-removal/22-03-SUMMARY.md`
</output>
