---
phase: 22-demo-mode-removal
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/auth/passcode/verify/route.ts
  - src/app/api/auth/passcode/setup/route.ts
  - src/app/api/auth/change-passcode/route.ts
  - src/app/api/auth/change-phone/route.ts
  - src/app/api/auth/verify-change-phone/route.ts
  - src/app/api/auth/register/step/route.ts
  - src/app/api/auth/register/consent/route.ts
  - src/app/api/auth/webauthn/register/route.ts
  - src/app/api/auth/webauthn/register/verify/route.ts
  - src/app/api/auth/webauthn/authenticate/route.ts
  - src/app/api/auth/webauthn/authenticate/verify/route.ts
autonomous: true
requirements:
  - DATA-08

must_haves:
  truths:
    - "No auth route imports from @/lib/demo"
    - "No isDemoMode conditional block exists in any Group B file"
    - "All auth routes execute only the real Supabase path"
  artifacts:
    - path: "src/app/api/auth/passcode/verify/route.ts"
      provides: "Passcode verify — real path only"
    - path: "src/app/api/auth/passcode/setup/route.ts"
      provides: "Passcode setup — real path only"
    - path: "src/app/api/auth/webauthn/register/route.ts"
      provides: "WebAuthn register — real path only"
  key_links:
    - from: "each Group B auth route"
      to: "Supabase auth.getUser()"
      via: "createClient(), no isDemoMode guard"
      pattern: "createClient"
---

<objective>
Remove all isDemoMode imports and conditional branches from the 11 Group B auth API routes (passcode, change-passcode, change-phone, register steps, webauthn). These routes use Supabase auth (not Drizzle) and the demo branches are simple early-return stubs. After this plan every auth route runs its real Supabase path unconditionally.

Purpose: Eliminate dead demo early-returns from auth routes.
Output: 11 modified auth route files with zero isDemoMode references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- All Group B files follow the same minimal pattern: -->

BEFORE:
```typescript
import { isDemoMode } from '@/lib/demo'
// ...
export async function POST(request: NextRequest) {
  try {
    if (isDemoMode) {
      return NextResponse.json({ success: true })   // or similar stub
    }
    // real Supabase logic...
  }
}
```

AFTER:
```typescript
// import line removed
// if (isDemoMode) block removed
export async function POST(request: NextRequest) {
  try {
    // real Supabase logic starts here immediately
  }
}
```

Rules (same as Plan 01):
1. Remove the `import { isDemoMode, ... } from '@/lib/demo'` line.
2. Remove the entire `if (isDemoMode) { ... return ... }` block.
3. Keep ALL remaining code unchanged.
4. Do NOT remove other imports or logic.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove isDemoMode from passcode, change-passcode, change-phone, verify-change-phone, register routes</name>
  <read_first>
    - src/app/api/auth/passcode/verify/route.ts
    - src/app/api/auth/passcode/setup/route.ts
    - src/app/api/auth/change-passcode/route.ts
    - src/app/api/auth/change-phone/route.ts
    - src/app/api/auth/verify-change-phone/route.ts
    - src/app/api/auth/register/step/route.ts
    - src/app/api/auth/register/consent/route.ts
  </read_first>
  <files>src/app/api/auth/passcode/verify/route.ts, src/app/api/auth/passcode/setup/route.ts, src/app/api/auth/change-passcode/route.ts, src/app/api/auth/change-phone/route.ts, src/app/api/auth/verify-change-phone/route.ts, src/app/api/auth/register/step/route.ts, src/app/api/auth/register/consent/route.ts</files>
  <action>
    Read each file in full, then apply the removal pattern (interfaces block above) to all 7 files:

    **src/app/api/auth/passcode/verify/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` block (after format validation, before createClient)

    **src/app/api/auth/passcode/setup/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` early-return block

    **src/app/api/auth/change-passcode/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ success: true }) }` early-return block

    **src/app/api/auth/change-phone/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block

    **src/app/api/auth/verify-change-phone/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block

    **src/app/api/auth/register/step/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { const body = await request.json(); const { step } = body...; return NextResponse.json({ success: true, nextStep: ... }) }` early-return block
    - Keep: the `const body = await request.json()` on the real path (it appears again after the demo block)

    **src/app/api/auth/register/consent/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/auth/passcode/verify/route.ts src/app/api/auth/passcode/setup/route.ts src/app/api/auth/change-passcode/route.ts src/app/api/auth/change-phone/route.ts src/app/api/auth/verify-change-phone/route.ts src/app/api/auth/register/step/route.ts src/app/api/auth/register/consent/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove isDemoMode from all webauthn routes</name>
  <read_first>
    - src/app/api/auth/webauthn/register/route.ts
    - src/app/api/auth/webauthn/register/verify/route.ts
    - src/app/api/auth/webauthn/authenticate/route.ts
    - src/app/api/auth/webauthn/authenticate/verify/route.ts
  </read_first>
  <files>src/app/api/auth/webauthn/register/route.ts, src/app/api/auth/webauthn/register/verify/route.ts, src/app/api/auth/webauthn/authenticate/route.ts, src/app/api/auth/webauthn/authenticate/verify/route.ts</files>
  <action>
    Apply the removal pattern to all 4 WebAuthn route files:

    **src/app/api/auth/webauthn/register/route.ts**
    - Remove: `import { isDemoMode } from '@/lib/demo'`
    - Remove: `if (isDemoMode) { return NextResponse.json({ options: null, mock: true }) }` early-return block
    - Keep: all WebAuthn registration logic (generateRegistrationOptions, supabase getUser, etc.) unchanged

    **src/app/api/auth/webauthn/register/verify/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block

    **src/app/api/auth/webauthn/authenticate/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block

    **src/app/api/auth/webauthn/authenticate/verify/route.ts**
    - Read first to identify exact isDemoMode block, then remove import + block
  </action>
  <verify>
    <automated>grep -rn "isDemoMode\|from '@/lib/demo'\|from \"@/lib/demo\"" src/app/api/auth/webauthn/register/route.ts src/app/api/auth/webauthn/register/verify/route.ts src/app/api/auth/webauthn/authenticate/route.ts src/app/api/auth/webauthn/authenticate/verify/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>The above grep returns 0. All 4 WebAuthn routes have no isDemoMode or demo import references.</done>
</task>

</tasks>

<verification>
After both tasks: `grep -rn "isDemoMode\|from '.*lib/demo'\|from \".*lib/demo\"" src/app/api/auth/` must return zero matches.
</verification>

<success_criteria>
All 11 Group B auth route files contain zero references to isDemoMode or @/lib/demo. Each file retains its complete Supabase auth path. No TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-mode-removal/22-02-SUMMARY.md`
</output>
