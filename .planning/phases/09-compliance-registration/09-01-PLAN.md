---
phase: 09-compliance-registration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/registration-store.ts
  - messages/en/auth.json
  - messages/th/auth.json
  - messages/mm/auth.json
  - src/app/api/auth/register/consent/route.ts
autonomous: true
requirements:
  - COMP-02

must_haves:
  truths:
    - "registration-store exports tcAcceptedAt (string) and tcVersion (string) fields with setConsent action"
    - "auth.json compliance sub-key exists in all three locales (en, th, mm) with all copy strings from UI-SPEC"
    - "POST /api/auth/register/consent persists tc_accepted_at and tc_version to user_profiles and returns 200"
  artifacts:
    - path: "src/stores/registration-store.ts"
      provides: "Zustand registration store with consent fields"
      contains: "tcAcceptedAt"
    - path: "src/app/api/auth/register/consent/route.ts"
      provides: "API endpoint persisting consent to Supabase"
      exports: ["POST"]
    - path: "messages/en/auth.json"
      provides: "English compliance copy strings"
      contains: "compliance"
  key_links:
    - from: "src/stores/registration-store.ts"
      to: "tcAcceptedAt / tcVersion fields"
      via: "setConsent action"
      pattern: "setConsent"
    - from: "src/app/api/auth/register/consent/route.ts"
      to: "supabase user_profiles"
      via: "supabase.from('user_profiles').update"
      pattern: "tc_accepted_at"
---

<objective>
Create the data foundation for Phase 9: extend the registration store with consent fields, add all compliance copy strings to the i18n files, and create the consent persistence API endpoint.

Purpose: Plans 02 and 03 build UI on top of these contracts. Doing this in Wave 1 means no executor needs to discover field names or copy strings.
Output: Updated registration-store.ts, updated auth.json in all 3 locales, new consent API route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-compliance-registration/09-CONTEXT.md
@.planning/phases/09-compliance-registration/09-UI-SPEC.md

<interfaces>
<!-- Current registration store shape (src/stores/registration-store.ts) -->
```typescript
export interface RegistrationState {
  step: 1 | 2 | 3
  phone: string
  countryCode: '+66' | '+95'
  firstName: string
  lastName: string
  dateOfBirth: string
  nationality: 'thai' | 'myanmar' | 'other' | ''
  idType: 'national_id' | 'passport' | 'work_permit' | 'other' | ''
  idNumber: string
  idExpiry: string
}
// persist key: 'wave-registration-state'
// partialize excludes actions
```

<!-- Current auth.json top-level keys -->
// login, otp, register, passcode, cta, errors, fields

<!-- Supabase user_profiles columns (from schema) -->
// id, full_name, phone, country_code, wallet_id, kyc_status, language, avatar_url, created_at, updated_at
// (tc_accepted_at and tc_version NOT YET in schema — add via migration in this task)

<!-- Existing register/step API pattern (src/app/api/auth/register/step/route.ts) -->
// POST { step, data } → supabase.from('user_profiles').update({...}).eq('id', user.id)
// createClient() from '@/lib/supabase/server'
// Requires authenticated session (supabase.auth.getUser())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend registration store with consent fields + bump persist version</name>
  <read_first>
    - src/stores/registration-store.ts (current store — read before ANY edit)
  </read_first>
  <files>src/stores/registration-store.ts</files>
  <action>
Add two new fields to RegistrationState interface and initialState, plus a setConsent action. Also bump the persist version to prevent stale localStorage hydration (STATE.md pitfall 5).

Exact changes:

1. Add to RegistrationState interface (after idExpiry):
```typescript
  // Compliance consent (Phase 9)
  tcAcceptedAt: string   // ISO timestamp or '' if not yet accepted
  tcVersion: string      // e.g. "1.0" or '' if not yet accepted
```

2. Add to initialState:
```typescript
  tcAcceptedAt: '',
  tcVersion: '',
```

3. Add to RegistrationActions interface:
```typescript
  setConsent: (acceptedAt: string, version: string) => void
```

4. Add action implementation inside create():
```typescript
setConsent: (acceptedAt, version) => set({ tcAcceptedAt: acceptedAt, tcVersion: version }),
```

5. Add both fields to partialize:
```typescript
tcAcceptedAt: state.tcAcceptedAt,
tcVersion: state.tcVersion,
```

6. Change persist name from `'wave-registration-state'` to `'wave-registration-state-v2'` to force fresh hydration.

Do NOT change any existing fields, actions, or logic.
  </action>
  <verify>
    <automated>grep -n "tcAcceptedAt\|tcVersion\|setConsent\|wave-registration-state-v2" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/stores/registration-store.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "tcAcceptedAt" on exactly 4 lines (interface, initialState, action impl, partialize)
    - grep finds "tcVersion" on exactly 4 lines (interface, initialState, action impl, partialize)
    - grep finds "setConsent" on exactly 2 lines (interface + impl)
    - grep finds "wave-registration-state-v2" on exactly 1 line
  </acceptance_criteria>
  <done>registration-store.ts exports tcAcceptedAt, tcVersion, and setConsent with persist key bumped to v2</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add compliance copy strings to all three locale auth.json files</name>
  <read_first>
    - messages/en/auth.json (read BEFORE editing — must append, not replace)
    - messages/th/auth.json
    - messages/mm/auth.json
  </read_first>
  <files>messages/en/auth.json, messages/th/auth.json, messages/mm/auth.json</files>
  <action>
Add a `"compliance"` sub-key to each auth.json file. Insert it as the last key before the closing brace. Do not modify any existing keys.

English (messages/en/auth.json) — add:
```json
  "compliance": {
    "preReg": {
      "heading": "Before You Register",
      "subtext": "Please have the following documents ready. You will need them to complete your registration.",
      "doc1": "Thai National ID Card or Passport",
      "doc2": "Work Permit (if applicable)",
      "cta": "Continue to Registration"
    },
    "terms": {
      "title": "Terms & Conditions",
      "checkbox1": "I have read and agree to the Terms & Conditions",
      "checkbox2": "I have read and agree to the Privacy Policy",
      "cta": "Agree and Continue",
      "errorBothRequired": "Please accept both the Terms & Conditions and Privacy Policy to continue"
    },
    "dailyLimit": {
      "title": "Transfer Limits",
      "cardHeading": "Daily Transfer Limits",
      "tier1Label": "KYC Pending",
      "tier1Amount": "฿50,000",
      "tier1Currency": "THB / day",
      "tier2Label": "KYC Approved",
      "tier2Amount": "฿500,000",
      "tier2Currency": "THB / day",
      "disclaimer": "Limits apply to all outgoing transfers combined across all channels.",
      "cta": "I Understand"
    },
    "selfie": {
      "instruction": "Position your face within the circle",
      "helper": "Look directly at the camera and ensure good lighting",
      "reviewTitle": "Check Your Selfie",
      "reviewHint": "Make sure your face is clearly visible with no obstructions"
    }
  }
```

Thai (messages/th/auth.json) — add (translated):
```json
  "compliance": {
    "preReg": {
      "heading": "ก่อนเริ่มลงทะเบียน",
      "subtext": "กรุณาเตรียมเอกสารต่อไปนี้ให้พร้อม คุณจะต้องใช้เพื่อทำการลงทะเบียนให้เสร็จสมบูรณ์",
      "doc1": "บัตรประชาชนไทย หรือ หนังสือเดินทาง",
      "doc2": "ใบอนุญาตทำงาน (ถ้ามี)",
      "cta": "ดำเนินการลงทะเบียน"
    },
    "terms": {
      "title": "ข้อกำหนดและเงื่อนไข",
      "checkbox1": "ฉันได้อ่านและยอมรับข้อกำหนดและเงื่อนไข",
      "checkbox2": "ฉันได้อ่านและยอมรับนโยบายความเป็นส่วนตัว",
      "cta": "ยอมรับและดำเนินการต่อ",
      "errorBothRequired": "กรุณายอมรับทั้งข้อกำหนดและเงื่อนไข และนโยบายความเป็นส่วนตัวเพื่อดำเนินการต่อ"
    },
    "dailyLimit": {
      "title": "วงเงินโอน",
      "cardHeading": "วงเงินโอนต่อวัน",
      "tier1Label": "รอยืนยัน KYC",
      "tier1Amount": "฿50,000",
      "tier1Currency": "บาท / วัน",
      "tier2Label": "ยืนยัน KYC แล้ว",
      "tier2Amount": "฿500,000",
      "tier2Currency": "บาท / วัน",
      "disclaimer": "วงเงินนี้ใช้สำหรับการโอนเงินขาออกทุกช่องทางรวมกัน",
      "cta": "รับทราบ"
    },
    "selfie": {
      "instruction": "วางใบหน้าของคุณในวงกลม",
      "helper": "มองตรงไปที่กล้องและให้แน่ใจว่ามีแสงสว่างเพียงพอ",
      "reviewTitle": "ตรวจสอบรูปเซลฟี่",
      "reviewHint": "ตรวจสอบให้แน่ใจว่าใบหน้าของคุณมองเห็นชัดเจนโดยไม่มีสิ่งกีดขวาง"
    }
  }
```

Myanmar (messages/mm/auth.json) — add (translated):
```json
  "compliance": {
    "preReg": {
      "heading": "မှတ်ပုံတင်ခြင်းမပြုမီ",
      "subtext": "အောက်ပါစာရွက်စာတမ်းများကို ကြိုတင်ပြင်ဆင်ထားပါ။ မှတ်ပုံတင်ခြင်းပြီးမြောက်ရန် လိုအပ်မည်ဖြစ်သည်။",
      "doc1": "ထိုင်းနိုင်ငံသားစိစစ်ရေးကတ် သို့မဟုတ် နိုင်ငံကူးလက်မှတ်",
      "doc2": "အလုပ်လုပ်ကိုင်ခွင့်ပါမစ် (ရှိပါက)",
      "cta": "မှတ်ပုံတင်ခြင်းဆက်လုပ်ပါ"
    },
    "terms": {
      "title": "စည်းကမ်းသတ်မှတ်ချက်များ",
      "checkbox1": "ကျွန်ုပ်သည် စည်းကမ်းသတ်မှတ်ချက်များကို ဖတ်ရှုပြီး သဘောတူပါသည်",
      "checkbox2": "ကျွန်ုပ်သည် ကိုယ်ရေးကိုယ်တာမူဝါဒကို ဖတ်ရှုပြီး သဘောတူပါသည်",
      "cta": "သဘောတူပြီး ဆက်လက်ဆောင်ရွက်ပါ",
      "errorBothRequired": "ဆက်လက်ဆောင်ရွက်ရန် စည်းကမ်းသတ်မှတ်ချက်များနှင့် ကိုယ်ရေးကိုယ်တာမူဝါဒနှစ်ခုလုံးကို သဘောတူပါ"
    },
    "dailyLimit": {
      "title": "လွှဲငွေကန့်သတ်ချက်",
      "cardHeading": "နေ့စဉ်လွှဲငွေကန့်သတ်ချက်",
      "tier1Label": "KYC စောင့်ဆိုင်းဆဲ",
      "tier1Amount": "฿50,000",
      "tier1Currency": "ဘတ် / နေ့",
      "tier2Label": "KYC အတည်ပြုပြီး",
      "tier2Amount": "฿500,000",
      "tier2Currency": "ဘတ် / နေ့",
      "disclaimer": "ကန့်သတ်ချက်သည် ချန်နယ်အားလုံးမှ ထွက်သွားသော ငွေလွှဲမှုများ ပေါင်းစုမှုကို သက်ရောက်သည်",
      "cta": "နားလည်ပါသည်"
    },
    "selfie": {
      "instruction": "သင့်မျက်နှာကို စက်ဝိုင်းအတွင်း ထားပါ",
      "helper": "ကင်မရာဆီ တိုက်ရိုက်ကြည့်ပြီး အလင်းရောင်ကောင်းမွန်ကြောင်း သေချာပါ",
      "reviewTitle": "သင်၏ Selfie စစ်ဆေးပါ",
      "reviewHint": "မည်သည့်အတားအဆီးမှမရှိဘဲ သင်၏မျက်နှာ ရှင်းရှင်းလင်းလင်း မြင်နိုင်ကြောင်း သေချာပါ"
    }
  }
```
  </action>
  <verify>
    <automated>grep -l '"compliance"' /Users/tindang/workspaces/tts/yoma/wave-2c2p/messages/en/auth.json /Users/tindang/workspaces/tts/yoma/wave-2c2p/messages/th/auth.json /Users/tindang/workspaces/tts/yoma/wave-2c2p/messages/mm/auth.json</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files returned by grep (compliance key present in all locales)
    - grep '"preReg"' finds match in all 3 files
    - grep '"selfie"' finds match in all 3 files
    - All 3 files are valid JSON (run: node -e "require('./messages/en/auth.json')" — no error)
  </acceptance_criteria>
  <done>All three locale auth.json files have a "compliance" sub-key with preReg, terms, dailyLimit, and selfie sections containing all copy strings from UI-SPEC Copywriting Contract</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create consent persistence API route + Supabase migration</name>
  <read_first>
    - src/app/api/auth/register/step/route.ts (existing pattern to match exactly)
    - .planning/supabase-schema.sql (current schema — tc columns not yet present)
  </read_first>
  <files>src/app/api/auth/register/consent/route.ts, supabase/migrations/20260415_add_tc_consent.sql</files>
  <action>
Create two files:

**File 1: src/app/api/auth/register/consent/route.ts**

POST endpoint that accepts `{ tcAcceptedAt: string, tcVersion: string }` and persists to `user_profiles`. Follow the exact same pattern as `register/step/route.ts`.

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { tcAcceptedAt, tcVersion } = body as { tcAcceptedAt: string; tcVersion: string }

    if (!tcAcceptedAt || !tcVersion) {
      return NextResponse.json({ error: 'Missing tcAcceptedAt or tcVersion' }, { status: 400 })
    }

    const supabase = await createClient()
    const { data: { user }, error: authError } = await supabase.auth.getUser()

    if (authError || !user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const { error: updateError } = await supabase
      .from('user_profiles')
      .update({
        tc_accepted_at: tcAcceptedAt,
        tc_version: tcVersion,
      })
      .eq('id', user.id)

    if (updateError) {
      return NextResponse.json({ error: 'Failed to save consent' }, { status: 500 })
    }

    return NextResponse.json({ success: true })
  } catch {
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

**File 2: supabase/migrations/20260415_add_tc_consent.sql**

```sql
-- Add T&C consent fields to user_profiles (Phase 9 COMP-02)
ALTER TABLE public.user_profiles
  ADD COLUMN IF NOT EXISTS tc_accepted_at timestamptz,
  ADD COLUMN IF NOT EXISTS tc_version text;
```

Create the `supabase/migrations/` directory if it does not exist. The migration file will be applied manually or via Supabase CLI — it is documentation of the schema change.
  </action>
  <verify>
    <automated>grep -n "tc_accepted_at\|tc_version\|tcAcceptedAt\|tcVersion" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/app/api/auth/register/consent/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/api/auth/register/consent/route.ts exists
    - grep finds "tc_accepted_at" in route.ts
    - grep finds "tc_version" in route.ts
    - grep finds "tcAcceptedAt" in route.ts
    - File supabase/migrations/20260415_add_tc_consent.sql exists
    - grep "ADD COLUMN" supabase/migrations/20260415_add_tc_consent.sql returns 2 lines
    - npm run build completes without TypeScript errors on the new route file
  </acceptance_criteria>
  <done>POST /api/auth/register/consent persists tc_accepted_at and tc_version to user_profiles, SQL migration file documents the schema change</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "tcAcceptedAt\|setConsent\|wave-registration-state-v2" src/stores/registration-store.ts` — all 3 terms found
2. `node -e "require('./messages/en/auth.json').compliance.terms.cta"` — prints "Agree and Continue"
3. `node -e "require('./messages/th/auth.json').compliance"` — no error
4. `node -e "require('./messages/mm/auth.json').compliance"` — no error
5. `curl -X POST /api/auth/register/consent -H "Content-Type: application/json" -d '{"tcAcceptedAt":"2026-01-01T00:00:00Z","tcVersion":"1.0"}'` — returns 401 (correct, no auth session in bare curl)
</verification>

<success_criteria>
- registration-store.ts has tcAcceptedAt, tcVersion fields and setConsent action, persist key is v2
- All 3 locale auth.json files have compliance sub-key with all 4 sections (preReg, terms, dailyLimit, selfie)
- consent API route file exists and follows same pattern as register/step/route.ts
- SQL migration file exists documenting tc_accepted_at and tc_version columns
- npm run build passes (no TypeScript errors in new files)
</success_criteria>

<output>
After completion, create `.planning/phases/09-compliance-registration/09-01-SUMMARY.md`
</output>
