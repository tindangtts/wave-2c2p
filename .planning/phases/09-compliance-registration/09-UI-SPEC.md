---
phase: 9
slug: compliance-registration
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-15
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for Phase 9: Compliance & Registration.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (components.json: style: "base-nova") |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React |
| Font | system-ui with next/font Thai + Noto Sans Myanmar UI fallback stacks |

Source: `components.json`, `globals.css`

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, checkbox-to-label gap, inline dividers |
| sm | 8px | Compact element spacing, helper text below fields |
| md | 16px | Default horizontal page padding (`--content-padding-x`), gap between checklist items |
| lg | 24px | Section top padding, gap between consent blocks |
| xl | 32px | Gap between the circular face guide and instruction text area |
| 2xl | 48px | Bottom CTA button top margin on info screens |
| 3xl | 64px | Page-level bottom padding before safe area |

Exceptions:
- Touch targets minimum 44×44px (all buttons, checkboxes, interactive icons) — WCAG 2.1 AA
- Circular face guide overlay: 240px diameter, centered in camera viewport
- Bottom CTA button: fixed height 48px (`h-12`), full width, rounded-full

Source: `globals.css --content-padding-x`, CLAUDE.md touch target requirement, CONTEXT.md Pencil selfie spec.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (1rem) | 400 | 1.5 |
| Label / Caption | 12px (0.75rem) | 400 | 1.4 |
| Heading | 20px (1.25rem) | 700 | 1.3 |
| Display | 28px (1.75rem) | 700 | 1.2 |

Notes:
- Label inside form fields: 12px / weight 400 / color `#595959` (matches existing personal-info pattern)
- Step counter ("Step 1/3") in BackHeader: 12px / weight 400 / color `#595959`
- Daily limit tier values: 20px / weight 700 / color `#212121`
- Checkbox label text: 16px / weight 400 — must be full-width tappable, not just the checkbox node

Source: `globals.css --text-*` tokens, `personal-info/page.tsx` inline classes.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #FFFFFF | Page background, all screen surfaces in auth flow |
| Secondary (30%) | #F5F5F5 | Read-only field backgrounds, daily limit card surface |
| Accent (10%) | #FFE600 | Primary CTA button bg, active checkbox border/fill ring |
| Destructive | #F44336 | Field validation errors only (`text-destructive`) |

Accent reserved for:
1. Primary CTA button background (Continue, I Understand, Confirm — all yellow pill buttons)
2. Active/checked state border ring on checkboxes (T&C and Privacy Policy)
3. Step indicator active dot

Secondary color (`#0091EA`) is used for:
- Focus-visible outlines (existing global rule in `globals.css`)
- Linked text within T&C scrollable body ("Terms & Conditions" and "Privacy Policy" hyperlinks)

Dark text `#212121` on yellow CTA backgrounds is required for WCAG AA contrast (locked decision from CLAUDE.md).

Source: `globals.css --background`, `--secondary`, `--primary: #FFE600`, `--destructive`, `--accent: #0091EA`.

---

## Screen Inventory

Four screens/states added or enhanced in this phase:

### Screen 1: Pre-Registration Info
- **Route**: Inserted between `/otp` completion and T&C screen
- **Layout**: BackHeader (no step counter) + scrollable content area + fixed bottom CTA
- **Content**: Heading + sub-paragraph + checklist of required documents (ID/Passport, Work Permit) — each item is an icon (Lucide `FileText`) + label row at 16px
- **Icon color**: `#0091EA` (brand blue) for document icons
- **CTA**: Yellow pill button "Continue" — full width, h-12

### Screen 2: T&C Consent
- **Route**: After pre-registration info, before daily limit
- **Layout**: BackHeader (title: "Terms & Conditions") + scrollable T&C text area + two separate checkboxes + fixed bottom CTA
- **Scrollable area**: `overflow-y-auto` with max-height `calc(100vh - 280px)`, border `1px solid #E0E0E0`, border-radius `--radius-lg (12px)`, inner padding 16px
- **Checkboxes**: shadcn `Checkbox` component — NOT pre-checked, each minimum 44×44px tap target achieved by wrapping in a label row with `gap-3 items-start` and `py-3`
- **Checkbox labels**: "I have read and agree to the Terms & Conditions" / "I have read and agree to the Privacy Policy" — 16px / weight 400
- **CTA disabled state**: Yellow button with `opacity-50` and `disabled:cursor-not-allowed` when either checkbox unchecked
- **CTA label**: "Continue" — becomes enabled only when both boxes are ticked
- **Linked terms**: "Terms & Conditions" and "Privacy Policy" within labels are `<a>` tags in `text-[#0091EA] underline`

### Screen 3: Daily Transfer Limit Acknowledgment
- **Route**: After T&C consent, before personal-info
- **Layout**: BackHeader (title: "Transfer Limits") + info card + "I Understand" CTA
- **Info card**: White card with `border border-[#E0E0E0] rounded-xl p-4`, shadow `var(--shadow-sm)`
- **Card content**: Two rows — "KYC Pending" tier and "KYC Approved" tier. Each row: tier label (12px / `#595959`) + limit amount (20px / weight 700 / `#212121`) + currency (14px / `#595959`)
- **Visual separator**: horizontal `<Separator />` component between tiers
- **CTA label**: "I Understand" — yellow pill, full width, h-12

### Screen 4: Selfie Capture Enhancement (existing KYC capture route)
- **Route**: `/kyc/capture` — `capture-selfie` and `review-selfie` steps (already scaffolded)
- **Enhancement**: Add circular face guide overlay to existing `CameraOverlay` when `variant="selfie"`
- **Face guide**: `240px × 240px` circle, centered horizontally in viewport, positioned at `top: 30%`
  - Border: `2px dashed rgba(255,255,255,0.85)`
  - Background: `transparent` (cutout via CSS clip or SVG mask)
  - Outer dimming: semi-transparent dark overlay `rgba(0,0,0,0.55)` around the circle
- **Instruction text**: Below the circle guide, white text `"Position your face within the circle"` — 14px / weight 400 / centered
- **Helper text**: Below instruction, white text at 12px / weight 400 / `rgba(255,255,255,0.75)` — e.g. "Look directly at the camera"
- **Step indicator**: Existing `StepIndicator` with `variant="dark"` overlay at top (unchanged)
- **Review step**: Same confirm/retake pattern already used for document review — no change needed

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — pre-reg info | "Continue" |
| Primary CTA — T&C | "Continue" (disabled until both checked) |
| Primary CTA — daily limit | "I Understand" |
| Pre-reg info heading | "Before You Register" |
| Pre-reg info subtext | "Please have the following documents ready. You will need them to complete your registration." |
| Pre-reg checklist item 1 | "Thai National ID Card or Passport" |
| Pre-reg checklist item 2 | "Work Permit (if applicable)" |
| T&C screen title | "Terms & Conditions" |
| T&C checkbox 1 label | "I have read and agree to the Terms & Conditions" |
| T&C checkbox 2 label | "I have read and agree to the Privacy Policy" |
| Daily limit screen title | "Transfer Limits" |
| Daily limit card heading | "Daily Transfer Limits" |
| Daily limit tier 1 label | "KYC Pending" |
| Daily limit tier 1 amount | "฿50,000 / day" |
| Daily limit tier 2 label | "KYC Approved" |
| Daily limit tier 2 amount | "฿500,000 / day" |
| Daily limit disclaimer | "Limits apply to all outgoing transfers combined across all channels." |
| Selfie capture instruction | "Position your face within the circle" |
| Selfie capture helper | "Look directly at the camera and ensure good lighting" |
| Selfie review title | "Check Your Selfie" |
| Selfie review hint | "Make sure your face is clearly visible with no obstructions" |
| Error — both checkboxes required | "Please accept both the Terms & Conditions and Privacy Policy to continue" (inline `role="alert"` below checkboxes) |
| Error — camera unavailable | "Camera access is required for identity verification. Please allow camera access in your browser settings." |
| Destructive actions | None in this phase |

All copy strings must be placed in the `auth` i18n namespace (`src/messages/en.json`, `th.json`, `my.json`) under a `compliance` sub-key.

Source: CONTEXT.md decisions, REQUIREMENTS.md COMP-01 through COMP-04, Pencil design reference.

---

## Interaction Contracts

### T&C Consent — Checkbox Gate
- CTA button starts `disabled` on mount
- Enabled when: `tcCheckbox === true AND privacyCheckbox === true`
- On submit: record `tcAcceptedAt = new Date().toISOString()` and `tcVersion = "1.0"` into registration store + Supabase `user_profiles`
- No confirmation dialog required — checkbox act is the confirmation

### Pre-Registration Info — Read-only
- No form validation
- Back navigates to OTP page
- Single tap on "Continue" advances to T&C screen

### Daily Limit — Acknowledgment
- No form validation
- Back navigates to T&C screen (user can return and re-read)
- Single tap on "I Understand" advances to personal-info

### Selfie Capture — Face Guide
- Face guide overlay renders only during `capture-selfie` step (not `review-selfie`)
- Camera stream stays active in the same route (no re-mount) — single-page KYC requirement from CONTEXT.md (iOS camera re-prompt prevention)
- Capture button: existing shutter button at bottom — no change to tap target or position
- Review (confirm/retake) pattern: unchanged from document review

### StepIndicator Updates
- Pre-registration info: no step indicator (it precedes OTP, outside the registration step sequence)
- T&C screen: show `StepIndicator` at step position before step 1 — use existing `StepIndicator` component with appropriate `currentStep` prop (implementation detail for planner)
- Daily limit screen: same step indicator position as T&C
- Selfie capture: existing `StepIndicator variant="dark"` at step 4 of 5 — unchanged

---

## Animation & Motion

Use existing animation classes from `globals.css`:

| Screen | Animation |
|--------|-----------|
| Pre-registration info mount | `animate-fade-up stagger-1` on heading, `stagger-2` on document list |
| T&C screen mount | `animate-fade-in` on scroll area, `animate-fade-up stagger-3` on checkboxes |
| Daily limit screen mount | `animate-fade-up stagger-1` on card |
| Face guide appearance | `animate-fade-in` (300ms) on overlay when camera stream starts |

Respect `prefers-reduced-motion` — existing global rule in `globals.css` disables all animations.

---

## Accessibility

- All checkboxes: `aria-required="true"`, `aria-checked={boolean}`
- CTA when disabled: `aria-disabled="true"` + `disabled` attribute
- Error messages: `role="alert"` + `aria-live="assertive"` for inline validation
- Face guide circle: `aria-hidden="true"` (decorative, camera stream has its own label)
- Scrollable T&C area: `role="region"` + `aria-label="Terms and Conditions text"`
- Minimum contrast: dark text `#212121` on white (`#FFFFFF`) — 16.1:1, exceeds WCAG AA
- Minimum contrast: `#595959` on white — 5.3:1, meets WCAG AA (confirmed in globals.css comment)
- Touch targets: 44×44px minimum for all interactive elements

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Checkbox, Button, Card, Separator, AlertDialog, ScrollArea, Label | not required |

No third-party registries declared for this phase.

Source: `components.json` registries field is empty `{}`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
