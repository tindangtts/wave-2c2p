---
phase: 09-compliance-registration
plan: "03"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/app/(auth)/register/daily-limit/page.tsx
  - src/components/features/camera-overlay.tsx
autonomous: true
requirements:
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Daily limit acknowledgment page renders a card with two tier rows (KYC Pending ฿50,000 and KYC Approved ฿500,000) separated by a Separator component, plus an I Understand CTA navigating to /register/personal-info"
    - "CameraOverlay with variant='selfie' renders a 240×240px circular face guide with 2px dashed white border, outer dark overlay rgba(0,0,0,0.55), and instruction text below the circle"
    - "The circular face guide does NOT render when variant='document'"
  artifacts:
    - path: "src/app/(auth)/register/daily-limit/page.tsx"
      provides: "Daily limit acknowledgment screen"
      min_lines: 60
    - path: "src/components/features/camera-overlay.tsx"
      provides: "Enhanced CameraOverlay with selfie face guide"
      contains: "240px"
  key_links:
    - from: "src/app/(auth)/register/daily-limit/page.tsx"
      to: "/register/personal-info"
      via: "router.push on I Understand"
      pattern: "personal-info"
    - from: "src/components/features/camera-overlay.tsx"
      to: "selfie face guide circle"
      via: "variant === 'selfie' conditional"
      pattern: "240px"
---

<objective>
Build the daily transfer limit acknowledgment screen (COMP-03) and enhance the existing CameraOverlay with a circular face guide overlay for selfie capture (COMP-04).

Purpose: Completes the compliance flow insertion before personal-info, and fixes the missing face guide on the selfie capture step.
Output: New daily-limit page, updated camera-overlay component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compliance-registration/09-CONTEXT.md
@.planning/phases/09-compliance-registration/09-UI-SPEC.md
@.planning/phases/09-compliance-registration/09-01-SUMMARY.md

<interfaces>
<!-- i18n keys (added in Plan 01, auth namespace) -->
// t('compliance.dailyLimit.title')       → "Transfer Limits"
// t('compliance.dailyLimit.cardHeading') → "Daily Transfer Limits"
// t('compliance.dailyLimit.tier1Label')  → "KYC Pending"
// t('compliance.dailyLimit.tier1Amount') → "฿50,000"
// t('compliance.dailyLimit.tier1Currency') → "THB / day"
// t('compliance.dailyLimit.tier2Label')  → "KYC Approved"
// t('compliance.dailyLimit.tier2Amount') → "฿500,000"
// t('compliance.dailyLimit.tier2Currency') → "THB / day"
// t('compliance.dailyLimit.disclaimer')  → "Limits apply to all outgoing transfers..."
// t('compliance.dailyLimit.cta')         → "I Understand"
// t('compliance.selfie.instruction')     → "Position your face within the circle"
// t('compliance.selfie.helper')          → "Look directly at the camera and ensure good lighting"

<!-- CameraOverlay current selfie section (camera-overlay.tsx lines 79-99) -->
// variant === 'selfie' renders:
//   - p.text-base.text-white.text-center.mb-6 (instruction)
//   - div.relative.w-60.h-60 with inner div.absolute.inset-0.rounded-full.border-[3px].border-[#FFE600]
//   - div.mt-6 with h2 "Take a Selfie" (hardcoded) + p.text-sm.text-white/70 (helper)
// REPLACE the selfie section — do not change the document section

<!-- KYC capture page uses CameraOverlay like this (kyc/capture/page.tsx lines 126-137) -->
// <CameraOverlay
//   variant={isSelfie ? 'selfie' : 'document'}
//   instruction={instruction}   ← already passes t('face.instruction') for selfie
//   helper={helper}             ← already passes t('face.helper') for selfie
//   ...
// />
// Note: kyc store t() calls use 'kyc' namespace, not 'auth'
// The capture page already passes the correct instruction/helper strings — CameraOverlay just needs to render them correctly with the new face guide UI

<!-- shadcn components to use -->
// Separator: from '@/components/ui/separator'
// Card: can use plain div with Tailwind — no shadcn Card needed (UI-SPEC uses border/rounded-xl)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create daily transfer limit acknowledgment page</name>
  <read_first>
    - src/app/(auth)/otp/page.tsx (Button + BackHeader layout reference)
    - .planning/phases/09-compliance-registration/09-UI-SPEC.md (Screen 3 spec, lines 124-130)
  </read_first>
  <files>src/app/(auth)/register/daily-limit/page.tsx</files>
  <action>
Create a Client Component page. Read-only acknowledgment screen — no form, just a card with two tier rows and a CTA.

```typescript
'use client'

import { useRouter } from 'next/navigation'
import { useTranslations } from 'next-intl'
import { Button } from '@/components/ui/button'
import { Separator } from '@/components/ui/separator'
import { BackHeader } from '@/components/layout/back-header'

export default function DailyLimitPage() {
  const router = useRouter()
  const t = useTranslations('auth')

  return (
    <div className="flex flex-col min-h-screen">
      <BackHeader
        title={t('compliance.dailyLimit.title')}
        onBack={() => router.back()}
      />

      <div className="flex-1 flex flex-col px-4 pt-8 pb-[64px]">
        {/* Limit info card */}
        <div
          className="border border-[#E0E0E0] rounded-xl p-4 bg-white shadow-sm animate-fade-up stagger-1"
          role="region"
          aria-label="Daily transfer limits"
        >
          <h2 className="text-base font-semibold text-[#212121] mb-4">
            {t('compliance.dailyLimit.cardHeading')}
          </h2>

          {/* KYC Pending tier */}
          <div className="flex items-center justify-between py-2">
            <span className="text-[12px] text-[#595959]">
              {t('compliance.dailyLimit.tier1Label')}
            </span>
            <div className="flex items-baseline gap-1">
              <span className="text-[20px] font-bold text-[#212121]">
                {t('compliance.dailyLimit.tier1Amount')}
              </span>
              <span className="text-[12px] text-[#595959]">
                {t('compliance.dailyLimit.tier1Currency')}
              </span>
            </div>
          </div>

          <Separator className="my-2" />

          {/* KYC Approved tier */}
          <div className="flex items-center justify-between py-2">
            <span className="text-[12px] text-[#595959]">
              {t('compliance.dailyLimit.tier2Label')}
            </span>
            <div className="flex items-baseline gap-1">
              <span className="text-[20px] font-bold text-[#212121]">
                {t('compliance.dailyLimit.tier2Amount')}
              </span>
              <span className="text-[12px] text-[#595959]">
                {t('compliance.dailyLimit.tier2Currency')}
              </span>
            </div>
          </div>
        </div>

        {/* Disclaimer */}
        <p className="text-[12px] text-[#595959] text-center mt-4 px-2">
          {t('compliance.dailyLimit.disclaimer')}
        </p>

        <div className="flex-1" />
      </div>

      {/* Fixed bottom CTA */}
      <div className="px-4 pb-8">
        <Button
          onClick={() => router.push('/register/personal-info')}
          className="w-full h-12 rounded-full bg-[#FFE600] text-[#212121] font-semibold text-base hover:bg-[#FFD600]"
        >
          {t('compliance.dailyLimit.cta')}
        </Button>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -n "compliance.dailyLimit\|Separator\|register/personal-info\|tier1Amount\|tier2Amount" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/app/\(auth\)/register/daily-limit/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/(auth)/register/daily-limit/page.tsx
    - grep finds "compliance.dailyLimit.tier1Amount" and "compliance.dailyLimit.tier2Amount"
    - grep finds "Separator" (component used between tiers)
    - grep finds "register/personal-info" (CTA navigation target)
    - grep finds "text-[20px] font-bold" for tier amounts
    - grep finds "text-[12px]" for tier labels and currency
    - File starts with 'use client'
  </acceptance_criteria>
  <done>Daily limit page renders card with two tier rows (KYC Pending ฿50,000 / KYC Approved ฿500,000) separated by Separator, disclaimer text, and I Understand CTA navigating to /register/personal-info</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enhance CameraOverlay with circular face guide for selfie variant</name>
  <read_first>
    - src/components/features/camera-overlay.tsx (read FULL file before editing — understand existing document and selfie sections)
    - .planning/phases/09-compliance-registration/09-UI-SPEC.md (Screen 4 spec, lines 132-143, Interaction Contracts lines 201-209)
  </read_first>
  <files>src/components/features/camera-overlay.tsx</files>
  <action>
Replace the selfie variant center content section (currently lines ~79-107) with the proper face guide implementation from UI-SPEC. Do NOT change anything in the document variant, bottom controls, flash overlay, or top header.

Current selfie section to REPLACE (the div.flex-1.flex.flex-col.items-center.justify-center content for selfie):

Old selfie rendering:
```jsx
{variant === 'selfie' && (
  <p className="text-base text-white text-center mb-6">{instruction}</p>
)}
{variant === 'document' ? (
  /* document guide frame — keep unchanged */
) : (
  /* Selfie circle guide — yellow ring per Pencil design */
  <div className="relative w-60 h-60">
    <div className="absolute inset-0 rounded-full border-[3px] border-[#FFE600]" />
  </div>
)}
{variant === 'selfie' && (
  <div className="mt-6 text-center">
    <h2 className="text-lg font-bold text-white mb-2">Take a Selfie</h2>
    <p className="text-sm text-white/70">{helper}</p>
  </div>
)}
```

Replace the entire center content div (the `div.flex-1.flex.flex-col.items-center.justify-center.px-4`) with:

```jsx
{/* Center content */}
<div className="flex-1 flex flex-col items-center justify-center px-4">
  {variant === 'document' ? (
    /* Document guide frame — 85.6:54 ratio — UNCHANGED */
    <div className="relative w-full max-w-[calc(100vw-64px)]" style={{ aspectRatio: '85.6 / 54' }}>
      <div className="absolute inset-0 border-2 border-white/30 rounded-lg" />
      {/* Corner markers */}
      <div className="absolute top-0 left-0 w-6 h-6 border-t-[3px] border-l-[3px] border-white rounded-tl" />
      <div className="absolute top-0 right-0 w-6 h-6 border-t-[3px] border-r-[3px] border-white rounded-tr" />
      <div className="absolute bottom-0 left-0 w-6 h-6 border-b-[3px] border-l-[3px] border-white rounded-bl" />
      <div className="absolute bottom-0 right-0 w-6 h-6 border-b-[3px] border-r-[3px] border-white rounded-br" />
    </div>
  ) : (
    /* Selfie face guide — circular cutout with dashed border */
    <div className="flex flex-col items-center">
      {/* Outer dimming overlay + circular cutout */}
      <div
        className="relative animate-fade-in"
        style={{ width: '240px', height: '240px' }}
        aria-hidden="true"
      >
        {/* Dark semi-transparent background around the circle */}
        <div
          className="absolute inset-0 rounded-full"
          style={{ boxShadow: '0 0 0 9999px rgba(0,0,0,0.55)' }}
        />
        {/* Dashed white circle border */}
        <div
          className="absolute inset-0 rounded-full"
          style={{ border: '2px dashed rgba(255,255,255,0.85)' }}
        />
      </div>

      {/* Instruction text — below circle, xl gap (32px) */}
      <p className="text-base text-white text-center mt-8 px-4">
        {instruction}
      </p>

      {/* Helper text */}
      <p
        className="text-[12px] text-center mt-2 px-4"
        style={{ color: 'rgba(255,255,255,0.75)' }}
      >
        {helper}
      </p>
    </div>
  )}
</div>
```

Also remove the standalone `{variant === 'document' && ...}` instruction banner ONLY for selfie — the yellow banner before center content already guards with `variant === 'document'`, so no change needed there.

Key constraints:
- `aria-hidden="true"` on the face guide div (decorative per UI-SPEC accessibility section)
- `animate-fade-in` on the face guide container (UI-SPEC animation table)
- `boxShadow: '0 0 0 9999px rgba(0,0,0,0.55)'` creates the outer dimming effect around the circle
- `border: '2px dashed rgba(255,255,255,0.85)'` is the dashed border per UI-SPEC
- `mt-8` (32px) between circle and instruction text (xl spacing token per UI-SPEC)
- instruction and helper are passed as props from capture page — do NOT hardcode strings
  </action>
  <verify>
    <automated>grep -n "240px\|dashed\|rgba(0,0,0,0.55)\|rgba(255,255,255,0.85)\|aria-hidden\|animate-fade-in" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/components/features/camera-overlay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "240px" in camera-overlay.tsx (face guide dimensions)
    - grep finds "dashed" in camera-overlay.tsx (dashed border spec)
    - grep finds "rgba(0,0,0,0.55)" in camera-overlay.tsx (outer dim overlay)
    - grep finds "aria-hidden" on the face guide element
    - grep finds "animate-fade-in" on the face guide container
    - grep does NOT find "Take a Selfie" (old hardcoded heading removed)
    - grep does NOT find "border-[#FFE600]" in the selfie section (old yellow ring removed)
    - document variant still has "border-t-[3px]" corner markers (unchanged)
  </acceptance_criteria>
  <done>CameraOverlay selfie variant renders 240×240px circle with dashed white border, outer rgba(0,0,0,0.55) dim via box-shadow, animate-fade-in entrance, instruction and helper text below; document variant unchanged</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `ls src/app/(auth)/register/daily-limit/page.tsx` — exists
2. `grep "Separator\|tier1Amount\|personal-info" src/app/(auth)/register/daily-limit/page.tsx` — all 3 found
3. `grep "240px\|dashed\|rgba(0,0,0,0.55)" src/components/features/camera-overlay.tsx` — all found
4. `grep "Take a Selfie\|border-\[#FFE600\]" src/components/features/camera-overlay.tsx` — should NOT be found (old selfie removed)
5. `npm run build` — no TypeScript errors
6. Manual: Navigate to /register/daily-limit (after /register/terms), verify tier card renders
7. Manual: Start KYC capture to capture-selfie step, verify circular face guide with dim overlay and dashed border appears
</verification>

<success_criteria>
- Daily limit page: card with two tier rows + Separator + disclaimer + I Understand CTA → /register/personal-info
- CameraOverlay selfie variant: 240px circle, 2px dashed rgba(255,255,255,0.85) border, rgba(0,0,0,0.55) outer dim, animate-fade-in, instruction + helper text below
- CameraOverlay document variant: unchanged (corner markers still present)
- npm run build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-compliance-registration/09-03-SUMMARY.md`
</output>
