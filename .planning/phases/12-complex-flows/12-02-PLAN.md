---
phase: 12-complex-flows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/mock-kyc/work-permit-update/route.ts
  - messages/en/kyc.json
  - messages/th/kyc.json
  - messages/mm/kyc.json
autonomous: true
requirements:
  - COMP-05

must_haves:
  truths:
    - "POST /api/mock-kyc/work-permit-update returns success:true and sets kyc_status='pending_update' when MOCK_KYC_AUTO_APPROVE is not 'false'"
    - "POST /api/mock-kyc/work-permit-update returns success:false with rejection reason when MOCK_KYC_AUTO_APPROVE='false'"
    - "kyc.json in all 3 locales contains workPermitUpdate.* keys for the standalone update flow"
  artifacts:
    - path: "src/app/api/mock-kyc/work-permit-update/route.ts"
      provides: "Mock work permit update KYC endpoint"
      exports: ["POST"]
    - path: "messages/en/kyc.json"
      provides: "Work permit update i18n strings"
      contains: "workPermitUpdate"
  key_links:
    - from: "src/app/api/mock-kyc/work-permit-update/route.ts"
      to: "user_profiles.kyc_status"
      via: "admin.from('user_profiles').update({ kyc_status: 'pending_update' })"
      pattern: "pending_update"
---

<objective>
Create the mock work permit update KYC API route and add i18n strings for the standalone document update flow.

Purpose: The UI plan (12-04) needs the API before implementation. The `pending_update` KYC status preserves existing transfer access while re-verification is in progress (per COMP-05).
Output: POST /api/mock-kyc/work-permit-update route + kyc.json additions in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-complex-flows/12-CONTEXT.md

<interfaces>
<!-- Existing KYC submit pattern from src/app/api/mock-kyc/submit/route.ts -->
```typescript
// Auth check
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

// Env flags (read at request time per D-04)
const autoApprove = process.env.MOCK_KYC_AUTO_APPROVE !== 'false'  // default: true
const delayMs = parseInt(process.env.MOCK_KYC_DELAY_MS ?? '1500', 10)

// Admin client for DB writes
import { createAdminClient } from '@/lib/auth/admin'
const admin = createAdminClient()

// Status values in user_profiles.kyc_status
// Existing: 'not_started' | 'pending' | 'approved' | 'rejected' | 'expired'
// NEW for this flow: 'pending_update' (preserves transfer access)
```

<!-- Request body -->
```typescript
interface WorkPermitUpdateRequest {
  document_type: 'work_permit'   // always work_permit for this flow
  // front_image_url and back_image_url passed as data URLs (mock://front, mock://back)
}

interface WorkPermitUpdateResponse {
  success: boolean
  status: 'pending_update' | 'rejected'
  verification_id: string
  rejection_reasons?: string[]
}
```

<!-- KYCStatusValue type location -->
// src/lib/kyc/schemas.ts — includes existing statuses; 'pending_update' is a new value
// The type needs to be extended (or use a string literal for the API route to avoid breaking imports)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POST /api/mock-kyc/work-permit-update route</name>
  <files>src/app/api/mock-kyc/work-permit-update/route.ts</files>
  <read_first>
    - src/app/api/mock-kyc/submit/route.ts (full file — auth + admin + env pattern)
    - src/lib/kyc/schemas.ts (KYCStatusValue type to check if pending_update needs adding)
    - src/lib/auth/admin.ts (createAdminClient import path)
  </read_first>
  <action>
    Create `src/app/api/mock-kyc/work-permit-update/route.ts`:

    1. Auth guard: `createClient()` → `supabase.auth.getUser()` → 401 if no user
    2. Read env flags at request time:
       - `const autoApprove = process.env.MOCK_KYC_AUTO_APPROVE !== 'false'`
       - `const delayMs = parseInt(process.env.MOCK_KYC_DELAY_MS ?? '1500', 10)`
    3. Simulate delay: `await new Promise(resolve => setTimeout(resolve, delayMs))`
    4. Generate `verificationId = 'WP-UPDATE-' + Date.now()`
    5. Use `createAdminClient()` for DB writes
    6. If autoApprove:
       - Insert into `kyc_documents`: `{ user_id, document_type: 'work_permit', front_image_url: 'mock://front', back_image_url: 'mock://back', status: 'pending', verified_at: null }` (status is 'pending' — will be approved asynchronously in a real system; the wallet status is what matters)
       - Update `user_profiles` set `kyc_status = 'pending_update'` where `id = user.id`
       - Return `{ success: true, status: 'pending_update', verification_id: verificationId }`
    7. If NOT autoApprove (rejection path):
       - Pick random reason from `['Document is blurry or unreadable.', 'Work permit has expired.', 'Name on document does not match registration.']`
       - Insert into `kyc_documents`: `{ user_id, document_type: 'work_permit', front_image_url: 'mock://front', back_image_url: 'mock://back', status: 'rejected', rejection_reason: reason }`
       - Do NOT change `kyc_status` in user_profiles (user retains current access)
       - Return `{ success: false, status: 'rejected', verification_id: verificationId, rejection_reasons: [reason] }`
    8. Wrap in try/catch → 500 `{ error: 'Verification could not be completed' }`

    IMPORTANT: `pending_update` is distinct from `pending` — it means re-verification in progress but existing access is preserved. Do not change user_profiles.kyc_status on rejection.

    Also check `src/lib/kyc/schemas.ts` — if `KYCStatusValue` is a zod enum or TypeScript union, add `'pending_update'` to it so the system type is consistent. Only modify that file if needed to avoid TypeScript errors elsewhere.
  </action>
  <verify>
    <automated>test -f src/app/api/mock-kyc/work-permit-update/route.ts && grep -q "pending_update" src/app/api/mock-kyc/work-permit-update/route.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    File exists, exports POST, sets kyc_status='pending_update' on success path, does NOT change kyc_status on rejection, returns appropriate JSON for both paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add work permit update i18n strings to all 3 locales</name>
  <files>messages/en/kyc.json, messages/th/kyc.json, messages/mm/kyc.json</files>
  <read_first>
    - messages/en/kyc.json (existing structure — add at top-level alongside existing keys)
    - messages/th/kyc.json
    - messages/mm/kyc.json
  </read_first>
  <action>
    Add a `workPermitUpdate` top-level key to each locale's kyc.json (alongside existing keys like "title", "capture", etc.).

    For `messages/en/kyc.json`:
    ```json
    "workPermitUpdate": {
      "title": "Update Work Permit",
      "subtitle": "Capture the front and back of your new work permit",
      "frontInstruction": "Place work permit (front) into the box",
      "backInstruction": "Place work permit (back) into the box",
      "submitCta": "Submit for Verification",
      "submittingLabel": "Submitting...",
      "successTitle": "Documents Submitted",
      "successBody": "Your work permit is under review. You can continue using transfers while we verify.",
      "successCta": "Back to Profile",
      "failTitle": "Submission Failed",
      "failBody": "Could not submit your documents. Please try again.",
      "failCta": "Try Again",
      "cancelDialog": {
        "title": "Cancel update?",
        "description": "Your captured photos will be lost.",
        "cancel": "Continue",
        "confirm": "Cancel Update"
      }
    }
    ```

    For `messages/th/kyc.json`:
    ```json
    "workPermitUpdate": {
      "title": "อัปเดตใบอนุญาตทำงาน",
      "subtitle": "ถ่ายภาพด้านหน้าและด้านหลังของใบอนุญาตใหม่",
      "frontInstruction": "วางใบอนุญาตทำงาน (ด้านหน้า) ในกรอบ",
      "backInstruction": "วางใบอนุญาตทำงาน (ด้านหลัง) ในกรอบ",
      "submitCta": "ส่งเพื่อตรวจสอบ",
      "submittingLabel": "กำลังส่ง...",
      "successTitle": "ส่งเอกสารแล้ว",
      "successBody": "ใบอนุญาตทำงานของคุณอยู่ระหว่างการตรวจสอบ คุณยังสามารถโอนเงินได้",
      "successCta": "กลับไปที่โปรไฟล์",
      "failTitle": "การส่งล้มเหลว",
      "failBody": "ไม่สามารถส่งเอกสารได้ กรุณาลองอีกครั้ง",
      "failCta": "ลองอีกครั้ง",
      "cancelDialog": {
        "title": "ยกเลิกการอัปเดต?",
        "description": "รูปถ่ายที่ถ่ายไว้จะหาย",
        "cancel": "ดำเนินการต่อ",
        "confirm": "ยกเลิกการอัปเดต"
      }
    }
    ```

    For `messages/mm/kyc.json`:
    ```json
    "workPermitUpdate": {
      "title": "အလုပ်ခွင့်ပြုချက် အပ်ဒိတ်လုပ်ရန်",
      "subtitle": "အလုပ်ခွင့်ပြုချက် အသစ်၏ ရှေ့နှင့်နောက်ကို ရိုက်ကူးပါ",
      "frontInstruction": "အလုပ်ခွင့်ပြုချက် (ရှေ့) ကို ဘောင်ထဲ ထည့်ပါ",
      "backInstruction": "အလုပ်ခွင့်ပြုချက် (နောက်) ကို ဘောင်ထဲ ထည့်ပါ",
      "submitCta": "အတည်ပြုချက်အတွက် တင်ပါ",
      "submittingLabel": "တင်နေသည်...",
      "successTitle": "စာရွက်စာတမ်းများ တင်ပြပြီးပါပြီ",
      "successBody": "သင်၏ အလုပ်ခွင့်ပြုချက်ကို စစ်ဆေးနေပါသည်။ စစ်ဆေးနေစဉ် ငွေလွှဲမှုများ ဆက်လုပ်နိုင်သည်",
      "successCta": "ပရိုဖိုင်သို့ ပြန်သွားပါ",
      "failTitle": "တင်ပြမှု မအောင်မြင်ပါ",
      "failBody": "စာရွက်စာတမ်းများ မတင်နိုင်ပါ။ ထပ်ကြိုးစားပါ",
      "failCta": "ထပ်ကြိုးစားပါ",
      "cancelDialog": {
        "title": "အပ်ဒိတ် ပယ်ဖျက်မည်လား?",
        "description": "ရိုက်ကူးထားသော ဓာတ်ပုံများ ပျောက်သွားမည်",
        "cancel": "ဆက်လုပ်ပါ",
        "confirm": "အပ်ဒိတ် ပယ်ဖျက်ပါ"
      }
    }
    ```

    IMPORTANT: Maintain valid JSON. Add a comma after the last existing top-level key before inserting `"workPermitUpdate"`.
  </action>
  <verify>
    <automated>node -e "['en','th','mm'].forEach(l=>{const f=require('./messages/'+l+'/kyc.json');console.assert(f.workPermitUpdate&&f.workPermitUpdate.title,'FAIL '+l)}); console.log('PASS')"</automated>
  </verify>
  <done>
    All three locale kyc.json files contain workPermitUpdate.* keys. Valid JSON. Keys cover: title, subtitle, front/back instructions, submit CTA, success and fail modal strings, cancel dialog strings.
  </done>
</task>

</tasks>

<verification>
1. `src/app/api/mock-kyc/work-permit-update/route.ts` exists and exports POST
2. `grep -q "pending_update" src/app/api/mock-kyc/work-permit-update/route.ts` passes
3. `grep -r "workPermitUpdate" messages/` returns hits in en, th, mm kyc.json files
4. All 3 kyc.json files parse as valid JSON
5. `npm run build` shows no TypeScript errors (especially in schemas.ts if KYCStatusValue was updated)
</verification>

<success_criteria>
- POST /api/mock-kyc/work-permit-update endpoint exists, sets kyc_status='pending_update' on success, does not change status on rejection
- All i18n keys for work permit update flow present in en/th/mm locales
- No JSON or TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-complex-flows/12-02-SUMMARY.md`
</output>
