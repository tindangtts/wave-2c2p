---
phase: 15-qr-scanner-webauthn-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/scan/page.tsx
  - src/components/features/live-scanner.tsx
  - package.json
autonomous: true
requirements: [QR-01]

must_haves:
  truths:
    - "User can open the scan page and see a live camera feed scanning for QR codes"
    - "Scanner uses @yudiel/react-qr-scanner, not manual getUserMedia"
    - "Scanner frame overlay with corner markers appears over the camera feed"
  artifacts:
    - path: "src/components/features/live-scanner.tsx"
      provides: "Live QR scanner wrapper using @yudiel/react-qr-scanner"
      contains: "Scanner"
    - path: "src/app/(main)/scan/page.tsx"
      provides: "Scan page using LiveScanner instead of manual camera"
      contains: "LiveScanner"
  key_links:
    - from: "src/app/(main)/scan/page.tsx"
      to: "src/components/features/live-scanner.tsx"
      via: "import LiveScanner"
      pattern: "import.*LiveScanner.*live-scanner"
    - from: "src/components/features/live-scanner.tsx"
      to: "@yudiel/react-qr-scanner"
      via: "Scanner component import"
      pattern: "import.*Scanner.*@yudiel/react-qr-scanner"
---

<objective>
Install @yudiel/react-qr-scanner and replace the mock scan page with a live camera scanner component.

Purpose: The current scan page opens a raw camera feed via getUserMedia but never actually decodes QR codes. This plan replaces the manual camera setup with @yudiel/react-qr-scanner which handles camera acquisition, frame capture, and barcode detection automatically.

Output: A working LiveScanner component and updated scan page that detects QR codes in real-time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qr-scanner-webauthn-migration/15-RESEARCH.md

@src/app/(main)/scan/page.tsx
@src/components/features/scanner-frame.tsx
@src/stores/p2p-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @yudiel/react-qr-scanner and create LiveScanner component</name>
  <files>package.json, src/components/features/live-scanner.tsx</files>
  <read_first>
    - src/components/features/scanner-frame.tsx (existing overlay component to reuse)
    - src/stores/p2p-store.ts (P2P store for setReceiverWalletId)
    - .planning/phases/15-qr-scanner-webauthn-migration/15-RESEARCH.md (Scanner API reference)
  </read_first>
  <action>
1. Run `npm install @yudiel/react-qr-scanner` to install the library.

2. Create `src/components/features/live-scanner.tsx` as a `'use client'` component:

- Import `Scanner` and `IDetectedBarcode` from `@yudiel/react-qr-scanner`
- Import `ScannerFrame` from `@/components/features/scanner-frame`
- Props interface: `{ onScan: (value: string) => void; onError?: (error: unknown) => void; paused?: boolean }`
- Render `<Scanner>` with these props:
  - `constraints={{ facingMode: 'environment' }}`
  - `formats={['qr_code']}`
  - `scanDelay={300}`
  - `components={{ finder: false }}` (we use our own ScannerFrame)
  - `paused={props.paused ?? false}`
  - `styles={{ container: { position: 'absolute', inset: 0, width: '100%', height: '100%' }, video: { width: '100%', height: '100%', objectFit: 'cover' } }}`
  - `onScan`: extract `codes[0]?.rawValue`, if truthy call `props.onScan(value)`
  - `onError`: call `props.onError?.(err)` or `console.error('[LiveScanner]', err)`
- Render `<ScannerFrame size={240} />` as sibling after Scanner (inside the same relative container)
- Wrap both in `<div className="relative flex-1 overflow-hidden">` (critical: parent must have height for Scanner to render)

IMPORTANT: Do NOT use `next/dynamic` — the scan page is already `'use client'` so static import is fine. Do NOT add a second BarcodeDetector polyfill — it is bundled with @yudiel/react-qr-scanner.
  </action>
  <verify>
    <automated>grep -q "Scanner" src/components/features/live-scanner.tsx && grep -q "@yudiel/react-qr-scanner" src/components/features/live-scanner.tsx && grep -q "@yudiel/react-qr-scanner" package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `@yudiel/react-qr-scanner` appears in package.json dependencies
    - `src/components/features/live-scanner.tsx` exists with `'use client'` directive
    - LiveScanner imports Scanner from `@yudiel/react-qr-scanner`
    - LiveScanner renders ScannerFrame overlay
    - LiveScanner accepts onScan callback prop
    - Components uses `finder: false` to disable built-in overlay
  </acceptance_criteria>
  <done>LiveScanner component created, library installed, component exports onScan callback</done>
</task>

<task type="auto">
  <name>Task 2: Replace mock camera in scan page with LiveScanner</name>
  <files>src/app/(main)/scan/page.tsx</files>
  <read_first>
    - src/app/(main)/scan/page.tsx (current mock implementation to replace)
    - src/components/features/live-scanner.tsx (just created in Task 1)
  </read_first>
  <action>
Rewrite `src/app/(main)/scan/page.tsx` to use `LiveScanner` instead of manual getUserMedia:

1. REMOVE all manual camera code:
   - Remove `videoRef`, `streamRef` refs
   - Remove the `stopCamera` callback
   - Remove the `useEffect` that calls `startCamera` / `getUserMedia`
   - Remove the `<video>` element
   - Remove `CameraState` type and `cameraState` state

2. ADD LiveScanner integration:
   - Import `LiveScanner` from `@/components/features/live-scanner`
   - Add state: `const [scanPaused, setScanPaused] = useState(false)` and `const handledRef = useRef(false)`
   - Add state: `const [cameraError, setCameraError] = useState(false)` for permission denied UI

3. KEEP the existing UI chrome:
   - Keep the yellow header bar with "Scan QR Code" title and X close button
   - Keep the bottom controls: gallery input, "Upload from Gallery" button, "Receive Money with QR" button
   - Keep `handleClose` (remove `stopCamera()` call — Scanner cleans up on unmount)
   - Keep `handleReceiveMoney` as-is
   - Keep `handleGallerySelect` as-is (still mock for now — Plan 15-02 adds real decode)
   - Keep `galleryInputRef` and hidden file input

4. CAMERA AREA replacement:
   - When `!cameraError`: render `<LiveScanner onScan={handleScanResult} onError={handleCameraError} paused={scanPaused} />`
   - When `cameraError`: render the existing denied/unavailable message ("Camera access denied. Use gallery to scan.")

5. `handleScanResult(value: string)`:
   - Guard: `if (handledRef.current) return`
   - Set `handledRef.current = true` and `setScanPaused(true)`
   - Call existing `handleQRResult(value)` logic (keep P2P_WALLET_REGEX check, setReceiverWalletId, router.push)

6. `handleCameraError(err: unknown)`:
   - Check if `err` is a DOMException with name `NotAllowedError` — set `setCameraError(true)`
   - Otherwise log: `console.error('[Scan] Camera error:', err)`

Keep the `P2P_WALLET_REGEX` and `handleQRResult` function — Plan 15-02 will enhance routing logic.
  </action>
  <verify>
    <automated>grep -q "LiveScanner" src/app/\(main\)/scan/page.tsx && ! grep -q "getUserMedia" src/app/\(main\)/scan/page.tsx && ! grep -q "srcObject" src/app/\(main\)/scan/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - scan/page.tsx imports and renders LiveScanner
    - No manual getUserMedia, no <video> element, no streamRef
    - P2P_WALLET_REGEX and handleQRResult logic preserved
    - Camera error state shows fallback message
    - handledRef prevents double-scan navigation
    - scanPaused set to true on first scan result
    - Gallery button and Receive Money button still present
    - Header with close button still present
  </acceptance_criteria>
  <done>Scan page uses LiveScanner for real QR detection; manual camera code fully removed; all existing UI chrome preserved</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors (Scanner used only in client component)
2. `grep -r "getUserMedia" src/app/(main)/scan/` returns no results
3. `grep "LiveScanner" src/app/(main)/scan/page.tsx` confirms integration
4. `grep "@yudiel/react-qr-scanner" package.json` confirms dependency
</verification>

<success_criteria>
- @yudiel/react-qr-scanner installed and used in LiveScanner component
- Scan page renders live camera feed via Scanner (not manual getUserMedia)
- ScannerFrame overlay renders over the camera feed
- First QR detection pauses scanner and routes appropriately
- Camera permission denial shows fallback UI
- Build succeeds without SSR errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-qr-scanner-webauthn-migration/15-01-SUMMARY.md`
</output>
