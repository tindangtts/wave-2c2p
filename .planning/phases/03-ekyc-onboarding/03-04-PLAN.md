---
phase: 03-ekyc-onboarding
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/app/(auth)/kyc/resubmit/page.tsx
  - src/components/features/field-status-row.tsx
  - src/components/features/kyc-expired-modal.tsx
autonomous: true
requirements: [EKYC-07, EKYC-08]
must_haves:
  truths:
    - "Rejected user sees which fields passed and which failed with clear visual distinction"
    - "User can retake only failed documents without re-capturing accepted ones"
    - "Resubmit button is disabled until all rejected fields have been re-captured"
    - "KYC expired modal shows consequences list and Update Now / Update Later buttons"
  artifacts:
    - path: "src/app/(auth)/kyc/resubmit/page.tsx"
      provides: "Re-submission page for rejected KYC"
      contains: "FieldStatusRow"
    - path: "src/components/features/field-status-row.tsx"
      provides: "Row showing accepted/rejected status with retake action"
      contains: "role.*alert"
    - path: "src/components/features/kyc-expired-modal.tsx"
      provides: "KYC expired alert dialog with consequences"
      contains: "AlertDialog"
  key_links:
    - from: "src/app/(auth)/kyc/resubmit/page.tsx"
      to: "src/stores/kyc-store.ts"
      via: "reads rejection state, writes recaptured images"
      pattern: "useKYCStore"
    - from: "src/app/(auth)/kyc/resubmit/page.tsx"
      to: "src/app/(auth)/kyc/capture/page.tsx"
      via: "navigates to capture for individual retakes"
      pattern: "router.push.*capture"
    - from: "src/components/features/kyc-expired-modal.tsx"
      to: "src/app/(auth)/kyc/document-type/page.tsx"
      via: "Update Now navigates to document type selection"
      pattern: "router.push.*document-type"
---

<objective>
Re-submission flow and expired modal: handling rejected KYC submissions and expired document notifications.

Purpose: Completes the error recovery paths — users who fail verification can correct specific issues, and users with expired KYC are informed of consequences and guided to update.
Output: Re-submission page with field-level status, FieldStatusRow component, KYCExpiredModal component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ekyc-onboarding/03-CONTEXT.md
@.planning/phases/03-ekyc-onboarding/03-UI-SPEC.md
@.planning/phases/03-ekyc-onboarding/03-RESEARCH.md

<interfaces>
<!-- From Plan 01-03 outputs -->
From src/stores/kyc-store.ts:
  useKYCStore: { documentType, frontImage, backImage, selfieImage, kycStatus, rejectionReasons, submissionId, setFrontImage, setBackImage, setSelfieImage, setCaptureStep, clearAll }

From src/components/features/camera-overlay.tsx:
  CameraOverlay({ variant, instruction, helper, onCapture, onBack })

From src/components/layout/back-header.tsx:
  BackHeader({ title, onBack })

From src/components/ui/alert-dialog.tsx:
  AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger

From src/components/ui/badge.tsx:
  Badge with variant="success" and other variants

From src/components/ui/button.tsx:
  Button with variant="outline" and size="sm"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FieldStatusRow component and re-submission page</name>
  <files>src/components/features/field-status-row.tsx, src/app/(auth)/kyc/resubmit/page.tsx</files>
  <read_first>
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 9: Re-submission flow layout, FieldStatusRow component contract, re-submission rules)
    - src/stores/kyc-store.ts (rejection state, image setters)
    - src/components/ui/badge.tsx (Badge API for "Accepted" badge)
    - src/components/ui/button.tsx (Button size="sm" variant="outline")
    - src/components/features/camera-overlay.tsx (CameraOverlay API for retake)
    - src/components/layout/back-header.tsx (BackHeader)
  </read_first>
  <action>
1. Create `src/components/features/field-status-row.tsx`:
   - 'use client' directive
   - Props: `{ label: string, status: 'accepted' | 'rejected', onRetake?: () => void }`
   - Container: `flex items-center gap-3 h-14 px-4 rounded-lg`
     - Accepted: `bg-[#FAFAFA]` no border
     - Rejected: `bg-white border border-[#F44336]`
   - Status icon: 20px
     - Accepted: CheckCircle `text-[#00C853]`
     - Rejected: XCircle `text-[#F44336]`
   - Label: `text-base flex-1`
     - Accepted: `text-[#757575]`
     - Rejected: `text-[#212121]`
   - Action (right side):
     - Accepted: Badge "Accepted" — use `className="bg-[#00C853]/10 text-[#00C853] border-0"` (custom success styling)
     - Rejected: Button "Retake" — `variant="outline" size="sm"`, onClick calls `onRetake`
   - `aria-label={label + ': ' + status}` on container
   - Imports: CheckCircle, XCircle from lucide-react, Badge, Button

2. Create `src/app/(auth)/kyc/resubmit/page.tsx`:
   - 'use client' directive
   - Import: BackHeader, FieldStatusRow, CameraOverlay, Button, useKYCStore, useRouter, useTranslations('kyc'), AlertTriangle from lucide-react
   - Hydration-safe mounted guard
   - Read from store: `documentType`, `frontImage`, `backImage`, `selfieImage`, `rejectionReasons`, `kycStatus`
   - State: `retakeMode` — null (list view) or `'front' | 'back' | 'selfie'` (camera view for individual retake)
   - State: `retakenFront`, `retakenBack`, `retakenSelfie` — boolean tracking which items have been re-captured
   - Determine field statuses — for mock, use a simple heuristic based on rejection reasons:
     - If rejection reasons mention "document" or "blurry" or "unreadable": front/back = rejected
     - If rejection reasons mention "face" or "photo": selfie = rejected
     - Default: mark front as rejected if no specific match (ensures at least one field needs retake)
     - Fields not rejected = accepted
   - When `retakeMode` is set: show CameraOverlay for that specific capture. On capture: save to store, set `retaken{Field}=true`, set `retakeMode=null` (back to list)
   - List view layout per UI-SPEC Screen 9:
     - BackHeader: title `t('resubmit.title')`, onBack → navigate to `/(auth)/kyc/status`
     - Section title: `t('resubmit.sectionTitle')` — `text-xl font-bold text-[#212121] mb-2`
     - Section subtitle: `t('resubmit.subtitle')` — `text-base text-[#757575] mb-6`
     - Field status list: `flex flex-col gap-3`
       - FieldStatusRow for "Document Front": status based on rejection, onRetake → `setRetakeMode('front')`
       - FieldStatusRow for "Document Back": same pattern
       - FieldStatusRow for "Selfie": same pattern
     - Rejection reasons summary: `mt-6`, each reason with AlertTriangle 16px `text-[#FF9800]` and `text-xs text-[#757575]`
     - Spacer: `flex-1`
     - CTA: Button `t('resubmit.cta')` — `w-full h-12 rounded-full`, variant default (yellow)
       - Disabled (opacity-50) until all rejected fields have been re-captured (`retaken{Field}` is true for each rejected field)
       - onClick: navigate to `/(auth)/kyc/processing` (re-triggers verification with updated images)
   - Container: `flex flex-col min-h-screen px-4 pt-6 pb-8`
  </action>
  <acceptance_criteria>
    - src/components/features/field-status-row.tsx contains `CheckCircle` and `XCircle` imports
    - src/components/features/field-status-row.tsx contains `aria-label` with status
    - src/components/features/field-status-row.tsx renders Badge for accepted and Button for rejected
    - src/app/(auth)/kyc/resubmit/page.tsx contains `FieldStatusRow`
    - src/app/(auth)/kyc/resubmit/page.tsx contains `retakeMode` state for individual field retakes
    - src/app/(auth)/kyc/resubmit/page.tsx contains disabled CTA logic (disabled until all rejected re-captured)
    - src/app/(auth)/kyc/resubmit/page.tsx contains `CameraOverlay` for retake camera
  </acceptance_criteria>
  <verify>
    <automated>grep "aria-label" src/components/features/field-status-row.tsx && grep "FieldStatusRow" src/app/'(auth)'/kyc/resubmit/page.tsx && grep "retakeMode" src/app/'(auth)'/kyc/resubmit/page.tsx && grep "CameraOverlay" src/app/'(auth)'/kyc/resubmit/page.tsx</automated>
  </verify>
  <done>Re-submission page shows accepted fields (dimmed, read-only) and rejected fields (highlighted, with Retake button). Tapping Retake opens camera for that specific field. CTA disabled until all rejected fields re-captured. Submitting navigates to processing for re-verification.</done>
</task>

<task type="auto">
  <name>Task 2: Create KYC expired modal component</name>
  <files>src/components/features/kyc-expired-modal.tsx</files>
  <read_first>
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 8: KYC Expired Modal layout and copy)
    - src/components/ui/alert-dialog.tsx (AlertDialog API)
    - src/app/(auth)/register/personal-info/page.tsx (AlertDialog usage pattern from Phase 2)
    - messages/en/kyc.json (expiredModal namespace)
  </read_first>
  <action>
1. Create `src/components/features/kyc-expired-modal.tsx`:
   - 'use client' directive
   - Props: `{ open: boolean, onUpdateNow: () => void, onUpdateLater: () => void }`
   - Uses shadcn AlertDialog in controlled mode: `<AlertDialog open={open}>`
   - AlertDialogContent: `max-w-[340px] p-6`
   - Layout per UI-SPEC Screen 8:
     - Clock icon: 40px, `text-[#9E9E9E]`, centered, `mb-4` — using flex centering
     - Title: `t('expiredModal.title')` — `text-xl font-bold text-[#212121] text-center mb-2`
     - Description: `t('expiredModal.description')` — `text-base text-[#757575] text-center mb-4`
     - Consequences section:
       - Intro: `t('expiredModal.consequences')` — `text-xs text-[#757575] mb-2`
       - List: `<ul className="text-xs text-[#757575] list-disc pl-5 mb-6 space-y-1">`
         - `<li>{t('expiredModal.transfers')}</li>` — "Send money transfers"
         - `<li>{t('expiredModal.deposits')}</li>` — "Add funds to your wallet"
         - `<li>{t('expiredModal.withdrawals')}</li>` — "Make withdrawals"
     - AlertDialogAction as "Update Now": Button variant default (yellow), `w-full h-12 rounded-full mb-2`, onClick → `onUpdateNow()`
       - Label: `t('expiredModal.updateNow')`
     - AlertDialogCancel as "Update Later": Button variant outline, `w-full h-12 rounded-full`, onClick → `onUpdateLater()`
       - Label: `t('expiredModal.updateLater')`
   - `role="alertdialog"` is automatic from AlertDialog component
   - Import: Clock from lucide-react, AlertDialog*, Button, useTranslations('kyc')
   - Auto-focus on "Update Now" button when modal opens (AlertDialog default behavior)
  </action>
  <acceptance_criteria>
    - src/components/features/kyc-expired-modal.tsx contains `AlertDialog`
    - src/components/features/kyc-expired-modal.tsx contains `Clock` icon import
    - src/components/features/kyc-expired-modal.tsx contains `onUpdateNow` and `onUpdateLater` props
    - src/components/features/kyc-expired-modal.tsx contains `expiredModal.title` i18n key usage
    - src/components/features/kyc-expired-modal.tsx contains consequences list items
  </acceptance_criteria>
  <verify>
    <automated>grep "AlertDialog" src/components/features/kyc-expired-modal.tsx && grep "onUpdateNow" src/components/features/kyc-expired-modal.tsx && grep "expiredModal" src/components/features/kyc-expired-modal.tsx</automated>
  </verify>
  <done>KYC expired modal renders with Clock icon, expiration message, consequences list, and Update Now / Update Later buttons. Uses shadcn AlertDialog for accessibility. All copy from i18n.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no type errors
- Re-submission page at `/(auth)/kyc/resubmit` renders field status rows
- Retake button opens camera overlay for individual field
- CTA disabled until all rejected fields re-captured
- KYC expired modal can be rendered when `open={true}`
- Full flow: document-type → capture (front/back/selfie) → processing → status → resubmit (if rejected) → processing → status
</verification>

<success_criteria>
Re-submission flow works end-to-end: rejected users see field-level status, retake specific fields, and resubmit. Expired modal displays consequences and routes to document type selection. All eKYC requirements EKYC-01 through EKYC-08 are fully addressed across plans 01-04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ekyc-onboarding/03-04-SUMMARY.md`
</output>
