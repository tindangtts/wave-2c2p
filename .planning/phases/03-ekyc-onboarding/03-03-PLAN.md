---
phase: 03-ekyc-onboarding
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/(auth)/kyc/processing/page.tsx
  - src/app/(auth)/kyc/status/page.tsx
  - src/components/features/processing-steps.tsx
  - src/components/features/kyc-status-card.tsx
autonomous: true
requirements: [EKYC-05, EKYC-06]
must_haves:
  truths:
    - "Processing screen shows 3 animated steps completing over ~3 seconds"
    - "Processing calls mock API and routes to status page based on result"
    - "Status page shows correct state (pending/approved/rejected/expired) with matching icon, color, and CTA"
    - "Approved status auto-redirects to /home after 2 seconds"
  artifacts:
    - path: "src/app/(auth)/kyc/processing/page.tsx"
      provides: "Verification processing screen with animated steps"
      contains: "ProcessingSteps"
    - path: "src/app/(auth)/kyc/status/page.tsx"
      provides: "KYC status page with 4 states"
      contains: "KYCStatusCard"
    - path: "src/components/features/processing-steps.tsx"
      provides: "Animated step list component"
      contains: "aria-live"
    - path: "src/components/features/kyc-status-card.tsx"
      provides: "Status display with icon, title, description"
      contains: "CheckCircle"
  key_links:
    - from: "src/app/(auth)/kyc/processing/page.tsx"
      to: "src/app/api/mock-kyc/submit/route.ts"
      via: "fetch POST to submit endpoint"
      pattern: "fetch.*mock-kyc/submit"
    - from: "src/app/(auth)/kyc/status/page.tsx"
      to: "src/stores/kyc-store.ts"
      via: "reads kycStatus and rejectionReasons from store"
      pattern: "useKYCStore"
---

<objective>
Processing and status screens: the verification processing animation and the KYC status page showing approval/rejection/pending/expired states.

Purpose: Completes the post-capture experience — user sees their verification being processed and gets a clear status with next-step guidance.
Output: Processing page with animated steps, status page with 4 states, ProcessingSteps and KYCStatusCard reusable components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ekyc-onboarding/03-CONTEXT.md
@.planning/phases/03-ekyc-onboarding/03-UI-SPEC.md
@.planning/phases/03-ekyc-onboarding/03-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/stores/kyc-store.ts:
  useKYCStore: { documentType, frontImage, backImage, selfieImage, kycStatus, rejectionReasons, submissionId, setSubmissionResult, clearAll }

From src/app/api/mock-kyc/submit/route.ts:
  POST body: { document_type, front_image, back_image, selfie_image }
  Response (success): { success: true, status: 'approved', verification_id: string }
  Response (rejected): { success: false, status: 'rejected', rejection_reasons: string[], verification_id: string }

<!-- Existing components -->
From src/components/layout/back-header.tsx:
  BackHeader({ title, onBack })

From src/components/ui/card.tsx:
  Card, CardContent, CardHeader, CardTitle

From src/components/ui/badge.tsx:
  Badge with variants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProcessingSteps component and processing page</name>
  <files>src/components/features/processing-steps.tsx, src/app/(auth)/kyc/processing/page.tsx</files>
  <read_first>
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 6: Processing contract, ProcessingSteps component contract)
    - src/stores/kyc-store.ts (store API, image getters, setSubmissionResult)
    - src/app/api/mock-kyc/submit/route.ts (API response shape from Plan 01)
    - src/components/features/step-indicator.tsx (component pattern reference)
  </read_first>
  <action>
1. Create `src/components/features/processing-steps.tsx`:
   - 'use client' directive
   - Props: `{ steps: { label: string, status: 'pending' | 'active' | 'complete' }[] }`
   - Container: `flex flex-col gap-4 w-full max-w-[300px]` with `aria-live="polite"`
   - Each step row: `flex items-center gap-3`
     - Icon circle: `w-10 h-10 rounded-full flex items-center justify-center`
       - Pending: `bg-[#F5F5F5]` — empty circle
       - Active: `bg-[#FFE600]` — Loader2 icon 20px `text-[#212121] animate-spin`
       - Complete: `bg-[#00C853]` — Check icon 20px `text-white`
     - Label: `text-base`
       - Pending: `text-[#9E9E9E]`
       - Active: `text-[#212121]`
       - Complete: `text-[#757575]`
   - Imports: Loader2, Check from lucide-react

2. Create `src/app/(auth)/kyc/processing/page.tsx`:
   - 'use client' directive
   - Import: ProcessingSteps, useKYCStore, useRouter, useTranslations('kyc'), Loader2
   - No BackHeader — no back navigation during processing (per UI-SPEC)
   - Hydration-safe mounted guard
   - Full screen layout: `flex flex-col items-center justify-center min-h-screen px-4 bg-white`
   - Processing spinner: Loader2 icon 48px, `text-[#FFE600] animate-spin`, `mb-6`
   - Title: `t('processing.title')` — `text-xl font-bold text-[#212121] text-center mb-2`
   - Subtitle: `t('processing.subtitle')` — `text-base text-[#757575] text-center mb-8`
   - ProcessingSteps component with 3 steps from i18n
   - Animation logic using `useEffect` + `useState`:
     - `steps` state: array of 3 items, all start as 'pending'
     - On mount, start animation sequence:
       - Immediately: step 0 = 'active'
       - After 1s: step 0 = 'complete', step 1 = 'active'
       - After 2s: step 1 = 'complete', step 2 = 'active'
       - After 3s: step 2 = 'complete', trigger API submission
     - Use `setTimeout` chain with cleanup in useEffect return
   - API submission after animation:
     - Read `documentType`, `frontImage`, `backImage`, `selfieImage` from store
     - `fetch('/api/mock-kyc/submit', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ document_type: documentType, front_image: frontImage, back_image: backImage, selfie_image: selfieImage }) })`
     - Parse response JSON
     - Call `store.setSubmissionResult({ kycStatus: response.status, rejectionReasons: response.rejection_reasons || [], submissionId: response.verification_id })`
     - Navigate to `/(auth)/kyc/status`
   - Error handling: if fetch fails, set kycStatus to 'pending' and navigate to status (shows pending state)
   - Prevent back navigation: `useEffect` with `history.pushState` to prevent back button
  </action>
  <acceptance_criteria>
    - src/components/features/processing-steps.tsx contains `aria-live="polite"`
    - src/components/features/processing-steps.tsx contains `Loader2` and `Check` imports
    - src/app/(auth)/kyc/processing/page.tsx contains `fetch.*mock-kyc/submit` pattern
    - src/app/(auth)/kyc/processing/page.tsx contains `setSubmissionResult`
    - src/app/(auth)/kyc/processing/page.tsx contains `setTimeout` for animation sequence
    - src/app/(auth)/kyc/processing/page.tsx does NOT contain `BackHeader`
  </acceptance_criteria>
  <verify>
    <automated>grep "aria-live" src/components/features/processing-steps.tsx && grep "mock-kyc/submit" src/app/'(auth)'/kyc/processing/page.tsx && grep "setSubmissionResult" src/app/'(auth)'/kyc/processing/page.tsx</automated>
  </verify>
  <done>Processing page shows 3 steps animating in sequence over ~3s, then submits to mock API, stores result, and navigates to status page.</done>
</task>

<task type="auto">
  <name>Task 2: Create KYCStatusCard component and status page</name>
  <files>src/components/features/kyc-status-card.tsx, src/app/(auth)/kyc/status/page.tsx</files>
  <read_first>
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 7: KYC Status Page layout, status states table, auto-redirect contract, KYCStatusCard component contract)
    - src/stores/kyc-store.ts (kycStatus, rejectionReasons, submissionId, documentType)
    - src/components/ui/card.tsx (Card, CardContent)
    - src/components/ui/badge.tsx (Badge variants)
    - src/components/ui/separator.tsx (Separator)
    - src/components/layout/back-header.tsx (BackHeader)
  </read_first>
  <action>
1. Create `src/components/features/kyc-status-card.tsx`:
   - 'use client' directive
   - Props: `{ status: 'pending' | 'approved' | 'rejected' | 'expired', submittedAt?: string, documentType?: string, verificationId?: string, rejectionReasons?: string[] }`
   - Status icon: 56x56px circle centered with `mb-4`
     - Pending: `bg-[#FF9800]` + Clock icon 24px white
     - Approved: `bg-[#00C853]` + CheckCircle icon 24px white
     - Rejected: `bg-[#F44336]` + XCircle icon 24px white
     - Expired: `bg-[#9E9E9E]` + Clock icon 24px white
   - `aria-label={'Status: ' + status}` on icon container
   - Card component with `w-full p-4 rounded-xl`:
     - Submitted date line: `text-xs text-[#757575]`
     - Document type line: `text-base text-[#212121] mt-2`
     - Reference line: `text-xs text-[#757575] mt-1`
     - If rejected and rejectionReasons provided:
       - Separator `my-3`
       - "Issues found:" heading: `text-xl font-bold text-[#212121] mb-2`
       - Reasons list: `<ul role="list">` with `<li>` items, each with XCircle 16px `text-[#F44336]` + reason text
   - Imports: Clock, CheckCircle, XCircle from lucide-react

2. Create `src/app/(auth)/kyc/status/page.tsx`:
   - 'use client' directive
   - Import: BackHeader, KYCStatusCard, Button, useKYCStore, useRouter, useTranslations('kyc')
   - Hydration-safe mounted guard
   - Read from store: `kycStatus`, `rejectionReasons`, `submissionId`, `documentType`
   - Layout per UI-SPEC Screen 7:
     - BackHeader: title `t('status.' + kycStatus + '.title')` — fallback to "Verification Status"
       - onBack: navigate to /home if approved, stay on page otherwise
     - Centered content: `flex flex-col items-center px-4 pt-8 pb-8`
     - Status title: `text-xl font-bold text-center mb-2` — `t('status.' + kycStatus + '.title')`
     - Status description: `text-base text-[#757575] text-center mb-6 max-w-[300px]` — `t('status.' + kycStatus + '.description')`
     - KYCStatusCard with props from store
     - Spacer: `flex-1`
     - Action area based on status:
       - Approved: Button `t('status.approved.cta')` ("Continue to Home"), variant default (yellow), `w-full h-12 rounded-full`, onClick → navigate to `/home`
       - Rejected: Button `t('status.rejected.cta')` ("Resubmit Documents"), variant default (yellow), `w-full h-12 rounded-full`, onClick → navigate to `/(auth)/kyc/resubmit`
       - Pending: Button `t('status.pending.cta')` ("Check Status"), variant outline, `w-full h-12 rounded-full` + note text `t('status.pending.note')` below
       - Expired: Button `t('status.expired.cta')` ("Update Documents"), variant default (yellow), `w-full h-12 rounded-full`, onClick → navigate to `/(auth)/kyc/document-type`
   - Auto-redirect on approval (per D-12):
     - `useEffect` when `kycStatus === 'approved'`: `setTimeout(() => router.push('/home'), 2000)` with cleanup
     - `aria-live="assertive"` div showing `t('status.approved.redirect')` ("Redirecting to home...") when approved
   - Date formatting: use `new Date().toLocaleDateString()` for submitted date display
  </action>
  <acceptance_criteria>
    - src/components/features/kyc-status-card.tsx contains `CheckCircle` and `XCircle` and `Clock` imports
    - src/components/features/kyc-status-card.tsx contains `aria-label` with status
    - src/components/features/kyc-status-card.tsx contains `role="list"` for rejection reasons
    - src/app/(auth)/kyc/status/page.tsx contains `aria-live="assertive"` for redirect announcement
    - src/app/(auth)/kyc/status/page.tsx contains `setTimeout.*router.push.*home.*2000`
    - src/app/(auth)/kyc/status/page.tsx renders different CTAs for each status (approved, rejected, pending, expired)
  </acceptance_criteria>
  <verify>
    <automated>grep "aria-live" src/app/'(auth)'/kyc/status/page.tsx && grep "CheckCircle" src/components/features/kyc-status-card.tsx && grep "role.*list" src/components/features/kyc-status-card.tsx</automated>
  </verify>
  <done>Status page renders 4 states with correct icon/color/CTA. Approved auto-redirects to /home after 2s. Rejected shows rejection reasons list. Pending shows check status button with note. Expired shows update documents button.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no type errors
- Processing page at `/(auth)/kyc/processing` animates 3 steps and calls mock API
- Status page at `/(auth)/kyc/status` renders correct state from store
- Approved status auto-redirects to /home after 2 seconds
- Rejected status shows rejection reasons with XCircle icons
</verification>

<success_criteria>
Processing animation and API submission work end-to-end. Status page correctly displays all 4 KYC states with appropriate CTAs, icons, and colors. Auto-redirect on approval works with accessible announcement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ekyc-onboarding/03-03-SUMMARY.md`
</output>
