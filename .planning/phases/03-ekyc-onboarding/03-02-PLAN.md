---
phase: 03-ekyc-onboarding
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/(auth)/kyc/document-type/page.tsx
  - src/app/(auth)/kyc/capture/page.tsx
  - src/components/features/camera-overlay.tsx
  - src/components/features/document-type-card.tsx
autonomous: true
requirements: [EKYC-01, EKYC-02, EKYC-03, EKYC-04]
must_haves:
  truths:
    - "User can select from 5 document types via radio card UI"
    - "User can capture front and back of document with guide overlay"
    - "User can capture selfie with circular face guide and pulse animation"
    - "Camera falls back to file picker on iOS PWA without error"
    - "User can review each capture and retake if needed"
  artifacts:
    - path: "src/app/(auth)/kyc/document-type/page.tsx"
      provides: "Document type selection screen"
      contains: "DocumentTypeCard"
    - path: "src/app/(auth)/kyc/capture/page.tsx"
      provides: "Camera capture flow (front, back, selfie, reviews)"
      contains: "CameraOverlay"
    - path: "src/components/features/camera-overlay.tsx"
      provides: "Full-screen camera overlay with guide frames"
      contains: "variant"
    - path: "src/components/features/document-type-card.tsx"
      provides: "Selectable document type radio card"
      contains: "role.*radio"
  key_links:
    - from: "src/app/(auth)/kyc/document-type/page.tsx"
      to: "src/stores/kyc-store.ts"
      via: "useKYCStore for document type selection"
      pattern: "useKYCStore"
    - from: "src/app/(auth)/kyc/capture/page.tsx"
      to: "src/components/features/camera-overlay.tsx"
      via: "component composition"
      pattern: "CameraOverlay"
---

<objective>
Document type selection and camera capture flow: the user-facing screens for choosing a document type, capturing front/back photos with guide overlay, and taking a selfie with circular face guide.

Purpose: Implements the core visual capture experience — the most distinctive screens in the eKYC flow. All captures happen within a single route to avoid iOS PWA camera permission re-prompts (per research decision).
Output: Document type selection page, camera capture page (state-driven multi-step), CameraOverlay component, DocumentTypeCard component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ekyc-onboarding/03-CONTEXT.md
@.planning/phases/03-ekyc-onboarding/03-UI-SPEC.md
@.planning/phases/03-ekyc-onboarding/03-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/stores/kyc-store.ts:
  export const useKYCStore (Zustand store with persist)
  State: captureStep, documentType, frontImage, backImage, selfieImage
  Actions: setDocumentType, setCaptureStep, setFrontImage, setBackImage, setSelfieImage, clearAll

From src/lib/kyc/schemas.ts:
  export const documentTypeSchema: z.ZodEnum<['national_id', 'work_permit', 'pink_card', 'owic', 'visa']>

<!-- Existing components -->
From src/components/layout/back-header.tsx:
  export function BackHeader({ title, onBack }: { title: string; onBack?: () => void })

From src/components/features/step-indicator.tsx:
  export function StepIndicator({ currentStep, totalSteps, variant, namespace })
  // After Plan 01: supports number type, 'dark' variant, custom namespace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DocumentTypeCard component and document type selection page</name>
  <files>src/components/features/document-type-card.tsx, src/app/(auth)/kyc/document-type/page.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (shadcn Card component API)
    - src/app/(auth)/register/personal-info/page.tsx (auth page pattern: 'use client', BackHeader, StepIndicator, form, Zustand hydration-safe read)
    - src/stores/kyc-store.ts (store API from Plan 01)
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 1: Document Type Selection layout and DocumentTypeCard contract)
  </read_first>
  <action>
1. Create `src/components/features/document-type-card.tsx`:
   - 'use client' directive
   - Props: `{ type: string, label: string, icon: LucideIcon, selected: boolean, onSelect: () => void }`
   - Render shadcn `Card` with: `flex-row items-center gap-3 p-4 min-h-[64px] cursor-pointer`
   - Selected state: `border-2 border-[#FFE600] bg-[#FFFDE7]`, unselected: `border border-[#E0E0E0]`
   - Left: icon component rendered at 24px, color `#757575` (unselected) or `#212121` (selected)
   - Center: label text `text-base text-[#212121] flex-1`
   - Right: radio indicator — 20px circle div. Unselected: `border-2 border-[#E0E0E0]`. Selected: `bg-[#FFE600]` with inner 8px white dot
   - `role="radio"` on card, `aria-checked={selected}` attribute
   - `onClick={onSelect}` on entire card
   - Border radius `rounded-xl` (--radius-lg = 12px)
   - Transition: `transition-colors duration-150`

2. Create `src/app/(auth)/kyc/document-type/page.tsx`:
   - 'use client' directive
   - Import: BackHeader, StepIndicator (from Plan 01 with namespace='kyc'), Button, DocumentTypeCard
   - Import: useKYCStore, useRouter, useTranslations('kyc')
   - Hydration-safe pattern: `useState(false)` for `mounted`, `useEffect(() => setMounted(true), [])`
   - Read `documentType` from store after mount
   - State: `selectedType` local state (synced from store on mount)
   - Document types array: `[ { type: 'national_id', icon: CreditCard }, { type: 'work_permit', icon: FileText }, { type: 'pink_card', icon: CreditCard }, { type: 'owic', icon: FileCheck }, { type: 'visa', icon: Stamp } ]`
   - Icon imports from lucide-react: `CreditCard`, `FileText`, `FileCheck`, `Stamp`
   - Layout per UI-SPEC Screen 1:
     - BackHeader with title `t('title')` ("Verify Identity"), onBack navigates to previous page
     - StepIndicator: `currentStep={1} totalSteps={5} namespace="kyc"`
     - Section title: `t('selectDocType.title')` — `text-xl font-bold text-[#212121]`
     - Section subtitle: `t('selectDocType.subtitle')` — `text-base text-[#757575]`
     - Document type list: `flex flex-col gap-3`
     - Each type: `<DocumentTypeCard type={dt.type} label={t('docTypes.' + dt.type)} icon={dt.icon} selected={selectedType === dt.type} onSelect={() => setSelectedType(dt.type)} />`
     - Spacer: `flex-1`
     - CTA: Button `t('cta.continue')`, `w-full h-12 rounded-full`, disabled when no type selected (opacity-50)
     - OnClick CTA: `store.setDocumentType(selectedType)`, `store.setCaptureStep('capture-front')`, navigate to `/(auth)/kyc/capture`
   - Container div with `role="radiogroup"` and `aria-label="Document type selection"`
   - Screen padding: `px-4 pt-6 pb-8` per UI-SPEC
  </action>
  <acceptance_criteria>
    - src/components/features/document-type-card.tsx contains `role="radio"`
    - src/components/features/document-type-card.tsx contains `aria-checked`
    - src/app/(auth)/kyc/document-type/page.tsx contains `DocumentTypeCard`
    - src/app/(auth)/kyc/document-type/page.tsx contains `useKYCStore`
    - src/app/(auth)/kyc/document-type/page.tsx contains `role="radiogroup"`
    - src/app/(auth)/kyc/document-type/page.tsx contains `national_id` and `work_permit` and `pink_card` and `owic` and `visa`
  </acceptance_criteria>
  <verify>
    <automated>grep -l "role.*radio" src/components/features/document-type-card.tsx && grep -l "radiogroup" src/app/'(auth)'/kyc/document-type/page.tsx && grep "national_id" src/app/'(auth)'/kyc/document-type/page.tsx</automated>
  </verify>
  <done>Document type selection renders 5 types as radio cards. Selection state persists to Zustand store. CTA navigates to capture page.</done>
</task>

<task type="auto">
  <name>Task 2: Create CameraOverlay component and multi-step capture page</name>
  <files>src/components/features/camera-overlay.tsx, src/app/(auth)/kyc/capture/page.tsx</files>
  <read_first>
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (Screen 2-5: Camera capture, review, face verify contracts and CameraOverlay component contract)
    - src/stores/kyc-store.ts (captureStep state machine, image setters)
    - src/components/features/step-indicator.tsx (dark variant API from Plan 01)
    - src/components/layout/back-header.tsx (BackHeader API)
    - src/components/ui/button.tsx (Button variants)
  </read_first>
  <action>
1. Create `src/components/features/camera-overlay.tsx`:
   - 'use client' directive
   - Props: `{ variant: 'document' | 'selfie', instruction: string, helper: string, onCapture: (imageData: string) => void, onBack: () => void }`
   - Full-screen overlay: `fixed inset-0 bg-[#1A1A2E]/90 z-50 flex flex-col`
   - Top safe zone: `pt-12 px-4 flex items-center justify-between`
     - Back button: ChevronLeft 24px `#FFFFFF`, `aria-label="Go back"`
   - Center content: `flex-1 flex flex-col items-center justify-center px-4`
     - Instruction text: `text-base text-white text-center mb-4`
     - Document variant guide frame:
       - Outer container with aspect ratio `[85.6/54]` (approximately 1.586:1), `max-w-[calc(100vw-64px)]`
       - Border: `2px border-white/80 rounded-lg`
       - 4 corner markers: absolute positioned, `border-[#FFE600] border-3` L-shapes, 20px arm length at each corner (use pseudo-elements or 4 small divs)
     - Selfie variant guide frame:
       - Circle: `w-60 h-60 rounded-full border-2 border-white/80 relative`
       - Pulse ring: `w-[248px] h-[248px] rounded-full absolute -inset-1 border-2 border-[#FFE600]/40 animate-pulse`
       - Face silhouette: subtle SVG path inside circle at `text-white/20` (optional decorative)
       - CSS keyframes for pulse: `@keyframes pulse { 0%,100%: opacity 0.4; 50%: opacity 0.1 }` — add via inline style or Tailwind animate-pulse
     - Helper text: `text-xs text-white/70 text-center mt-4`
   - Bottom safe zone: `pb-12 px-4 flex flex-col items-center gap-4`
     - Hidden file input: `<input ref={fileRef} type="file" accept="image/*" capture={variant === 'selfie' ? 'user' : 'environment'} className="hidden" onChange={handleCapture} />`
     - Shutter button: `w-16 h-16 rounded-full bg-white ring-4 ring-[#FFE600]`, onClick triggers `fileRef.current?.click()`
       - `aria-label={variant === 'selfie' ? 'Take selfie' : 'Capture photo'}`
     - Gallery link: `<button onClick={() => { /* trigger input without capture attr */ }} className="text-xs text-white/70 underline">` with text from props or hardcoded fallback
   - `handleCapture`: reads file via FileReader.readAsDataURL, calls `onCapture(base64String)`
   - Capture flash: brief white overlay div with `opacity transition-opacity duration-100` toggled on capture

2. Create `src/app/(auth)/kyc/capture/page.tsx`:
   - 'use client' directive
   - Single route with internal state machine driven by `captureStep` from Zustand store
   - Import: CameraOverlay, BackHeader, StepIndicator (dark variant), Button, useKYCStore, useRouter, useTranslations('kyc')
   - Hydration-safe: mounted guard
   - State machine rendering based on `captureStep`:
     - `'capture-front'`: CameraOverlay variant="document", instruction=`t('capture.frontInstruction')`, step 2 of 5
     - `'review-front'`: Review screen with captured image preview (per UI-SPEC Screen 3). Image in `max-w-[300px]` container, `rounded-lg border-2 border-[#E0E0E0]`. Two buttons: "Retake" (outline, sets step back to capture-front) and "Use Photo" (yellow, saves to store, advances to capture-back)
     - `'capture-back'`: CameraOverlay variant="document", instruction=`t('capture.backInstruction')`, step 3 of 5
     - `'review-back'`: Same review pattern, "Use Photo" advances to capture-selfie
     - `'capture-selfie'`: CameraOverlay variant="selfie", instruction=`t('face.instruction')`, step 4 of 5
     - `'review-selfie'`: Same review pattern, "Use Photo" advances to processing (navigates to `/(auth)/kyc/processing`)
   - Back navigation per UI-SPEC:
     - capture-front: back to document-type
     - review-front: back to capture-front (retake)
     - capture-back: back to review-front
     - review-back: back to capture-back
     - capture-selfie: back to review-back
     - review-selfie: back to capture-selfie
   - Review screen layout:
     - BackHeader with title `t('review.title')`
     - Image preview: `<img src={imageData} alt="Captured document" className="w-full max-w-[300px] rounded-lg border-2 border-[#E0E0E0] object-cover" />`
     - Hint text: `t('review.hint')` — `text-xs text-[#757575] text-center mt-4`
     - Action row: `flex gap-4 mt-6 w-full` with two flex-1 buttons
   - Camera overlay screens show StepIndicator with `variant="dark"` in the overlay top area (not in BackHeader)
  </action>
  <acceptance_criteria>
    - src/components/features/camera-overlay.tsx contains `capture=` attribute on input element
    - src/components/features/camera-overlay.tsx contains `variant` prop handling for 'document' and 'selfie'
    - src/components/features/camera-overlay.tsx contains `aria-label` on shutter button
    - src/components/features/camera-overlay.tsx contains `FileReader` for base64 conversion
    - src/app/(auth)/kyc/capture/page.tsx contains `captureStep` state machine
    - src/app/(auth)/kyc/capture/page.tsx contains `CameraOverlay`
    - src/app/(auth)/kyc/capture/page.tsx handles all 6 steps: capture-front, review-front, capture-back, review-back, capture-selfie, review-selfie
  </acceptance_criteria>
  <verify>
    <automated>grep "capture=" src/components/features/camera-overlay.tsx && grep "captureStep" src/app/'(auth)'/kyc/capture/page.tsx && grep "CameraOverlay" src/app/'(auth)'/kyc/capture/page.tsx</automated>
  </verify>
  <done>Camera overlay renders with document rectangle (corner markers) and selfie circle (pulse ring). Capture page flows through front→review→back→review→selfie→review via state machine. iOS falls back to file picker automatically via native input capture behavior. All images stored as base64 in Zustand store.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no type errors in kyc pages
- Document type selection page renders at `/(auth)/kyc/document-type`
- Camera capture page renders at `/(auth)/kyc/capture`
- Selecting a document type and tapping Continue navigates to capture
- Camera overlay shows document guide frame for front/back, circle for selfie
- Review screen shows captured image with Retake/Use Photo buttons
</verification>

<success_criteria>
All capture UI screens render correctly. User can select doc type, capture front/back/selfie, review each capture, and retake if needed. Camera input uses native `<input capture>` for cross-platform compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ekyc-onboarding/03-02-SUMMARY.md`
</output>
