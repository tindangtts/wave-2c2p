---
phase: 03-ekyc-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kyc/schemas.ts
  - src/stores/kyc-store.ts
  - messages/en/kyc.json
  - messages/th/kyc.json
  - messages/mm/kyc.json
  - src/i18n/request.ts
  - src/components/features/step-indicator.tsx
  - src/app/api/mock-kyc/submit/route.ts
autonomous: true
requirements: [EKYC-01, EKYC-05]
must_haves:
  truths:
    - "KYC Zod schemas validate document type and submission payloads"
    - "KYC Zustand store persists step progress and document type selection across browser refresh"
    - "StepIndicator renders 5 steps correctly for KYC flow"
    - "Mock KYC submit endpoint orchestrates document + face verification and writes to DB"
    - "All KYC copy is in i18n message files for en/th/mm"
  artifacts:
    - path: "src/lib/kyc/schemas.ts"
      provides: "Zod schemas for KYC forms and API payloads"
      contains: "documentTypeSchema"
    - path: "src/stores/kyc-store.ts"
      provides: "Zustand store for KYC flow state"
      contains: "useKYCStore"
    - path: "messages/en/kyc.json"
      provides: "English i18n messages for KYC"
      contains: "documentType"
    - path: "src/app/api/mock-kyc/submit/route.ts"
      provides: "KYC submission orchestration endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/stores/kyc-store.ts"
      to: "zustand/middleware persist"
      via: "localStorage persistence"
      pattern: "persist.*wave-kyc-state"
    - from: "src/app/api/mock-kyc/submit/route.ts"
      to: "src/app/api/mock-kyc/verify-document/route.ts"
      via: "internal fetch to verify endpoints"
      pattern: "fetch.*mock-kyc/verify"
---

<objective>
KYC infrastructure: schemas, state management, i18n, StepIndicator adaptation, and mock submission API.

Purpose: Establishes all foundation code that the camera capture, processing, and status screens depend on. No UI pages yet — this is the data layer and shared components.
Output: Zod schemas, Zustand store, i18n messages, adapted StepIndicator, mock submission API route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ekyc-onboarding/03-CONTEXT.md
@.planning/phases/03-ekyc-onboarding/03-UI-SPEC.md
@.planning/phases/03-ekyc-onboarding/03-RESEARCH.md

<interfaces>
<!-- Existing types from src/types/index.ts -->
export type KYCStatus = "pending" | "approved" | "rejected" | "expired" | "not_started";
export interface KYCDocument {
  id: string;
  user_id: string;
  document_type: "id_card" | "passport" | "work_permit" | "pink_card" | "owic" | "visa";
  front_image_url?: string;
  back_image_url?: string;
  selfie_image_url?: string;
  status: KYCStatus;
  rejection_reason?: string;
  verified_at?: string;
  expires_at?: string;
  created_at: string;
}

<!-- Existing registration store pattern from src/stores/registration-store.ts -->
import { create } from 'zustand'
import { persist, createJSONStorage } from 'zustand/middleware'
// Uses partialize to exclude actions from persistence

<!-- Existing StepIndicator from src/components/features/step-indicator.tsx -->
interface StepIndicatorProps {
  currentStep: 1 | 2 | 3
  totalSteps?: number
}
// Uses useTranslations('auth.register') for step label

<!-- Existing mock API pattern from src/app/api/mock-kyc/verify-document/route.ts -->
// Reads MOCK_KYC_AUTO_APPROVE and MOCK_KYC_DELAY_MS env vars at request time
// Returns { success, status, document_type, extracted_data, confidence_score } or rejection

<!-- Existing i18n loading from src/i18n/request.ts -->
// Spreads message files: { ...common, auth: authMessages }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KYC Zod schemas, Zustand store, and i18n messages</name>
  <files>src/lib/kyc/schemas.ts, src/stores/kyc-store.ts, messages/en/kyc.json, messages/th/kyc.json, messages/mm/kyc.json, src/i18n/request.ts</files>
  <read_first>
    - src/types/index.ts (KYCDocument type, document_type union)
    - src/stores/registration-store.ts (Zustand persist pattern to replicate)
    - src/i18n/request.ts (message loading pattern — spread under namespace)
    - messages/en/auth.json (i18n message structure to follow)
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (all copywriting contract values)
  </read_first>
  <action>
1. Create `src/lib/kyc/schemas.ts`:
   - `documentTypeSchema`: z.enum(['national_id', 'work_permit', 'pink_card', 'owic', 'visa']) — matches KYCDocument.document_type from types/index.ts
   - `kycSubmissionSchema`: z.object({ documentType: documentTypeSchema, frontImage: z.string().min(1), backImage: z.string().min(1), selfieImage: z.string().min(1) }) — base64 strings
   - `kycStatusSchema`: z.enum(['not_started', 'pending', 'approved', 'rejected', 'expired']) — matches KYCStatus type
   - Export all schemas and inferred types

2. Create `src/stores/kyc-store.ts` following registration-store.ts pattern:
   - State interface: `{ captureStep: 'select-type' | 'capture-front' | 'review-front' | 'capture-back' | 'review-back' | 'capture-selfie' | 'review-selfie' | 'processing' | 'status', documentType: string, frontImage: string, backImage: string, selfieImage: string, submissionId: string, kycStatus: KYCStatus, rejectionReasons: string[] }`
   - Actions: `setDocumentType`, `setCaptureStep`, `setFrontImage`, `setBackImage`, `setSelfieImage`, `setSubmissionResult`, `clearAll`
   - Persist middleware with key `'wave-kyc-state'`, partialize to exclude actions
   - Use `createJSONStorage(() => localStorage)` same as registration store

3. Create `messages/en/kyc.json` with ALL copy from UI-SPEC copywriting contract:
   - `title`: "Verify Identity"
   - `selectDocType.title`: "Select Document Type"
   - `selectDocType.subtitle`: "Choose the document you'd like to verify with"
   - `docTypes.national_id`: "National ID", `docTypes.work_permit`: "Work Permit", `docTypes.pink_card`: "Pink Card", `docTypes.owic`: "OWIC", `docTypes.visa`: "Visa"
   - `capture.frontInstruction`: "Position the FRONT of your document"
   - `capture.backInstruction`: "Position the BACK of your document"
   - `capture.helper`: "Align edges with the frame"
   - `capture.gallery`: "Choose from gallery"
   - `review.title`: "Review Photo"
   - `review.hint`: "Make sure text is clear and all edges are visible"
   - `review.retake`: "Retake"
   - `review.usePhoto`: "Use Photo"
   - `face.title`: "Take a Selfie"
   - `face.instruction`: "Position your face in the circle"
   - `face.helper`: "Hold steady and look straight ahead"
   - `processing.title`: "Verifying your identity"
   - `processing.subtitle`: "This usually takes a few seconds"
   - `processing.steps.document`: "Checking document quality"
   - `processing.steps.identity`: "Verifying identity"
   - `processing.steps.face`: "Matching face to document"
   - `status.pending.title`: "Verification In Progress"
   - `status.pending.description`: "We're reviewing your documents. This usually takes a few minutes."
   - `status.approved.title`: "Identity Verified"
   - `status.approved.description`: "Your identity has been verified. You can now use all features."
   - `status.approved.redirect`: "Redirecting to home..."
   - `status.approved.cta`: "Continue to Home"
   - `status.rejected.title`: "Verification Failed"
   - `status.rejected.description`: "We couldn't verify your identity. Please review the issues below and resubmit."
   - `status.rejected.cta`: "Resubmit Documents"
   - `status.rejected.issuesTitle`: "Issues found:"
   - `status.expired.title`: "Verification Expired"
   - `status.expired.description`: "Your verification has expired. Please submit updated documents to continue."
   - `status.expired.cta`: "Update Documents"
   - `status.pending.cta`: "Check Status"
   - `status.pending.note`: "We'll notify you when verification is complete"
   - `resubmit.title`: "Resubmit Documents"
   - `resubmit.subtitle`: "Only re-capture the items marked below"
   - `resubmit.sectionTitle`: "Fix the following issues"
   - `resubmit.cta`: "Resubmit for Verification"
   - `resubmit.accepted`: "Accepted"
   - `resubmit.retake`: "Retake"
   - `expiredModal.title`: "Documents Expired"
   - `expiredModal.description`: "Your identity documents have expired. To continue using transfers and deposits, please update your documents."
   - `expiredModal.consequences`: "Without updating, you won't be able to:"
   - `expiredModal.transfers`: "Send money transfers"
   - `expiredModal.deposits`: "Add funds to your wallet"
   - `expiredModal.withdrawals`: "Make withdrawals"
   - `expiredModal.updateNow`: "Update Now"
   - `expiredModal.updateLater`: "Update Later"
   - `cancelDialog.title`: "Cancel verification?"
   - `cancelDialog.description`: "Your progress will be saved. You can resume verification later."
   - `cancelDialog.cancel`: "Cancel"
   - `cancelDialog.continue`: "Continue Verification"
   - `errors.cameraPermission`: "Camera access is needed to capture your document. Please allow camera access in your browser settings."
   - `errors.captureFailed`: "Could not capture photo. Please try again or choose from gallery."
   - `errors.uploadTooLarge`: "Image is too large. Please try again with a smaller photo."
   - `errors.unsupportedFormat`: "Image format not supported. Please use JPG or PNG."
   - `errors.verificationFailed`: "Verification could not be completed. Please try again."
   - `errors.networkError`: "Connection lost. Your photos are saved. Please try again when connected."
   - `stepLabel`: "Step {current} of {total}"
   - `cta.continue`: "Continue"

4. Create `messages/th/kyc.json` — Thai translations for all keys above. Use appropriate Thai financial/legal terminology.

5. Create `messages/mm/kyc.json` — Myanmar/Burmese translations for all keys above. Use appropriate Myanmar terminology.

6. Update `src/i18n/request.ts`: Import and spread kyc messages under 'kyc' namespace alongside existing 'auth' and 'common' namespaces.
  </action>
  <acceptance_criteria>
    - src/lib/kyc/schemas.ts contains `export const documentTypeSchema`
    - src/lib/kyc/schemas.ts contains `export const kycSubmissionSchema`
    - src/stores/kyc-store.ts contains `export const useKYCStore`
    - src/stores/kyc-store.ts contains `wave-kyc-state` (persist key)
    - src/stores/kyc-store.ts contains `partialize`
    - messages/en/kyc.json contains `"selectDocType"` and `"status"` and `"processing"` and `"expiredModal"`
    - messages/th/kyc.json contains `"selectDocType"`
    - messages/mm/kyc.json contains `"selectDocType"`
    - src/i18n/request.ts contains `kyc` namespace import
  </acceptance_criteria>
  <verify>
    <automated>grep -l "documentTypeSchema" src/lib/kyc/schemas.ts && grep -l "useKYCStore" src/stores/kyc-store.ts && grep -l "selectDocType" messages/en/kyc.json && grep -l "kyc" src/i18n/request.ts</automated>
  </verify>
  <done>All KYC schemas, Zustand store, and i18n messages exist. Store uses persist middleware with localStorage. i18n messages cover all UI-SPEC copy in en/th/mm.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt StepIndicator for 5 steps and create mock submission API</name>
  <files>src/components/features/step-indicator.tsx, src/app/api/mock-kyc/submit/route.ts</files>
  <read_first>
    - src/components/features/step-indicator.tsx (current implementation — currentStep typed as 1|2|3)
    - src/app/api/mock-kyc/verify-document/route.ts (mock verify pattern, env var usage)
    - src/app/api/mock-kyc/verify-face/route.ts (face verify pattern)
    - src/lib/supabase/server.ts (Supabase server client creation pattern)
    - .planning/supabase-schema.sql (kyc_documents table schema)
    - .planning/phases/03-ekyc-onboarding/03-UI-SPEC.md (StepIndicator dark variant, processing contract)
  </read_first>
  <action>
1. Update `src/components/features/step-indicator.tsx`:
   - Change `currentStep` prop type from `1 | 2 | 3` to `number`
   - Keep `totalSteps` as optional `number` prop (default 3 for backward compat)
   - Add `variant` prop: `'light' | 'dark'` defaulting to `'light'`
   - When `variant="dark"`: dots use `#FFFFFF` for completed, `#FFE600` for active, `#FFFFFF/40%` for upcoming, connectors `#FFFFFF/40%` for inactive, `#FFFFFF` for active — all on dark background
   - When `variant="light"`: keep existing colors (`#0091EA` completed, `#FFE600` active, `#E0E0E0` upcoming)
   - Add `namespace` prop: `string` defaulting to `'auth.register'` — allows KYC to pass `'kyc'` namespace for step label i18n key
   - Update step label: `t('stepLabel', { current: currentStep, total: totalSteps })` — uses provided namespace
   - In dark variant, step label text color: `text-white/70`
   - Ensure backward compatibility: existing usage in registration pages continues to work unchanged (default props match current behavior)

2. Create `src/app/api/mock-kyc/submit/route.ts`:
   - POST handler accepting JSON body: `{ document_type, front_image, back_image, selfie_image }`
   - Validate `document_type` against `documentTypeSchema` from `src/lib/kyc/schemas.ts` using `z.safeParse`
   - Read `MOCK_KYC_AUTO_APPROVE` env var (default: `'true'`)
   - Read `MOCK_KYC_DELAY_MS` env var (default: `'1500'`)
   - Simulate processing delay: `await new Promise(r => setTimeout(r, delayMs))`
   - If auto-approve:
     - Create Supabase admin client (service role) to write to `kyc_documents` table
     - Insert row: `{ user_id: (from auth session), document_type, front_image_url: 'mock://front', back_image_url: 'mock://back', selfie_image_url: 'mock://selfie', status: 'approved', verified_at: new Date().toISOString() }`
     - Update `user_profiles.kyc_status` to `'approved'`
     - Return `{ success: true, status: 'approved', verification_id: 'KYC-{timestamp}' }`
   - If not auto-approve:
     - Pick random rejection reason from array: `["Document is blurry or unreadable.", "Document is expired.", "Name on document does not match registration.", "Photo unclear or face not visible."]`
     - Insert row with `status: 'rejected'` and `rejection_reason`
     - Update `user_profiles.kyc_status` to `'rejected'`
     - Return `{ success: false, status: 'rejected', rejection_reasons: [reason], verification_id: 'KYC-{timestamp}' }`
   - Error handling: try/catch wrapping, return 500 with `{ error: 'Verification could not be completed' }` on failure
   - Auth check: get session from Supabase, return 401 if no session
  </action>
  <acceptance_criteria>
    - src/components/features/step-indicator.tsx prop `currentStep` typed as `number` (not `1 | 2 | 3`)
    - src/components/features/step-indicator.tsx contains `variant` prop with `'dark'` option
    - src/components/features/step-indicator.tsx contains `namespace` prop
    - src/app/api/mock-kyc/submit/route.ts contains `export async function POST`
    - src/app/api/mock-kyc/submit/route.ts contains `MOCK_KYC_AUTO_APPROVE`
    - src/app/api/mock-kyc/submit/route.ts contains `kyc_documents`
    - src/app/api/mock-kyc/submit/route.ts contains `user_profiles`
  </acceptance_criteria>
  <verify>
    <automated>grep "currentStep.*number" src/components/features/step-indicator.tsx && grep "variant" src/components/features/step-indicator.tsx && grep "POST" src/app/api/mock-kyc/submit/route.ts && grep "kyc_documents" src/app/api/mock-kyc/submit/route.ts</automated>
  </verify>
  <done>StepIndicator supports 5+ steps with dark variant for camera overlays. Mock submission API orchestrates verification and writes to Supabase. Both backward-compatible with Phase 2.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no type errors related to StepIndicator or KYC schemas
- StepIndicator renders correctly in existing Phase 2 registration pages (backward compat)
- `curl -X POST http://localhost:3000/api/mock-kyc/submit -H "Content-Type: application/json" -d '{"document_type":"national_id","front_image":"test","back_image":"test","selfie_image":"test"}'` returns JSON response
</verification>

<success_criteria>
All KYC infrastructure code exists and compiles. Schemas validate, store persists, i18n loads, StepIndicator works for 5 steps, mock API returns configurable results.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ekyc-onboarding/03-01-SUMMARY.md`
</output>
