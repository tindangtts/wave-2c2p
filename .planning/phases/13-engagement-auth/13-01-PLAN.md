---
phase: 13-engagement-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/referral/stats/route.ts
  - src/app/(main)/profile/refer-friends/page.tsx
  - messages/en/profile.json
  - messages/th/profile.json
  - messages/mm/profile.json
autonomous: true
requirements:
  - REF-01
  - REF-02

must_haves:
  truths:
    - "User can see referred friends count and total bonus earned on the refer-friends page"
    - "User can share referral link via WhatsApp, Line, or copy-to-clipboard"
    - "Share buttons are visible below the referral QR card"
  artifacts:
    - path: "src/app/api/referral/stats/route.ts"
      provides: "GET endpoint returning { referredCount, totalBonus } for authenticated user"
      exports: ["GET"]
    - path: "src/app/(main)/profile/refer-friends/page.tsx"
      provides: "Referral page with stats display and social share buttons"
      contains: "wa.me"
  key_links:
    - from: "src/app/(main)/profile/refer-friends/page.tsx"
      to: "/api/referral/stats"
      via: "useSWR fetch on mount"
      pattern: "api/referral/stats"
    - from: "refer-friends/page.tsx"
      to: "WhatsApp/Line URL schemes"
      via: "anchor href or window.open"
      pattern: "wa.me|line.me"
---

<objective>
Add referral stats display (referred count + bonus earned) and social share buttons (WhatsApp, Line, copy link) to the existing refer-friends profile page, backed by a mock stats API route.

Purpose: REF-01 and REF-02 — users can see their referral impact and invite friends via preferred social channels.
Output: `/api/referral/stats` route + updated `refer-friends/page.tsx` with stats card and share buttons + i18n strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing ReferralCard component (src/components/features/referral-card.tsx) -->
```typescript
interface ReferralCardProps {
  referralCode: string
  monthlyCount?: number
}
export function ReferralCard({ referralCode, monthlyCount = 0 }: ReferralCardProps)
```

<!-- Existing refer-friends page uses these imports -->
import { BackHeader } from '@/components/layout/back-header'
import { ReferralCard } from '@/components/features/referral-card'
import { Button } from '@/components/ui/button'
// Navigates back to '/profile', uses REFERRAL_CODE = 'WAVE2C2P'
// Has handleShare() using navigator.share + fallback copy

<!-- Existing profile messages have: -->
// profile.referFriends.title, instruction, codeLabel, shareCta, monthlyCount
// profile.referFriends.monthlyCount uses {n} param

<!-- referrals table schema -->
// id, referrer_id, referee_id, code, status ('pending'|'completed'), reward_amount bigint, created_at
// reward_amount stored as satang (bigint) — 10000 satang = 100 THB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Referral stats API route</name>
  <files>src/app/api/referral/stats/route.ts</files>
  <read_first>
    - src/app/api/wallet/route.ts (server API pattern: createClient, getUser, isDemoMode)
    - src/lib/demo.ts (isDemoMode export)
  </read_first>
  <action>
Create GET handler at `src/app/api/referral/stats/route.ts`:

1. Import `createClient` from `@/lib/supabase/server`, `NextResponse` from `next/server`, `isDemoMode` from `@/lib/demo`.
2. Demo mode returns: `{ referredCount: 3, totalBonusSatang: 30000, referralCode: 'WAVE2C2P' }` (30000 satang = 300 THB mock bonus).
3. Real mode:
   - `createClient()` + `supabase.auth.getUser()` — return 401 if no user.
   - Query `referrals` table: `.from('referrals').select('id, status, reward_amount').eq('referrer_id', user.id)`.
   - `referredCount` = rows where `status === 'completed'`.
   - `totalBonusSatang` = sum of `reward_amount` on completed rows (null treated as 0).
   - Fetch user's referral code from `user_profiles`: `.select('referral_code').eq('id', user.id).single()` — if column missing, fall back to mock code `'WAVE2C2P'`.
4. Return `{ referredCount, totalBonusSatang, referralCode }` with status 200.
5. Wrap in try/catch, return 500 on unexpected error.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/referral/stats | grep -q "referredCount"</automated>
  </verify>
  <done>GET /api/referral/stats returns JSON with referredCount, totalBonusSatang, referralCode fields</done>
</task>

<task type="auto">
  <name>Task 2: Referral stats UI + social share buttons + i18n</name>
  <files>
    src/app/(main)/profile/refer-friends/page.tsx
    messages/en/profile.json
    messages/th/profile.json
    messages/mm/profile.json
  </files>
  <read_first>
    - src/app/(main)/profile/refer-friends/page.tsx (full file — current state)
    - messages/en/profile.json (referFriends section for existing keys)
    - messages/th/profile.json (referFriends section)
    - messages/mm/profile.json (referFriends section)
  </read_first>
  <action>
**i18n first** — add to the `referFriends` key in each messages file:

en:
```json
"referredCount": "{n} friends joined",
"totalBonus": "Total bonus earned",
"bonusValue": "{amount} THB",
"shareWhatsApp": "Share on WhatsApp",
"shareLine": "Share on Line",
"copyLink": "Copy Link",
"linkCopied": "Link copied!"
```
th: translate appropriately (Thai equivalents)
mm: translate appropriately (Burmese equivalents)

**Page rewrite** (replace entire `refer-friends/page.tsx`):

1. Add `useSWR` import (`import useSWR from 'swr'`) and fetch stats: `const { data: stats } = useSWR('/api/referral/stats')`.
2. Derive `referralCode` from `stats?.referralCode ?? 'WAVE2C2P'`.
3. Derive display values: `referredCount = stats?.referredCount ?? 0`, `totalBonusTHB = stats ? (stats.totalBonusSatang / 100).toFixed(0) : '0'`.
4. **Stats card** — render below the instruction `<p>` and above `<ReferralCard>`:
   ```
   <div className="grid grid-cols-2 gap-3">
     <div className="bg-white rounded-xl p-4 shadow-sm text-center">
       <p className="text-2xl font-bold text-foreground">{referredCount}</p>
       <p className="text-xs text-[#595959]">{t('referFriends.referredCount', { n: referredCount })}</p>
     </div>
     <div className="bg-white rounded-xl p-4 shadow-sm text-center">
       <p className="text-2xl font-bold text-[#0091EA]">{t('referFriends.bonusValue', { amount: totalBonusTHB })}</p>
       <p className="text-xs text-[#595959]">{t('referFriends.totalBonus')}</p>
     </div>
   </div>
   ```
5. **Social share buttons** — replace single yellow Share button with 3 buttons in a row + 1 copy button:
   - WhatsApp: `window.open('https://wa.me/?text=' + encodeURIComponent(shareText), '_blank')` — WhatsApp green bg (`bg-[#25D366]`) with white text, rounded-full, h-12, flex-1.
   - Line: `window.open('https://line.me/R/msg/text/' + encodeURIComponent(shareText), '_blank')` — Line green bg (`bg-[#06C755]`) with white text.
   - Copy Link: copies `shareUrl` to clipboard, shows sonner toast `t('referFriends.linkCopied')` — yellow bg `bg-[#FFE600]` text-foreground.
   - `shareText` = `` `Use my referral code ${referralCode} to join 2C2P Wave!\nhttps://wave.2c2p.com/register?ref=${referralCode}` ``
   - Keep existing `navigator.share()` as the primary "Share" button above the row (already present — update to use `shareUrl`).
6. Remove old `handleShare` + `fallbackCopy` functions; replace with `handleWhatsApp`, `handleLine`, `handleCopyLink` inline arrow functions.
7. Update `ReferralCard` `monthlyCount` prop to pass `referredCount`.
  </action>
  <verify>
    <automated>grep -n "wa.me" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/app/\(main\)/profile/refer-friends/page.tsx && grep -n "line.me" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/app/\(main\)/profile/refer-friends/page.tsx && grep -n "referral/stats" /Users/tindang/workspaces/tts/yoma/wave-2c2p/src/app/\(main\)/profile/refer-friends/page.tsx</automated>
  </verify>
  <done>Refer-friends page shows 2 stats cards (referredCount + totalBonus), 3 share buttons (WhatsApp/Line/Copy), and fetches live stats from /api/referral/stats</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors
- `/api/referral/stats` returns `{ referredCount, totalBonusSatang, referralCode }` JSON
- `refer-friends/page.tsx` contains `wa.me`, `line.me`, `api/referral/stats`
- i18n files contain `referredCount` and `shareWhatsApp` keys in all 3 locales
</verification>

<success_criteria>
- REF-01: Stats card visible on refer-friends page showing referred friend count and bonus earned
- REF-02: WhatsApp, Line, and copy-link buttons present and functional; navigator.share() used as primary share on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/13-engagement-auth/13-01-SUMMARY.md`
</output>
