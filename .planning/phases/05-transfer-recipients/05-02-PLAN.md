---
phase: 05-transfer-recipients
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/features/recipient-list.tsx
  - src/components/features/recipient-row.tsx
  - src/app/(main)/transfer/recipient/page.tsx
  - src/app/(main)/transfer/new-recipient/page.tsx
  - src/app/(main)/transfer/edit-recipient/[id]/page.tsx
  - src/hooks/use-recipients.ts
autonomous: true
requirements:
  - XFER-01
  - RCPT-01
  - RCPT-02
  - RCPT-03
  - RCPT-04
  - RCPT-05

must_haves:
  truths:
    - "User can see recipient list with favorites first, then alphabetical, and search filters both"
    - "User can create a new recipient with all AML/EDD fields and see it in the list"
    - "User can edit an existing recipient and see updates reflected"
    - "User can delete a recipient with confirmation dialog"
    - "User can toggle favorite with optimistic update"
    - "User can select a recipient and it persists in transfer store"
  artifacts:
    - path: "src/components/features/recipient-list.tsx"
      provides: "Sectioned list with favorites + all, search bar, create link"
      contains: "RecipientList"
    - path: "src/components/features/recipient-row.tsx"
      provides: "Row with avatar, name, type, star, kebab menu"
      contains: "RecipientRow"
    - path: "src/app/(main)/transfer/recipient/page.tsx"
      provides: "Recipient selection page for transfer flow"
    - path: "src/app/(main)/transfer/new-recipient/page.tsx"
      provides: "New recipient form with all AML/EDD fields"
    - path: "src/app/(main)/transfer/edit-recipient/[id]/page.tsx"
      provides: "Edit recipient form pre-filled with existing data"
    - path: "src/hooks/use-recipients.ts"
      provides: "SWR hook for recipient list data"
      contains: "useRecipients"
  key_links:
    - from: "src/app/(main)/transfer/recipient/page.tsx"
      to: "src/stores/transfer-store.ts"
      via: "setRecipient on row tap"
      pattern: "useTransferStore.*setRecipient"
    - from: "src/hooks/use-recipients.ts"
      to: "/api/recipients"
      via: "SWR fetch"
      pattern: "useSWR.*api/recipients"
    - from: "src/app/(main)/transfer/new-recipient/page.tsx"
      to: "src/lib/transfer/schemas.ts"
      via: "react-hook-form + Zod resolver"
      pattern: "zodResolver.*recipientFormSchema"
---

<objective>
Recipient management UI: list with favorites/search, new recipient form with AML/EDD fields, edit form, delete with confirmation, favorite toggle.

Purpose: Users must be able to select or create a recipient before entering transfer amounts. This is step 1 of the 5-step transfer flow.
Output: 3 route pages, 2 feature components, 1 SWR hook.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-transfer-recipients/05-CONTEXT.md
@.planning/phases/05-transfer-recipients/05-UI-SPEC.md
@.planning/phases/05-transfer-recipients/05-01-SUMMARY.md

@src/stores/transfer-store.ts
@src/lib/transfer/schemas.ts
@src/types/index.ts
@src/hooks/use-wallet.ts
@src/components/layout/back-header.tsx
@src/components/features/step-indicator.tsx

<interfaces>
From src/stores/transfer-store.ts (created in Plan 01):
```typescript
export function useTransferStore(): {
  selectedRecipient: Recipient | null
  setRecipient: (r: Recipient) => void
  reset: () => void
  // ... other fields
}
```

From src/lib/transfer/schemas.ts (created in Plan 01):
```typescript
export const recipientFormSchema: ZodSchema  // validates all new-recipient form fields
export const occupationOptions: string[]
export const purposeOptions: string[]
export const relationshipOptions: string[]
```

From src/types/index.ts:
```typescript
export interface Recipient {
  id: string; user_id: string; full_name: string; first_name: string; last_name: string;
  phone: string; country_code: string; transfer_type: TransferChannel;
  bank_name?: string; account_no?: string; nrc?: string; occupation?: string;
  transfer_purpose?: string; relationship?: string;
  address?: string; address_line_2?: string; city?: string; state_region?: string;
  is_favorite: boolean; created_at: string; updated_at: string;
}
```

From src/components/layout/back-header.tsx:
```typescript
export function BackHeader({ title, onBack }: { title: string; onBack?: () => void }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SWR hook and recipient list page with search, favorites, selection</name>
  <files>
    src/hooks/use-recipients.ts,
    src/components/features/recipient-list.tsx,
    src/components/features/recipient-row.tsx,
    src/app/(main)/transfer/recipient/page.tsx
  </files>
  <read_first>
    src/hooks/use-wallet.ts,
    src/stores/transfer-store.ts,
    src/types/index.ts,
    src/components/layout/back-header.tsx,
    src/app/(main)/home/page.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/hooks/use-recipients.ts`** — SWR hook following use-wallet.ts pattern exactly:
   - `useRecipients()`: fetches `GET /api/recipients`, returns `{ data: { recipients: Recipient[] }, error, isLoading, mutate }`. Same SWR_OPTIONS (revalidateOnFocus: true, dedupingInterval: 30000).
   - Export a `toggleFavorite(id, isFavorite, mutate)` helper that does optimistic update: mutate locally, PATCH `/api/recipients/{id}` with `{ is_favorite: !isFavorite }`, revert on error + show sonner toast "Could not update favorite".

2. **Create `src/components/features/recipient-row.tsx`** — per UI-SPEC RecipientRow Contract:
   - Props: `recipient: Recipient`, `isSelected: boolean`, `onSelect: () => void`, `onToggleFavorite: () => void`, `onEdit: () => void`, `onDelete: () => void`
   - Layout: `flex items-center gap-3 px-4 py-3 min-h-[64px]` with `border-b border-gray-100` (last-of-type no border).
   - Yellow circle avatar 48px with first initial (20px bold `#212121` on `bg-[#FFE600]`).
   - Star icon left of name: `Star` lucide 16px, filled `#0091EA` if favorite, outline `#C0C0C0` if not. `onClick` calls `onToggleFavorite`. Touch target 44x44 via padding.
   - Name: 16px bold `#212121`, truncate 1 line.
   - Transfer type below name: 12px regular `#757575`.
   - Kebab menu (right): `MoreVertical` lucide 24px `#757575`, 44x44px touch target. Uses shadcn `DropdownMenu` with "Edit" and "Delete" items. `aria-label="More options for {name}"`.
   - Selected state: 3px left border `#FFE600`, `bg-[#FFFDE7]`.
   - Press state: `active:bg-gray-50`.

3. **Create `src/components/features/recipient-list.tsx`** — per UI-SPEC Screen 1:
   - Props: `recipients: Recipient[]`, `isLoading: boolean`, `onSelect: (r: Recipient) => void`, `selectedId: string | null`, `onToggleFavorite: (id: string, isFav: boolean) => void`, `onEdit: (id: string) => void`, `onDelete: (id: string) => void`
   - Search bar: controlled input with Search icon, `rounded-xl bg-[#F5F5F5] border-none h-11`, 200ms debounce filter on `full_name` + `transfer_type` (client-side). X clear button when query non-empty.
   - "Create new recipient" link: `UserPlus` icon + text in `#0091EA`, full-width h-11 touch target, routes to `/transfer/new-recipient`.
   - Favorites section: "Favorites" label 12px `#757575`, renders starred recipients.
   - All section: "All" label 12px `#757575`, renders all recipients alphabetically.
   - Empty state: "No recipients yet" heading + "Add your first recipient to start sending money." body.
   - Search no results: 'No recipients match "{query}"'.
   - Loading: 5x skeleton rows 64px `rounded-xl animate-pulse`.
   - Delete triggers AlertDialog with recipient name + "This cannot be undone." + red "Delete" button + "Keep Recipient" cancel. On confirm: `DELETE /api/recipients/{id}`, optimistic remove, revert on error.

4. **Create `src/app/(main)/transfer/recipient/page.tsx`** — 'use client':
   - Uses BackHeader with title from `useTranslations('transfer')('title_recipient')`, back navigates to `/home`.
   - Uses `useRecipients()` hook for data.
   - Uses `useTransferStore()` for selectedRecipient.
   - On recipient row tap: `setRecipient(recipient)` + `router.push('/transfer/amount')`.
   - Wires RecipientList with all handlers (select, favorite toggle, edit -> `/transfer/edit-recipient/{id}`, delete -> API call + mutate).
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useRecipients" src/hooks/use-recipients.ts
    - grep -q "RecipientRow" src/components/features/recipient-row.tsx
    - grep -q "RecipientList" src/components/features/recipient-list.tsx
    - grep -q "useTransferStore" src/app/\(main\)/transfer/recipient/page.tsx
    - grep -q "setRecipient" src/app/\(main\)/transfer/recipient/page.tsx
    - grep -q "DropdownMenu" src/components/features/recipient-row.tsx
    - grep -q "AlertDialog" src/components/features/recipient-list.tsx
    - grep -q "Star" src/components/features/recipient-row.tsx
  </acceptance_criteria>
  <done>
    - Recipient list page renders with search, favorites section, all section
    - Favorite toggle works optimistically
    - Delete shows confirmation dialog
    - Selecting a recipient stores it and navigates to amount page
    - Skeleton loading state shows during fetch
  </done>
</task>

<task type="auto">
  <name>Task 2: New recipient form and edit recipient form</name>
  <files>
    src/app/(main)/transfer/new-recipient/page.tsx,
    src/app/(main)/transfer/edit-recipient/[id]/page.tsx
  </files>
  <read_first>
    src/lib/transfer/schemas.ts,
    src/hooks/use-recipients.ts,
    src/components/layout/back-header.tsx,
    src/components/ui/input.tsx,
    src/components/ui/select.tsx,
    src/components/ui/button.tsx,
    src/components/ui/label.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/app/(main)/transfer/new-recipient/page.tsx`** — 'use client', per UI-SPEC Screen 2 and D-06:
   - BackHeader with "New Recipient" title.
   - Single scrollable form using react-hook-form + zodResolver(recipientFormSchema).
   - **Section: Transfer Info** — Type of Transfer (Select with 4 channels: Bank Transfer, Wave App, Wave Agent, Cash Pickup). Conditional fields: when type='bank_transfer', show Bank Name (Input) and Account No (Input). These hide when other types selected.
   - **Divider** — 1px `#E0E0E0 my-6`.
   - **Section: Recipient Identity** — First Name, Last Name, NRC (optional — text input with placeholder "e.g. 12/DAGANA(N)123456"), Phone (Input with +95 prefix, 9 digits), Occupation (Select from occupationOptions).
   - **Section: Transfer Compliance** — Transfer Purpose (Select from purposeOptions, required), Relationship (Select from relationshipOptions, required).
   - **Section: Address** — Address Line 1 (required), Address Line 2 (optional), City/Township (required), State/Region (required).
   - Form field styling per UI-SPEC: Label 12px `#212121` above, Input h-12 rounded-xl border `#E0E0E0` default / 2px `#0091EA` focus / 1px `#F44336` error, error msg 12px `#F44336` mt-1. Required fields get `*` in `#F44336` after label. Optional fields get "(optional)" in 12px `#9E9E9E`.
   - Sticky bottom CTA: "Create New" button, rounded-full bg-[#FFE600] text-[#212121] full-width h-14. Loading state: spinner inside button, disabled.
   - On submit: POST to `/api/recipients`. On 201: sonner toast success, `router.push('/transfer/recipient')`, mutate recipients list. On 400: show inline field errors from Zod details.
   - All labels and copy from `useTranslations('transfer')`.

2. **Create `src/app/(main)/transfer/edit-recipient/[id]/page.tsx`** — 'use client':
   - Same form layout as new-recipient but pre-filled with existing data.
   - Fetch recipient by ID: `GET /api/recipients` then find by ID (or direct fetch if preferred). Show skeleton while loading.
   - On submit: PUT to `/api/recipients/{id}`. On success: toast + navigate back to `/transfer/recipient`, mutate.
   - BackHeader title "Edit Recipient".
   - CTA says "Save Changes" instead of "Create New".
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "recipientFormSchema" src/app/\(main\)/transfer/new-recipient/page.tsx
    - grep -q "zodResolver" src/app/\(main\)/transfer/new-recipient/page.tsx
    - grep -q "transfer_type" src/app/\(main\)/transfer/new-recipient/page.tsx
    - grep -q "bank_transfer" src/app/\(main\)/transfer/new-recipient/page.tsx
    - grep -q "Save Changes" src/app/\(main\)/transfer/edit-recipient/\[id\]/page.tsx
    - grep -q "PUT" src/app/\(main\)/transfer/edit-recipient/\[id\]/page.tsx
    - test -f src/app/\(main\)/transfer/edit-recipient/\[id\]/page.tsx
  </acceptance_criteria>
  <done>
    - New recipient form validates all AML/EDD fields with Zod
    - Bank fields show/hide based on transfer type
    - Edit form loads existing data and submits PUT
    - Form errors display inline per UI-SPEC styling
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Recipient list page renders at /transfer/recipient
- New recipient form at /transfer/new-recipient
- Edit recipient form at /transfer/edit-recipient/[id]
</verification>

<success_criteria>
- Complete recipient management flow: list, search, favorites, create, edit, delete
- All RCPT-01 through RCPT-05 requirements met
- XFER-01 (select recipient) functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-transfer-recipients/05-02-SUMMARY.md`
</output>
