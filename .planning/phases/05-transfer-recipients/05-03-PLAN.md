---
phase: 05-transfer-recipients
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/features/amount-input.tsx
  - src/components/features/conversion-card.tsx
  - src/components/features/channel-card.tsx
  - src/app/(main)/transfer/amount/page.tsx
  - src/app/(main)/transfer/channel/page.tsx
autonomous: true
requirements:
  - XFER-02
  - XFER-03
  - XFER-04
  - XFER-05

must_haves:
  truths:
    - "User can enter amount in THB with real-time MMK conversion displayed below"
    - "Exchange rate fetches on mount with 5-minute validUntil stored in transfer store"
    - "Amount validates min 100 THB, max 25000 THB, and wallet balance check"
    - "User can select from 4 receiving channels with per-channel fee breakdown"
    - "Selected channel and fee persist in transfer store for confirmation screen"
  artifacts:
    - path: "src/components/features/amount-input.tsx"
      provides: "Large numeric display with custom keypad"
      contains: "AmountInput"
    - path: "src/components/features/conversion-card.tsx"
      provides: "Side-by-side THB/MMK with flags and rate line"
      contains: "ConversionCard"
    - path: "src/components/features/channel-card.tsx"
      provides: "Selectable channel card with fee and radio indicator"
      contains: "ChannelCard"
    - path: "src/app/(main)/transfer/amount/page.tsx"
      provides: "Amount entry page with keypad, conversion, validation"
    - path: "src/app/(main)/transfer/channel/page.tsx"
      provides: "Channel selection page with 4 channel cards"
  key_links:
    - from: "src/app/(main)/transfer/amount/page.tsx"
      to: "/api/mock-payment/rate"
      via: "fetch on mount for exchange rate"
      pattern: "fetch.*api/mock-payment/rate"
    - from: "src/app/(main)/transfer/amount/page.tsx"
      to: "src/stores/transfer-store.ts"
      via: "setAmount, setRate on Next tap"
      pattern: "useTransferStore.*setAmount"
    - from: "src/app/(main)/transfer/channel/page.tsx"
      to: "src/stores/transfer-store.ts"
      via: "setChannel, setFee on Next tap"
      pattern: "useTransferStore.*setChannel"
---

<objective>
Amount entry with live MMK conversion and receiving channel selection with fee breakdown.

Purpose: Steps 2 and 3 of the 5-step transfer flow. User enters how much to send and how recipient will receive it.
Output: 2 route pages, 3 feature components.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-transfer-recipients/05-CONTEXT.md
@.planning/phases/05-transfer-recipients/05-UI-SPEC.md
@.planning/phases/05-transfer-recipients/05-01-SUMMARY.md

@src/stores/transfer-store.ts
@src/lib/transfer/schemas.ts
@src/lib/currency.ts
@src/types/index.ts
@src/hooks/use-wallet.ts
@src/components/layout/back-header.tsx

<interfaces>
From src/stores/transfer-store.ts (created in Plan 01):
```typescript
export function useTransferStore(): {
  selectedRecipient: Recipient | null
  amountSatang: number
  channel: TransferChannel | null
  rate: number
  rateValidUntil: string
  feeSatang: number
  setAmount: (satang: number) => void
  setChannel: (ch: TransferChannel) => void
  setRate: (rate: number, validUntil: string) => void
  setFee: (feeSatang: number) => void
}
```

From src/lib/currency.ts:
```typescript
export function formatCurrency(amountInSmallestUnit: number, currency: CurrencyCode): string
export function convertSatangToPya(satang: number, rateKyatPerBaht: number): number
export function toSmallestUnit(displayAmount: number, currency: CurrencyCode): number
export function fromSmallestUnit(amountInSmallestUnit: number, currency: CurrencyCode): number
```

From src/hooks/use-wallet.ts:
```typescript
export function useWallet(): { data: WalletData | undefined, error, isLoading, mutate }
// WalletData.wallet.balance is in satang (bigint from DB)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amount entry page with custom keypad, live conversion, and validation</name>
  <files>
    src/components/features/amount-input.tsx,
    src/app/(main)/transfer/amount/page.tsx
  </files>
  <read_first>
    src/stores/transfer-store.ts,
    src/lib/currency.ts,
    src/hooks/use-wallet.ts,
    src/components/layout/back-header.tsx,
    src/components/features/passcode-keypad.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/components/features/amount-input.tsx`** — per UI-SPEC Screen 3:
   - Props: `value: string` (display string like "1000.50"), `onChange: (val: string) => void`, `disabled?: boolean`
   - 3x4 numeric keypad grid with gap-2. Keys: 1-9, ".", 0, backspace (Delete lucide icon).
   - Each key: 68x56px, rounded-xl, bg-[#F5F5F5], 20px bold #212121. Active state: `active:bg-[#E0E0E0]`.
   - Digit tap: append to value string. Decimal tap: add "." if not already present. Backspace: remove last char. Long press backspace (300ms): clear entire amount.
   - Strip leading zeros: "007" -> "7". Max 8 characters total. Only allow 2 decimal places max.
   - Expose the keypad only — amount display is handled by the parent page.

2. **Create `src/app/(main)/transfer/amount/page.tsx`** — 'use client', per UI-SPEC Screen 3:
   - Guard: if `useTransferStore().selectedRecipient` is null, redirect to `/transfer/recipient`.
   - BackHeader with "Transfer" title. Below header: selected recipient row showing avatar + name + transfer type (12px, compact).
   - **Amount display area** (flex-col items-center, pt-12 pb-8):
     - Amount number: 48px bold #212121. If empty, show "0" in #9E9E9E.
     - "THB" label: 16px regular #757575, ml-2, baseline-aligned.
     - Converted amount: mt-2, 16px regular #757575 — "= {converted} MMK". Use `convertSatangToPya` from currency.ts for conversion. Debounce 100ms after keypress.
     - Rate line: mt-1, 12px regular #757575 — "1 THB = {rate} MMK".
   - **Exchange rate fetch**: On mount, fetch `GET /api/mock-payment/rate`. Store rate and validUntil in transfer store via `setRate()`. While loading, show skeleton bar 120x16px where conversion text goes.
   - **Validation** (per D-11): Display inline error 14px #F44336 text-center below conversion:
     - Amount < 100 THB (and non-zero): "Minimum transfer: 100 THB"
     - Amount > 25,000 THB: "Maximum transfer: 25,000.00 THB"
     - Amount > wallet balance: "Exceeds your available balance of {balance} THB" — use `useWallet()` for balance.
   - **AmountInput keypad** at bottom (mt-auto mb-4 px-4).
   - **Sticky CTA**: "Next" button, rounded-full bg-[#FFE600] text-[#212121] full-width h-14 mx-4. Disabled (bg-[#E0E0E0] text-[#9E9E9E]) when amount=0 or invalid. On tap: `setAmount(toSmallestUnit(parseFloat(amountStr), 'THB'))` + `router.push('/transfer/channel')`.
   - All text from `useTranslations('transfer')`.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AmountInput" src/components/features/amount-input.tsx
    - grep -q "convertSatangToPya" src/app/\(main\)/transfer/amount/page.tsx
    - grep -q "api/mock-payment/rate" src/app/\(main\)/transfer/amount/page.tsx
    - grep -q "useWallet" src/app/\(main\)/transfer/amount/page.tsx
    - grep -q "setAmount" src/app/\(main\)/transfer/amount/page.tsx
    - grep -q "setRate" src/app/\(main\)/transfer/amount/page.tsx
    - grep -q "Minimum transfer" src/app/\(main\)/transfer/amount/page.tsx
  </acceptance_criteria>
  <done>
    - Amount page shows numeric keypad with live MMK conversion
    - Rate fetched from mock API and stored with validUntil
    - Validation errors display inline for min/max/balance
    - Next button disabled when amount invalid
    - Navigates to channel selection on valid Next tap
  </done>
</task>

<task type="auto">
  <name>Task 2: Channel selection page with fee breakdown and conversion card</name>
  <files>
    src/components/features/conversion-card.tsx,
    src/components/features/channel-card.tsx,
    src/app/(main)/transfer/channel/page.tsx
  </files>
  <read_first>
    src/stores/transfer-store.ts,
    src/lib/currency.ts,
    src/types/index.ts,
    src/components/layout/back-header.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/components/features/conversion-card.tsx`** — per UI-SPEC ConversionCard Contract:
   - Props: `amountSatang: number`, `convertedPya: number`, `rate: number`
   - Layout: flex items-center gap-4, bg-[#F5F5F5] rounded-xl p-4.
   - Left: TH flag emoji + formatted THB amount (16px bold).
   - Arrow: `ArrowRight` lucide 20px #757575.
   - Right: MM flag emoji + formatted MMK amount (16px bold).
   - Rate line below: "1 THB = {rate} MMK" in 12px regular #757575, centered, mt-2.
   - Use `formatCurrency` from currency.ts for both amounts.

2. **Create `src/components/features/channel-card.tsx`** — per UI-SPEC ChannelCard Contract:
   - Props: `channel: TransferChannel`, `name: string`, `convertedAmount: string`, `fee: string`, `isSelected: boolean`, `onSelect: () => void`
   - Layout: flex items-center justify-between p-4 rounded-xl border min-h-[72px].
   - Left: 40px circle icon with channel-specific icon and bg color:
     - wave_agent: `Store` lucide on `#E8F5E9`
     - wave_app: `Smartphone` lucide on `#E3F2FD`
     - bank_transfer: `Landmark` lucide on `#F3E5F5`
     - cash_pickup: `Banknote` lucide on `#FFF3E0`
   - Channel name: 16px bold #212121. Converted amount below: 12px #757575.
   - Right: "Fee" label 12px #757575 above, fee value 16px bold #212121 below.
   - Far right: 20px radio circle — unselected: #E0E0E0 outline, selected: #0091EA filled.
   - Border states: unselected 1px #E0E0E0, selected 2px #0091EA + bg-[#F0F9FF].
   - Press: `active:scale-[0.98]` transform.

3. **Create `src/app/(main)/transfer/channel/page.tsx`** — 'use client', per UI-SPEC Screen 4:
   - Guard: if `useTransferStore().amountSatang` is 0, redirect to `/transfer/amount`.
   - BackHeader with "Receiving Channels" title.
   - ConversionCard at top showing THB amount -> MMK converted amount + rate (from store).
   - Hint text: "Select how the recipient will receive funds" (12px #757575, mx-4 mt-2).
   - 4 ChannelCard components in flex-col gap-3 mx-4 mt-4. Fee schedule per D-14: Wave Agent 10 THB, Wave App 10 THB, Bank Transfer 50 THB, Cash Pickup 30 THB. Define as const map. Convert fees to satang for storage: `toSmallestUnit(fee, 'THB')`.
   - Channel converted amounts: for each channel, compute `convertSatangToPya(amountSatang, rate)` and format with `formatCurrency`.
   - Single selection model. Default: none selected. "Next" disabled until selection.
   - Sticky CTA: "Next" button. On tap: `setChannel(selected)` + `setFee(toSmallestUnit(channelFee, 'THB'))` + `router.push('/transfer/confirm')`.
   - All text from `useTranslations('transfer')`.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ConversionCard" src/components/features/conversion-card.tsx
    - grep -q "ChannelCard" src/components/features/channel-card.tsx
    - grep -q "wave_agent" src/app/\(main\)/transfer/channel/page.tsx
    - grep -q "setChannel" src/app/\(main\)/transfer/channel/page.tsx
    - grep -q "setFee" src/app/\(main\)/transfer/channel/page.tsx
    - grep -q "ArrowRight" src/components/features/conversion-card.tsx
    - grep -q "Store" src/components/features/channel-card.tsx
    - grep -q "Landmark" src/components/features/channel-card.tsx
  </acceptance_criteria>
  <done>
    - Amount and channel selection flow complete
    - Conversion card shows THB/MMK with flags and rate
    - 4 channel cards with correct icons, fees, and selection states
    - Channel and fee stored in transfer store
    - Navigates to confirmation on Next
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Amount page at /transfer/amount shows keypad and live conversion
- Channel page at /transfer/channel shows 4 selectable cards
</verification>

<success_criteria>
- Amount entry with real-time MMK conversion and validation
- Channel selection with per-channel fee breakdown
- Both pages read/write transfer store correctly
- XFER-02 through XFER-05 requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/05-transfer-recipients/05-03-SUMMARY.md`
</output>
