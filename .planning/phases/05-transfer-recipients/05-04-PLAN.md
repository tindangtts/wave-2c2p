---
phase: 05-transfer-recipients
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/components/features/rate-timer.tsx
  - src/components/features/passcode-sheet.tsx
  - src/components/features/transfer-receipt.tsx
  - src/app/(main)/transfer/confirm/page.tsx
  - src/app/(main)/transfer/receipt/page.tsx
autonomous: true
requirements:
  - XFER-06
  - XFER-07
  - XFER-08
  - XFER-09

must_haves:
  truths:
    - "Confirmation screen shows full transfer summary with amount, converted, rate, fees, total, sender, receiver"
    - "Rate lock timer counts down from validUntil and changes color at 3min/1min thresholds"
    - "User enters passcode in bottom sheet and transfer submits after verification"
    - "Receipt shows success badge, reference number, amounts, rate, timestamp, share button"
    - "Transfer status polls from pending to completed and receipt card reveals on completion"
  artifacts:
    - path: "src/components/features/rate-timer.tsx"
      provides: "MM:SS countdown badge with color thresholds"
      contains: "RateTimer"
    - path: "src/components/features/passcode-sheet.tsx"
      provides: "Bottom sheet with 6-digit passcode input"
      contains: "PasscodeSheet"
    - path: "src/components/features/transfer-receipt.tsx"
      provides: "Receipt card with success badge, breakdown, share"
      contains: "TransferReceipt"
    - path: "src/app/(main)/transfer/confirm/page.tsx"
      provides: "Confirmation page with summary, timer, passcode"
    - path: "src/app/(main)/transfer/receipt/page.tsx"
      provides: "Receipt page with status polling and receipt display"
  key_links:
    - from: "src/app/(main)/transfer/confirm/page.tsx"
      to: "/api/mock-payment/process-transfer"
      via: "POST on passcode verification"
      pattern: "fetch.*api/mock-payment/process-transfer"
    - from: "src/app/(main)/transfer/confirm/page.tsx"
      to: "src/lib/auth/passcode.ts"
      via: "Server-side passcode verification"
      pattern: "verifyPasscode|api/auth"
    - from: "src/app/(main)/transfer/receipt/page.tsx"
      to: "/api/mock-payment/status/[id]"
      via: "setInterval polling every 2s"
      pattern: "api/mock-payment/status"
    - from: "src/app/(main)/transfer/receipt/page.tsx"
      to: "src/stores/transfer-store.ts"
      via: "reads transfer data, calls reset on close"
      pattern: "useTransferStore.*reset"
---

<objective>
Confirmation with passcode entry and receipt with status polling — the final 2 screens of the transfer flow.

Purpose: User reviews the full transfer summary, confirms with passcode, and receives a receipt with reference number. This completes the core remittance value proposition.
Output: 2 route pages, 3 feature components.
</objective>

<execution_context>
@.claude/agents/gsd-executor.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-transfer-recipients/05-CONTEXT.md
@.planning/phases/05-transfer-recipients/05-UI-SPEC.md
@.planning/phases/05-transfer-recipients/05-01-SUMMARY.md
@.planning/phases/05-transfer-recipients/05-02-SUMMARY.md
@.planning/phases/05-transfer-recipients/05-03-SUMMARY.md

@src/stores/transfer-store.ts
@src/lib/currency.ts
@src/lib/auth/passcode.ts
@src/types/index.ts
@src/hooks/use-wallet.ts
@src/components/layout/back-header.tsx
@src/components/features/passcode-keypad.tsx

<interfaces>
From src/stores/transfer-store.ts (created in Plan 01):
```typescript
export function useTransferStore(): {
  selectedRecipient: Recipient | null
  amountSatang: number
  channel: TransferChannel | null
  rate: number
  rateValidUntil: string
  feeSatang: number
  note: string
  transactionId: string
  status: 'idle' | 'pending' | 'processing' | 'completed' | 'failed'
  setNote: (n: string) => void
  setTransactionId: (id: string) => void
  setStatus: (s: string) => void
  reset: () => void
}
```

From src/lib/auth/passcode.ts:
```typescript
export function verifyPasscode(passcode: string, stored: string): boolean
```

From src/components/features/passcode-keypad.tsx:
```typescript
export function PasscodeKeypad({ value, onChange, onComplete, error, isLoading }): JSX.Element
```

From src/lib/currency.ts:
```typescript
export function formatCurrency(amountInSmallestUnit: number, currency: CurrencyCode): string
export function convertSatangToPya(satang: number, rate: number): number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmation page with rate timer, summary, note, and passcode bottom sheet</name>
  <files>
    src/components/features/rate-timer.tsx,
    src/components/features/passcode-sheet.tsx,
    src/app/(main)/transfer/confirm/page.tsx
  </files>
  <read_first>
    src/stores/transfer-store.ts,
    src/lib/currency.ts,
    src/lib/auth/passcode.ts,
    src/hooks/use-wallet.ts,
    src/components/features/passcode-keypad.tsx,
    src/components/layout/back-header.tsx,
    src/components/ui/drawer.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/components/features/rate-timer.tsx`** — per UI-SPEC Rate Lock Timer interaction contract:
   - Props: `validUntil: string` (ISO timestamp), `onExpired: () => void`
   - Uses `useEffect` with `setInterval(1000)` to compute remaining seconds from `validUntil - Date.now()`.
   - Display: "Rate expires in MM:SS" in a rounded-full badge with px-3 py-1.
   - Color states per UI-SPEC: >3min: `#212121` text on `bg-[#F5F5F5]`; 1-3min: `#FF9800` text on `bg-[#FFF3E0]` + copy changes to "Rate expires soon — MM:SS remaining"; <60s: `#F44336` text on `bg-[#FFEBEE]` + pulsing animation (`animate-pulse`).
   - When timer hits 0: call `onExpired()`. Clean up interval on unmount.

2. **Create `src/components/features/passcode-sheet.tsx`** — per UI-SPEC PasscodeSheet Contract:
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onVerified: () => void`
   - Uses Vaul `Drawer` (from shadcn drawer component) with snap-points [0.4].
   - Title: "Enter Passcode" 20px bold centered. Subtitle: "Confirm your transfer" 12px #757575.
   - Reuses `PasscodeKeypad` component from Phase 2 for the 6-dot input and keypad.
   - On complete (6 digits): POST to `/api/auth/verify-passcode` (or similar API — check existing auth routes for passcode verification endpoint). If passcode doesn't exist as API, create an inline `fetch('/api/auth/verify-passcode', { method: 'POST', body: JSON.stringify({ passcode }) })` that checks against stored hash. The passcode-keypad already handles auto-submit at 6 digits.
   - Error state: dots shake animation (CSS `@keyframes shake` — 3 oscillations, 300ms), "Incorrect passcode. {N} attempt(s) remaining." in 12px #F44336.
   - Attempt counter: 3 max. After 3 failures: "Too many attempts. Try again in 5 minutes." and sheet closes.
   - Success: call `onVerified()`, sheet closes.
   - Close on backdrop tap allowed.

3. **Create `src/app/(main)/transfer/confirm/page.tsx`** — 'use client', per UI-SPEC Screen 5:
   - Guard: if no channel in store, redirect to `/transfer/channel`.
   - BackHeader "Confirmation".
   - **Transfer summary** (centered, px-4 pt-4):
     - "Transfer" label 12px #757575.
     - Amount: 28px bold #212121 — formatted THB from store.
     - Converted: 16px #757575 — formatted MMK.
   - **RateTimer** component, mt-3 centered. `onExpired`: re-fetch rate from `/api/mock-payment/rate`, update store, show sonner toast "Exchange rate expired. Fetching new rate...".
   - **Sender/Receiver card** (mt-4, bg-white rounded-xl border p-4):
     - Sender: avatar with user initials (bg-[#E3F2FD]), name, phone from user profile (use `useWallet()` data for user info).
     - ArrowDown icon between.
     - Receiver: avatar with recipient initials (bg-[#FFE600]), name, transfer type. From store.selectedRecipient.
   - **Fee breakdown card** (mt-4):
     - Row: "Amount" — formatted THB (label #757575 left, value bold #212121 right).
     - Row: "Fee" — formatted THB in #F44336.
     - Separator 1px #E0E0E0.
     - Row: "Total" — 20px bold both sides. Total = amountSatang + feeSatang.
   - **Note field** (mt-4): Label "Note (optional)", Textarea 3 rows, rounded-xl, border #E0E0E0. Wired to `setNote()`.
   - **Sticky CTA**: "Confirm" button. On tap: open PasscodeSheet.
   - **PasscodeSheet**: `onVerified` handler:
     1. Check rate not expired. If expired, re-fetch and show "Rate updated. Please confirm again." toast, return.
     2. POST to `/api/mock-payment/process-transfer` with `{ amount: fromSmallestUnit(amountSatang, 'THB'), currency: 'THB', recipient_id: selectedRecipient.id, channel }`.
     3. On success: `setTransactionId(response.transfer.reference_number)`, `setStatus('pending')`, `router.push('/transfer/receipt')`.
     4. On error: sonner toast with error message.
     5. Also call `useWallet().mutate()` to trigger balance refresh.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RateTimer" src/components/features/rate-timer.tsx
    - grep -q "onExpired" src/components/features/rate-timer.tsx
    - grep -q "PasscodeSheet" src/components/features/passcode-sheet.tsx
    - grep -q "PasscodeKeypad" src/components/features/passcode-sheet.tsx
    - grep -q "Drawer" src/components/features/passcode-sheet.tsx
    - grep -q "process-transfer" src/app/\(main\)/transfer/confirm/page.tsx
    - grep -q "RateTimer" src/app/\(main\)/transfer/confirm/page.tsx
    - grep -q "PasscodeSheet" src/app/\(main\)/transfer/confirm/page.tsx
    - grep -q "setTransactionId" src/app/\(main\)/transfer/confirm/page.tsx
  </acceptance_criteria>
  <done>
    - Confirmation screen shows full summary with sender/receiver, amounts, fees, total
    - Rate timer counts down with color thresholds and auto-refreshes on expiry
    - Passcode sheet opens on Confirm tap, verifies against stored hash
    - Transfer submits to mock API and navigates to receipt
  </done>
</task>

<task type="auto">
  <name>Task 2: Receipt page with status polling, receipt card, and share functionality</name>
  <files>
    src/components/features/transfer-receipt.tsx,
    src/app/(main)/transfer/receipt/page.tsx
  </files>
  <read_first>
    src/stores/transfer-store.ts,
    src/lib/currency.ts,
    src/types/index.ts,
    src/components/layout/back-header.tsx,
    .planning/phases/05-transfer-recipients/05-UI-SPEC.md
  </read_first>
  <action>
1. **Create `src/components/features/transfer-receipt.tsx`** — per UI-SPEC Screen 6 Receipt card:
   - Props: `transactionId: string`, `amount: number` (satang), `fee: number` (satang), `convertedPya: number`, `rate: number`, `channel: TransferChannel`, `senderName: string`, `senderPhone: string`, `recipientName: string`, `recipientType: string`, `note?: string`, `createdAt: string`
   - Receipt card layout: mx-4 mt-4 bg-white rounded-xl border p-4.
   - **Success area** (centered, pb-4, border-b border-gray-100):
     - `CheckCircle` lucide 48px #00C853 mb-2.
     - "Success!" 16px bold #00C853.
     - "2c2p WAVE" 14px #757575 mt-1.
     - Date formatted: "14 Apr 2026, 10:30 AM" using date-fns `format(date, "dd MMM yyyy, hh:mm a")`.
   - **Reference**: "Ref: {transactionId}" 12px #757575 centered.
   - **Transfer section**: "TRANSFER" label 12px bold #757575 uppercase tracking-wide. "From" row: label left, sender name + phone right. "To" row: label left, recipient name + transfer type right.
   - **Separator** 1px my-4.
   - **Amount breakdown**:
     - "Amount" row: formatted THB — label #757575, value bold #212121.
     - "Fee" row: formatted THB in bold #F44336.
     - "Converted" row: formatted MMK in regular #757575.
     - Separator.
     - "Total" row: 20px bold both sides. Total = amount + fee.
   - **Note section** (if note): "Note" label #757575, note text #212121.
   - **Share button**: `Share2` lucide 20px + "Share" text in #0091EA centered, 44x44 touch target. On tap: `navigator.share({ text: 'Transfer of {THB} to {name} completed. Ref: {id}. 2c2p WAVE' })` if available, else `navigator.clipboard.writeText()` + sonner toast "Receipt copied to clipboard".

2. **Create `src/app/(main)/transfer/receipt/page.tsx`** — 'use client', per UI-SPEC Screen 6:
   - Guard: if no transactionId in store, redirect to `/home`.
   - Header: BackHeader variant with "Transaction Detail" title + X close button right instead of back arrow. X button navigates to `/home` and calls `store.reset()`.
   - **Status polling** (per D-16): On mount, start `setInterval` every 2s calling `GET /api/mock-payment/status/{transactionId}`. Update store status. Stop polling when status is `completed` or `failed`.
   - **Pending state**: Show spinner (24px #0091EA animate-spin) + "Processing your transfer..." text centered. Hide receipt card.
   - **Completed state**: Reveal full TransferReceipt card with all data from store.
   - **Failed state**: Show `XCircle` 48px #F44336 + "Transfer failed" heading + "Please contact support for assistance." body. No receipt card.
   - **Sticky bottom CTA**: "Close" button, bg-[#FFE600]. On tap: `store.reset()` + `router.push('/home')`. Also trigger `useWallet().mutate()` to refresh balance.
   - Clean up polling interval on unmount.
   - All text from `useTranslations('transfer')`.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tts/yoma/wave-2c2p && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TransferReceipt" src/components/features/transfer-receipt.tsx
    - grep -q "CheckCircle" src/components/features/transfer-receipt.tsx
    - grep -q "navigator.share" src/components/features/transfer-receipt.tsx
    - grep -q "setInterval" src/app/\(main\)/transfer/receipt/page.tsx
    - grep -q "mock-payment/status" src/app/\(main\)/transfer/receipt/page.tsx
    - grep -q "reset" src/app/\(main\)/transfer/receipt/page.tsx
    - grep -q "Processing" src/app/\(main\)/transfer/receipt/page.tsx
    - grep -q "XCircle" src/app/\(main\)/transfer/receipt/page.tsx
  </acceptance_criteria>
  <done>
    - Receipt page polls transfer status every 2s
    - Pending state shows spinner, completed shows full receipt
    - Failed state shows error card
    - Share button uses Web Share API with clipboard fallback
    - Close clears transfer store and navigates home
    - Wallet balance refreshes after transfer
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Confirmation page at /transfer/confirm shows full summary
- Receipt page at /transfer/receipt shows status polling -> receipt
- Full 5-step transfer flow works end-to-end: recipient -> amount -> channel -> confirm -> receipt
</verification>

<success_criteria>
- Confirmation screen with rate timer, sender/receiver, fee breakdown, passcode entry
- Receipt with status polling, reference number, amounts, share
- Complete transfer flow from recipient selection to receipt
- XFER-06 through XFER-09 requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/05-transfer-recipients/05-04-SUMMARY.md`
</output>
